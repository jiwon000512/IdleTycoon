# 동물원 타이쿤 — 포트폴리오 (AI 활용 · 기술 중심)

목적: 매출과 무관하게 "AI 에이전트와 1인 개발로 모바일 게임을 어떻게 만들었나"를 보여 주는 문서. **기능 하나를 완성할 때마다 4장에 한 절을 추가**한다. 기획 내용은 `동물원-타이쿤-기획서.md`, 절차·규칙은 `코드-규칙.md`가 원본이고 여기는 요약·성과만 둔다.

---

## 1. 한 줄 요약

Unity 6 · URP · 세로 모바일 방치형 타이쿤. 기획서 작성부터 코드·씬·프리팹·더미 리소스·테스트까지 **Claude Code 에이전트가 Unity 에디터를 CLI로 직접 조작**해 구현하고, 사람은 결정과 확인만 한다.

## 2. 기술 스택과 구조 결정

| 항목 | 결정 | 왜 |
|---|---|---|
| 엔진 | Unity 6000.3, URP 2D Renderer, uGUI + TMP, Input System | 모바일 2D 표준 구성 |
| 계층 | asmdef 6개(`Core / Data / Game / UI / World / Editor`) + 테스트. `Core`·`Data`는 `noEngineReferences` | 게임 규칙을 UnityEngine 없이 순수 C#으로 고정해 유닛 테스트 가능. 의존은 `Game → UI/World/Data → Core` 한 방향 |
| 패턴 | MVP(View MonoBehaviour + Presenter 순수 C#), `GameManager`(MonoSingleton)가 상태·서비스를 만들어 보관하고 씬 스크립트가 생성자 주입으로 Presenter 조립, C# `event` | 게임 규칙은 순수 C# 서비스에만. 싱글턴은 상태 보관·틱 호출용 하나뿐, `Find` 없음 |
| 데이터 | JSON 테이블 5개 단일 원본 + Newtonsoft, `TableValidator`가 규칙 검증. ScriptableObject 미사용 | 기획서 숫자 → JSON → 테스트의 한 줄 흐름. 에셋은 경로 칼럼(`animals.sprite`) |
| 공통 기반 | 자체 UPM 패키지 GameKit(`MonoSingleton`, UI·Table·Data·Event·Pool Manager) | 다음 프로젝트에 재사용 |
| 시점 | 원근 카메라(55° 내려다봄) + XZ 평면 + 카메라를 향해 세운 2D 스프라이트 카드 | 방향 1개 스프라이트로 입체감. 쿼터뷰·우리 단위 원근을 시도한 뒤 기각(4.3) |
| 문서 | 기획서(v0.x) · 코드 규칙 · 프로그래밍 규약 · 데이터 테이블 규칙 · 기능별 설계 문서 · 진행상황(세션 인수인계) | 에이전트가 세션마다 같은 규칙으로 일하게 하는 장치 |

## 3. AI 활용 방식

### 3.1 작업 절차 (기능 하나 = 세션 하나)

1. **설계**: 에이전트가 `기획/설계/NN-*.md`에 클래스·책임·의존·검증 항목을 쓰고, 결정이 필요한 것은 객관식으로 묻는다. 사람이 확정.
2. **구현**: 에이전트가 한 번에 전부 구현. 스크립트뿐 아니라 **씬·프리팹·게임오브젝트·더미 스프라이트까지 Unity CLI(`run_script`, `recompile`, `capture_game_view`)로 에디터 안에서 생성**한다.
3. **검증**: 에이전트가 1회 실행 — 컴파일 0·콘솔 0·EditMode 테스트·플레이 모드 캡처.
4. **보고**: 만든 것·검증 결과·달라진 점을 짧게. 진행상황·포트폴리오 갱신.

### 3.2 에이전트가 맡은 것 / 사람이 맡은 것

| 에이전트 | 사람 |
|---|---|
| 기획서 초안·버전 관리, 레퍼런스 조사, 수치 초안(비용 곡선·확률·레벨 표) | 장르·루프·시점 같은 방향 결정, 객관식 답변 |
| 설계 문서, 전 코드, 테스트, JSON, 씬·프리팹 조립, 더미 리소스 생성 | 플레이 감각 확인("작동은 잘돼", "원근감이 안 나타나"), 스크린샷으로 문제 제보 |
| 규칙 문서 유지(코드 규칙·데이터 규칙·규약), 세션 인수인계 문서 | 절차 자체를 바꾸는 결정 |
| 커밋 메시지 | 커밋 시점 결정 |

### 3.3 재현 가능하게 만든 장치

- **규칙 문서가 코드보다 먼저**: 표기·계층·오류 처리·이벤트·테스트 규칙을 문서로 두고 에이전트가 grep으로 스스로 검사(`UnityEngine` in Core, `Find`, 한국어 리터럴 0건).
- **UI 문구 전부 `strings.json`**: 코드·프리팹에 한국어 리터럴 금지 → 다국어 대비.
- **더미 리소스 우선**: 에이전트가 단색 스프라이트·프리팹을 먼저 만들고, 사람이 같은 경로에 실제 아트를 덮어쓰면 코드 수정 없이 교체.
- **세션 인수인계 문서(`진행상황.md`)**: 다음 세션이 알아야 할 것만 — Unity CLI에서 겪은 함정(플레이 모드 중 `run_tests` 금지 등)을 포함.

## 4. 기능별 기록

### 4.1 기반 세팅 + GameKit (2026-09-13)

- 기술: asmdef 계층, `.editorconfig`, JSON 테이블 규칙, 세로 고정, 불필요 패키지 제거. 공통 기반을 별도 UPM 패키지(GameKit v0.1)로 분리해 `file:` 참조.
- AI: 초기 세팅 절차를 Claude 스킬(`unity-project-setup`)로 만들어 다음 프로젝트에서 재사용. 규칙 문서 3종을 에이전트가 초안 → 사람이 다듬음.

### 4.2 기능 01 — 부트스트랩과 상단 바 (2026-09-14)

- 기술: 테이블 로드 → 검증 → 상태 생성 → Presenter 조립의 컴포지션 루트. `TableValidator`가 데이터 규칙 7장을 코드로. EditMode 테스트 47개.
- AI: 한글 TMP 폰트가 없어 `초당`이 □로 나오는 것을 캡처로 발견하고 SDF 폰트 에셋까지 스스로 생성. 의존 방향(`UI → Game`)을 구현 중 `Game → UI`로 뒤집고 규칙 문서에 이유를 남김.

### 4.3 기능 02·03 — 가차와 월드 슬라이스 (2026-09-14)

- 기술:
  - 가차: 등급 가중치 × 종 가중치를 누적 표 하나로 만들어 난수 1회(`GachaTable`), 비용 `100 × 1.12^n` 반올림, `IRandom` 경계로 결정적 테스트(`SequenceRandom`). 테스트 케이스 78개.
  - 월드: 처음엔 uGUI 3×3 격자 → 기획 전환으로 **2D 월드 맵**. 동물은 우리 안을 랜덤 목적지로 걷는 개체, 중복 뽑기 = 마리 수 증가(`Level` = 마리 수, 수식 유지).
  - 시점: 쿼터뷰(방향별 스프라이트 부담) 기각 → "우리 단위 원근"(사다리꼴·축소) 구현 → **세로로 쌓으면 소실점이 모순**되는 것을 사용자 스크린샷으로 확인 → **원근 카메라 + 빌보드 카드**로 전환. 깊이 정렬·축소 코드가 사라지고 World가 더 단순해짐.
- AI:
  - 프리팹 4개·씬 오브젝트·더미 스프라이트 15개를 전부 `run_script`(에디터 C#)로 생성. 플레이 모드 없이 `capture_game_view --source camera`로 카메라 프레이밍을 세 번 반복 조정.
  - 기획 방향이 세션 중간에 바뀌었을 때(격자 → 월드), 살릴 코드(Core·버튼·카드)와 버릴 코드(격자 View·대기실)를 분리해 기획서 v0.9·로드맵을 다시 씀.
  - 문제 특정: 스크린샷 한 장에서 "우리마다 소실점이 따로라 인접 원근이 모순"이라는 구조적 원인을 짚고 선택지 3개(원근 카메라 / 원근 포기 / 소실점 공유)와 비용을 제시.

### 4.4 기능 04 — 수입 틱과 GameManager (2026-09-14)

- 기술:
  - `IncomeService.Tick(dt)` 5줄: 초당 수입 × 경과 초 적립 후 레벨 판정. `GameManager.Update`가 `game_config.income.tickSeconds`(1초)마다 호출. 표시 보간 없음.
  - 버튼 남은 시간 = ⌈(비용 − 코인) ÷ 초당 수입⌉, 수식은 `GachaService`에, 문구는 `strings.json`. 갱신은 기존 `CoinsChanged` 구독으로 끝(틱마다 발생).
  - 구조 변경: 컴포지션 루트 `GameBootstrap` → **`GameManager`(MonoSingleton) + 씬 스크립트 `MainScene`**. 씬이 늘어도 상태는 한 곳. `.meta`만 이름을 바꿔 씬 참조를 살렸다.
- AI:
  - 구현 직전 전 스크립트를 과잉 설계 관점으로 리뷰해 12건 -105줄 정리(미사용 API·수동 코루틴 핸들·설정 아닌 설정값 `costBasis` 삭제) 후 기능을 얹음.
  - 설계 단계에서 "매 프레임 틱, 보간 없음"을 제안 → 사용자가 "1초 틱"으로 확정. 틱 간격은 기획 숫자로 보고 JSON(version 4)에 둠.
  - 검증 중 에디터 창이 뒤에 있으면 프레임이 멈추는 것을 `Time.frameCount` 조회로 잡아 `editor_focus`로 해결, 인수인계 문서에 기록.

### 4.5 기능 05 — 동물 데이터와 상태, 웜뱃 (2026-09-14)

- 기술:
  - 동물의 스탯·연출값 7개(시트 경로·프레임 속도·크기·이동 속도·대기 시간)를 `animals.json` version 2로. 프리팹·코드에 종별 숫자 0개.
  - `AnimalActor`는 `AnimalState { Idle, Move }` + `switch`의 명시적 FSM. `SpriteAnimator`(20줄)가 프레임 배열을 순환. Unity Animator 에셋 없이 JSON만으로 새 종이 붙는다.
  - 등급 확률을 "동물이 있는 등급만 정규화"로 바꿔 종이 1개일 때도 표시·수식이 거짓이 아니다.
- AI:
  - 실제 아트 3장(정지·idle 시트·move 시트)을 받아 Unity 자동 슬라이스가 맞닿은 프레임을 합쳐 놓은 것을 meta에서 발견, `ISpriteEditorDataProvider`로 6×1 격자 슬라이스·피벗·PPU를 CLI 스크립트로 설정. 다운스케일된 크기로 자른 첫 시도의 오차(341 vs 362)를 원본 크기 조회로 바로잡음.
  - 더미 8종 삭제로 깨지는 테스트 6개 파일을 웜뱃 숫자로 다시 씀. 프레임 경계 비침은 아트 문제로 분리해 기록.

### 4.6 기능 06 — 관광객 (2026-09-14)

- 기술:
  - 관광객 수를 홍보 단계가 아니라 **초당 수입의 로그**(`min(12, 1 + ⌊log₂(1 + 수입)⌋)`)로 계산하는 순수 함수 `VisitorCalculator`(Core, 테스트 6케이스). 어떤 성장 축이든 수입을 올리면 길에 사람이 늘어 세 축 공통의 시각 피드백이 된다.
  - 길은 우리마다 앞에 깔린 **길 타일의 경계**(`CageView.PathBounds`)가 곧 걷는 영역. 웨이포인트·길찾기 없이 X 방향 직진 + Enter·View·Leave 3상태 FSM. 우리가 늘어도 데이터가 늘지 않는다.
  - 관광객 종류는 `visitors.json`(가중치 선택), 연출 숫자는 프리팹 인스펙터, 게임 규칙 숫자는 `game_config.visitors`. 세 종류의 숫자가 각각 제자리에 있다.
- AI:
  - 인터넷 레퍼런스 14건(Idle Miner Tycoon GDC, 고양이와 스프, Merge Town, AdVenture Capitalist 수학 등)을 조사해 "환생 없음 + 성장 축 3개 + 지점 유지형 2호점"을 근거와 함께 제안, 사용자가 확정(기획서 v0.13).
  - 설계 06의 관광객 수 제안(기본 3 + 단계)이 사용자 답변("수는 획득 가능한 코인에 따라")과 달라 로그 수식으로 다시 설계하고 구현. 더미 실루엣·길·코인 PNG 4장, 프리팹 2개, 씬 연결을 CLI 에디터 스크립트 2개로 생성. Unity 가짜 null(`?? AddComponent`) 실패를 `TryGetComponent`로 수정.
  - 플레이 검증은 리플렉션으로 FSM 상태를 14회 폴링해 View 상태와 0.7초짜리 코인 팝업을 실제로 잡았다(캡처만으로는 놓친다).
- 문제와 해결: 사용자가 "난간이 관광객을 가린다"고 지적. 플레이를 일시정지해 관광객 좌표를 읽고 그 지점을 4배 확대 크롭해 비교하는 방법으로 원인을 특정했다. 카메라 거리 정렬은 9.6유닛짜리 난간의 정렬점이 가운데 하나뿐이라 옆으로 간 관광객을 뒤로 보낸다. 정렬점 Pivot만으로는 가운데만 해결돼, URP 2D 렌더러를 Custom Axis(Z) 정렬로 바꿔 해결. 렌더러 에셋을 텍스트로 고친 뒤 강제 재임포트가 필요하다는 것도 이때 알았다.

### 4.7 구조 정리 06-1 — 리뷰 8건 (2026-09-14)

- 기술:
  - 월드 개체를 `Unit`(공통 몸체) → `Animal`·`Visitor`로 재구성하고, `switch` FSM을 **상태 객체**(`UnitState` → `WalkState`·`WaitState`)로 바꿨다. 두 개체가 "걷기"와 "기다리기" 두 상태를 공유하므로 상태 클래스는 2개뿐이고, 개체는 전이 순서만 적는다. 상태 인스턴스는 초기화 때 한 번 만들어 재사용.
  - 초당 수입의 소유자를 `IncomeService` 하나로(`IncomePerSecond` + `IncomeChanged`). 세 곳이 각자 재계산하던 것을 없애 다음 기능(홍보)의 구독 지점이 한 곳이 됐다.
  - "레벨"이라 부르던 마리 수를 코드·JSON·문구 키까지 `Count`로 통일해 다음 성장 축(별)과의 이름 충돌을 미리 제거. 데이터 변경 절차(기획서 → JSON version 6 → 검증 테스트)를 그대로 밟았다.
  - `FrameCache`(경로당 1회 로드, 번호 접미사 정렬)로 관광객이 1.5초마다 `Resources.LoadAll`을 부르던 비용과 11프레임 이상 시트의 정렬 버그를 함께 제거.
- AI:
  - 코드 3,000줄 전체를 읽고 규칙 문서와 대조해 8건을 순위와 근거로 보고 → 사용자가 "전부 + Unit 상속·State 패턴" 지시 → 코드 20파일·테스트 4파일·JSON 2개·기획 문서 4개를 한 번에 수정. 스크립트·프리팹 이름 변경은 `.meta`를 같이 옮겨 GUID를 유지(씬·프리팹 참조 무손실).
  - 검증은 플레이 모드에서 버튼 3회 호출 후 상태 조회(마리 수·스프라이트 프레임·관광객 수·결과 카드 활성)로 확인. 캡처만으로는 프레임 전환을 못 본다.
- 문제와 해결: 팝업 View를 Presenter에 주입하려면 GameKit `UIManager`에 "열지 않고 얻기"가 없었다. `GetView<T>`를 public으로 여는 한 줄 변경(패키지 저장소).

### 4.8 기능 07 — 우리 꾸미기, 시설 (2026-09-15)

- 기획 전환: 설계 시작 시점에 "우리 해금·맵 확장"이던 07을 사용자가 **"우리는 지점당 하나, 그 우리를 꾸미는 구조"**로 바꿨다. 에이전트가 객관식 5문(꾸미기의 본체·배치 방식·수입 효과·해금 조건·첫 범위)으로 결정을 받아 기획서 v0.16·데이터-테이블-규칙 v1.10·설계 07을 같은 세션에 반영하고 구현했다.
- 기술:
  - 시설 = 테이블 행(`facilities.json`: 자리·해금 레벨·최대 단계·단계당 배수·비용 곡선). 홍보는 첫 행일 뿐이라 `game_config.promotion`과 `ZooState.PromotionStage`를 지우고 **시설별 단계 사전 + `FacilityService`** 하나로 통일. 총 배수는 시설 배수의 곱.
  - `zoo_levels`에서 `cageCount`·`unlocksPromotion`을 빼 레벨은 "누적 코인 → 레벨" 계산만 남기고, 해금 여부는 시설 행이 자기 `unlockLevel`로 가진다(레벨 테이블이 다른 테이블을 알 필요가 없다).
  - UI는 버튼 View + 팝업 View + 행 프리팹, Presenter 하나. 행 → 레코드 매핑은 클로저 대신 사전. 월드 `Facility`는 `Unit`을 상속하지 않는 정지 카드(빌보드 + 그림자).
  - 씬을 우리 하나로 줄이며 `WorldView`의 우리 배열·`CagesWithAnimals`·우리 인덱스 배정을 삭제(−40줄).
- AI: 설계 문서·기획서 3종 갱신 → 코드 25파일·JSON 4개·테스트 8파일·에디터 스크립트(더미 스프라이트 4장 그리기, 프리팹 4개 조립, 씬 정리)를 한 번에 작성 → 플레이 모드에서 버튼·행 클릭을 스크립트로 호출하고 상태 조회 + 캡처로 확인.
- 문제와 해결: 캡처에서 **산 시설이 화면에 없었다.** 자리를 우리 바깥(x ±6.2)에 잡았는데 세로 화면은 길 깊이에서 ±5유닛만 보인다 → 우리 안 네 모서리로 옮기고 기획서·규칙 문서를 함께 고쳤다. 고친 JSON이 플레이에서 옛 값으로 읽혀(에디터 비포커스 상태에서 자동 임포트 안 됨) `AssetDatabase.ImportAsset(ForceUpdate)`를 eval로 돌려 해결.

### 4.9 기능 07-1 — 시점 전환, 원근에서 직교 쿼터뷰로 (2026-09-16)

- **기술 포인트**: 월드를 3D XZ 평면 위의 스프라이트 카드로 만들어 둔 덕에 시점이 카메라 설정(직교, 35°, Y 45°) + 정렬 축 (1,0,1) + 좌우 반전 판정(카메라 오른쪽 벡터 내적)만으로 바뀌었다. 아트 0장 재작업. 난간은 얇은 세운 카드라 카메라를 돌리자 그대로 쿼터뷰 울타리로 읽혔다.
- **AI가 한 일**: 결정 전에 같은 씬을 플레이 모드에서 카메라만 바꿔 원근·직교 정면·직교 쿼터뷰 세 장을 찍어 비교 페이지(아티팩트)로 만들고, 픽셀아트 궁합·존 3개 배치·시설 존재감·AI 아트 생성 난이도·코드 변경량을 표로 정리했다. 사용자가 세 장을 보고 결정 → 반나절 아닌 30분에 전환·검증.
- **문제와 해결**: 전날 쿼터뷰를 "방향별 스프라이트 부담"으로 기각했으나, 레퍼런스(고양이 오피스)의 캐릭터도 정면 카드라 그 걱정이 근거 없음을 확인하고 철회. 원근은 픽셀아트 도입으로 거리마다 픽셀 크기가 달라져 폐기.

### 4.10 기능 07-2 — 순수 2D 쿼터뷰로 전환 (2026-09-16)

- **기술 포인트**: 07-1의 "3D 카메라 + 빌보드"를 같은 날 버리고 장르 표준인 순수 2D로. 논리 좌표(XZ)는 그대로 두고 `Iso.ToScreen` 한 함수가 화면 XY(2:1)를 만들며, 깊이는 화면 y 정렬. 바닥·길은 평행사변형 스프라이트, 울타리는 칸 단위 스프라이트를 네 변에 늘어놓는 방식(긴 한 장은 y 정렬이 깨진다). World 계층 10개 파일 수정, Core·테스트 변경 0, 98/98 통과.
- **AI가 한 일**: "왜 그냥 2D에 안 뿌렸나"라는 질문에 두 방식의 등가성과 차이(변환 코드 vs 방향별 아트, 픽셀 리샘플링, 장르 관행)를 표로 답하고, 레퍼런스 게임들이 순수 2D임을 근거로 추천을 바꿨다. 더미 바닥·길·울타리 PNG를 외부 라이브러리 없이 순수 python(zlib)으로 생성하고, 실제 아트용 프롬프트(눕는 텍스처는 top-down seamless → 에이전트가 평행사변형 변환, 서 있는 물체만 쿼터뷰 각도)를 `아트-프롬프트.md`에 적었다.
- **문제와 해결**: 07-1 검증 중 Unity CLI 파이프라인이 `run_tests` 뒤 멈춰 프로세스를 강제 종료·재실행. 재실행 직후 스크립트를 쓰는 중에 컴파일이 돌아 옛 오류가 콘솔에 남았고, `console_status`의 groundTruth(오류 0)로 구분했다.

### 4.11 기능 07-3 — 타일맵과 1호점 맵 (2026-09-16)

- **기술 포인트**: 눕는 바닥(잔디·흙·길)을 Unity Tilemap(Isometric Grid, 셀 1×0.5)으로 옮기고 타일 64×32 px = 논리 1유닛이 되도록 `Iso` 상수만 0.5/0.25로 바꿨다. Unity의 `CellToWorld`와 `Iso.ToScreen`이 같은 식이라 셀 (x, z) = 논리 (x, z)로 두 좌표계가 하나가 됐다(`GetCellCenterWorld(0,0) == Iso.ToScreen(0.5,0.5)`로 검증). 존은 6×8 타일 프리팹(`ZoneView`), 1호점은 존 10개 + 길 2칸을 타일맵에 미리 칠해 둔 프리팹(`BranchView`)이고 씬에는 인스턴스 하나. 서 있는 것(울타리·시설·동물)은 스프라이트 + 화면 y 정렬을 유지해 타일맵 청크와 정렬 충돌을 피했다. 코드 변경은 World 7파일, Core·테스트 0.
- **AI가 한 일**: 타일맵 채택 여부와 1호점 배치를 아티팩트 두 장(현행 vs 안 B·C 비교, 위에서 본 배치 + 쿼터뷰 렌더링 SVG)으로 그려 논의를 이끌었고, "왜 Unity Tilemap을 안 쓰나"는 오해에 바닥층은 쓰고 서 있는 것만 안 쓰는 이유(청크 정렬·런타임 생성)를 답했다. 타일 더미·울타리 더미는 Pillow로 생성(첫 시도의 폴리곤 가장자리 틈을 행 단위 스팬으로 고쳐 이음새 제거), 에디터 스크립트 한 번으로 임포트 설정·Tile 에셋·프리팹 2개·타일 칠하기·씬 교체를 끝냈다. 캡처에서 발견한 세 가지(타일 틈, 개체가 타일 대비 큼, 울타리 뒤 모서리 X자 교차)를 그 자리에서 고쳤다(정수 좌표 기둥 + 뒤 모서리 기둥 스프라이트).
- **문제와 해결**: PATH의 python이 MSYS2용이라 pip이 없고 pacman은 gcc 의존 충돌. winget으로 Windows Python 3.12를 따로 설치해 Pillow를 쓴다. 아트 생성은 사용자의 ChatGPT 구독으로 로그인된 Codex CLI의 내장 이미지 생성을 명령줄에서 호출할 수 있음을 확인(자물쇠 1장 테스트)해, 이후 리소스는 에이전트가 직접 뽑는다.

### 4.12 기능 08 — 빵집 코어 루프, 손님이 걸어서 사 가는 가게 (2026-09-18)

- **기술 포인트**: 굴(존)을 누르면 가게 화면으로 넘어가고, 손님 동물이 입구 → 좌우 진열대(원하는 빵) → 가운데 줄 → 계산대 웜뱃 → 퇴장한다. 재고가 없으면 참다가 화내며 나간다. 규칙은 Core `ShopSim` 하나가 전부 갖고 **걷는 시간까지 Core가 센다**(진열대까지 = 1.5 + 층 × 0.8초). World는 이벤트를 받아 "경로 길이 ÷ 받은 시간"으로 속도를 맞춰 걷기만 하므로, 화면과 규칙이 어긋나지 않고 오프라인 계산(11)도 같은 시뮬을 화면 없이 돌릴 수 있다. 가게는 **구역 프리팹을 위에서 아래로 쌓은 것**(입구 · 진열 층 × n · 계산대 · 오븐 줄 × m)이라, 빵을 해금해 층이 늘면 한 층을 끼우고 아래를 내린다(굴 확장 연출 + 카메라 이동). 빵 종류 추가 = `breads.json` 한 행. 카메라는 가게에 들어가면 경계만 가게 사각형으로 바꿔 기존 드래그 코드 그대로 세로 스크롤이 된다. 다 구운 빵은 오븐에서 기다리다 진열대에 자리가 나면 옮겨진다.
- **AI가 한 일**: 첫 설계(v0.2)가 줄 게이지·러시·쿵 버튼으로 돌아가는 "HUD 게임"이라는 사용자 지적을 받고, 객관식 네 문항(손님 흐름·플레이어 역할·원하는 빵·업그레이드)으로 의도를 좁혀 v0.3 → 배치 지시 반영 v0.4 → 결정 3개 확정 v0.5로 설계 아티팩트를 갱신했다. 구현은 한 번에: Core 4파일 + 테스트 12개, World 10파일, UI 팝업 공용 베이스(`ListPopupView`) + 굽기·업그레이드 팝업, 더미 스프라이트 14장(Pillow), 구역 4종·가게·손님·HUD·팝업 프리팹을 굽는 `ShopBaker` 메뉴. 플레이 모드에서 도착·줄·계산·화난 퇴장·굴 확장·스크롤 경계·오븐 탭 → 팝업 → 굽기를 상태 조회와 캡처로 확인했다.
- **문제와 해결**: 캡처에서 바닥 점·구역 경계로 초록 배경이 비쳐 보여(반투명 픽셀) 불투명 어두운 색으로 다시 그렸다. 문자열 리터럴을 상수로 바꾸는 일괄 치환이 상수 선언 자체까지 바꿔 순환 정의 컴파일 오류가 났고 선언만 되돌렸다. 도착 4초·계산 1.5초에서는 줄이 1명 넘게 쌓이지 않아, 검증할 때만 실행 중 값을 바꿔 줄 모양을 확인했다(데이터는 그대로).

### 4.13 아트 — 첫 손님 토끼, 시안부터 인게임까지 (2026-09-21)

- **기술 포인트**: 손님 외형이 이제 앞·뒷모습 × 숨쉬기·걷기 4시트다. `Unit.Face`가 화면에서 위로 걸으면 뒷모습, 아래로 걸으면 앞모습을 고르고, 옆으로만 걸을 때는 직전 방향을 유지한다. 대기와 걷기 프레임 속도를 나눠 웜뱃과 같은 2.5·8fps로 맞췄다(`visitors` version 2). 말풍선은 외형 키(스프라이트 높이)에 맞춰 머리 위에 뜬다. 숨쉬기·걷기 프레임은 AI 없이 칸 편집 스크립트가 만들고, 웜뱃 스크립트에 토끼 설정 줄만 더해 공용으로 쓴다(다시 돌려도 웜뱃 결과는 그대로).
- **AI가 한 일**: 후보 4종을 실루엣 기준으로 제안하고, Codex CLI로 시안 3장을 뽑아 격자를 강제한 뒤 실제 가게 캡처 위에 합성한 비교 아티팩트 한 장으로 논의했다. 버전 6까지 같은 URL에서 갱신하며 선택을 좁혔다(앞모습 3장 → "A에 B의 손" 칸 편집 → 손가락 선 제거 → 뒷모습 3장 → 숨쉬기 → 걷기). 숨쉬기·걷기는 페이지 안에서 게임 속도로 재생해 보여 줬다. 적용은 한 번에: 시트 묶기 스크립트, 슬라이스 메뉴(`ZooTycoon/Bake/Import Visitor Sheets`), 레코드·검증·테스트, 방향 전환, 말풍선 높이. 플레이 모드에서 진열대 앞 앞모습, 줄 서기, 계산 뒤 뒷모습 퇴장, 화난 퇴장을 캡처로 확인했다.
- **문제와 해결**: 웜뱃 걷기처럼 귀 전체를 옆으로 밀면 긴 귀가 머리와 만나는 곳에서 외곽선이 끊겨, 귀 윗부분만 미는 `tip` 값을 추가했다. 격자 자동 측정이 시안 C에서 반 주기를 잡아 얼굴이 뭉개졌고, 칸 수를 직접 줘서 다시 맞췄다. 크림색 털이 가게 바닥과 비슷해 묻힌다는 점은 비교 페이지에 적었고, 사용자는 그 점을 알고 A를 골랐다.

### 4.14 아트 — 손님 3종 추가(펭귄·여우·고슴도치), 파이프라인 재사용 (2026-09-23)

- **기술 포인트**: 토끼 때 만든 파이프라인(격자 강제 → 칸 편집으로 숨쉬기·걷기 → 시트 → 슬라이스 메뉴 → `visitors` 행)에 스크립트 설정 줄과 JSON 행만 더해 새 손님을 넣었다. 세 종을 넣는 동안 코드 변경은 시트 칸 폭 상수 하나(60 → 88, 여우 걷기 프레임이 86px)와 테이블 버전 테스트뿐이다. 귀가 없는 종(펭귄·고슴도치)은 숨쉬기 스크립트의 귀 목록을 비우면 그대로 돌고, 뾰족귀(여우)는 토끼처럼 귀 끝만 갸웃한다.
- **AI가 한 일**: 앞모습을 참조로 뒷모습 3장을 뽑아 실루엣을 앞모습과 같게 맞추고, 앞모습 팔레트로 색을 통일했다. 격자 자동 측정이 한 장에서 48줄을 잡아 36줄로 강제했고, 팔레트 문턱에 하이라이트가 잘려 주황으로 바뀐 것을 문턱을 낮춰 살렸다. 여우·고슴도치는 사용자가 앞모습(여우 A·고슴도치 B)만 고르고, 뒷모습 3장씩은 그때까지의 선택 기준(앞모습과 같은 실루엣·같은 자리 하이라이트·앞뒤가 이어지는 특징·7색 이하)으로 에이전트가 골라 인게임까지 한 번에 적용했다. 뒷모습 실루엣이 앞과 칸 단위로 같아 숨쉬기·걷기 설정을 앞뒤가 공유한다.
- **검증**: EditMode 56/56, 콘솔 0, 플레이 캡처로 네 종의 진열대 앞 앞모습·계산 뒤 뒷모습 퇴장·화난 퇴장 확인.

### 4.15 아트 — 빵집 소품·배경 교체, 둥근 굴 방 (2026-09-23)

- **기술 포인트**: 소품 8항목(바닥·입구·진열대·오븐·계산대·빵 3종)을 한 칸 2px·PPU 80으로 통일하고 프리팹 좌표는 그대로 둔 채 PPU만 64 → 80으로 바꿨다. 구역 배경은 그림 파일이 아니라 스크립트(`make_shop_sections.py`)가 만든다: 벽 안쪽 선이 구역 가운데서 부풀고 위·아래 끝에서 같은 x로 돌아오므로 층을 어떤 순서로 쌓아도(굴 넓히기) 방이 이어진다. 오븐은 `shop_upgrades.lookLevel`(데이터)로 굽기 속도 5단계부터 벽돌 오븐으로 바뀐다 — 코드는 `OvenView.SetLook` 한 줄과 검증 한 줄.
- **AI가 한 일**: 항목마다 3장씩 24장을 한 번에 뽑아 실제 크기 목업 세트 A·B·C와 항목별 확대를 한 페이지에 놓고 추천 조합을 적었다. 사용자는 항목별로 골랐고 "입구는 구멍만 크게"는 재생성 대신 구멍 덩어리를 골라 1.7배로 키우는 칸 편집으로, "계산대 좌우로 길게"는 가운데 열 복제로 처리했다. 첫 인게임 캡처에 사용자가 "배경이 너무 사각형"이라 하자 직선 벽·수평 경계선을 버리고 굴 둔덕 결(외곽선 + 밝은 테두리)의 둥근 방으로 다시 만들었다.
- **검증**: EditMode 57/57(lookLevel 검증 테스트 추가), 콘솔 0, 플레이 캡처로 굽기·손님·굽기 속도 5단계 오븐 전환 확인.

### 4.16 연출 1차 — 돈이 들어오는 순간과 잃는 순간이 보이게 (2026-09-23)

- **기술 포인트**: 연출은 전부 View 쪽 코드(MonoBehaviour Update·코루틴)이고 Core는 `patienceWarnSeconds` 필드와 검증 한 줄만 늘었다. 상단 코인은 Presenter가 목표값과 포맷 함수를 넘기고 View가 0.25초 보간한다(MVP 유지). 손님 말풍선 흔들림은 Core `Customer.Timer`를 읽기만 하고, 안 되는 오븐 탭의 이유 판정은 씬 스크립트(`MainScene`)가 Core 상태로 하고 World는 문구만 받는다. 연출 시간은 코드 상수, 기획 숫자(경고 시간·문구)는 JSON.
- **AI가 한 일**: 지난 점검 보고서의 1순위 5개를 설계 한 장으로 묶어 한 번에 구현했다. 코드 상수·JSON 키·프리팹 변경(코인 팝업 글자, 오븐 힌트 텍스트, 줄 간격)을 베이커에 넣어 재굽기 한 번으로 반영했고, 플레이 모드에서 결제 순간을 잡기 위해 코인 팝업 개수를 폴링해 그 프레임을 캡처했다.
- **검증**: EditMode 58/58(patienceWarn 검증 테스트 추가), 콘솔 0, 플레이 캡처.

### 4.17 UI — 사물 터치 시트와 픽셀 UI 한 벌 (2026-09-23)

- **기술 포인트**: 업그레이드 버튼과 팝업 2개를 지우고, 가게 안 사물(진열대·잠긴 진열대·오븐·잠긴 오븐·계산대)을 탭하면 같은 모양의 하단 시트가 뜬다(칩 줄 = 선택, 행 = 구매). 어느 사물에서 무엇을 파는지는 `shop_upgrades.target`이 정하고, 효과 전후 문구는 `effectFormat` + `ShopSim.UpgradeValue`로 만든다(Core 규칙 추가 없음). 월드는 `ShopView.HitTest`가 잠긴 사물까지 맞혀 `ShopTarget`을 올리고, UI는 World를 참조하지 않으므로 씬 스크립트가 종류를 옮긴다. UI 규칙 v1.0: 1 UI px = 화면 4px(월드 한 칸과 같음), 12색 팔레트, 9-slice 부품, 갈무리 픽셀 폰트(OFL) TMP 비트맵 동적 에셋. 프리팹은 `UIBaker`가 규칙 숫자로 조립한다.
- **AI가 한 일**: 기획(원칙·사물별 상호작용·시스템·결정 6개)과 "선택 + 구매가 한 사물에 있을 때" 보강(후보 5개)을 아티팩트로 만들어 사용자가 고르게 했고, UI 규칙 문서를 v1.0으로 확정했다. 부품은 스크립트 도형 대신 Codex 시안(시트 3·버튼 3·아이콘 3)에서 사용자가 결(시트 B·버튼 A·아이콘 A)을 고르면 그 결로 부품별을 1장씩 뽑아 격자를 강제하고 `make_ui.py`가 팔레트 스냅·9-slice 경계·크기 보정을 해 PNG와 슬라이스 표를 만들었다. 갈무리 폰트는 GitHub 릴리스에서 받아 TMP 래스터 에셋을 에디터 스크립트로 생성했다.
- **문제와 해결**: 첫 캡처에서 패널이 화면보다 100px 넓어 왼쪽이 잘렸다(RectTransform 기본 sizeDelta) → 0으로. 행·칩 글자가 겹쳐 정렬을 위/아래 기준으로 바꾸고 칩을 56×52로 키우며 문구를 줄였다. 월드 태그 "300"이 "30"으로 잘려 폭·줄바꿈을 고쳤다.
- **검증**: EditMode 60/60(target 검증·UpgradeValue 테스트 추가), 콘솔 0, 플레이에서 사물 6종 시트 캡처와 굴 넓히기(잠긴 진열대 탭 → 구매 → 층 추가) 확인.

### 4.18 굴 격자 — 흙 화면과 좌우로 불어나는 굴 (2026-09-23)

- **기술 포인트**: 가게를 「구역을 세로로 쌓은 기둥」에서 「보이지 않는 칸(반 폭 3.375 × 2.4유닛)의 집합」으로 바꿨다. 파낸 칸의 합집합에서 굴 모양을 계산해(`BurrowShape`, 순수 C#: 격자 꼭짓점마다 둘레 네 칸을 보고 바깥 모서리는 반지름 12칸으로 깎고 ㄱ 자 안쪽은 메움, 입구는 4분의 1 타원 깔때기) 실행 중 텍스처 한 장으로 칠한다(`BurrowPainter`, 한 칸 = 1px·PPU 40, 바닥·벽 타일을 굴 원점 기준 칸 좌표로 샘플해 무한 흙 배경과 이음새 없이 이어짐). 방·통로·배경 변형 그림이 없어져 어떤 모양으로 파도 한 덩어리 굴이 된다. 규칙은 `BurrowGrid`(칸·파기 규칙·비용 150 × 1.4ⁿ·BFS 경로·거리)에 있고, 진열대·오븐은 칸의 자리에 놓이며 걷기는 경로 길이 ÷ `walkSpeed`. 카메라는 가게에서 고무줄(넘긴 몫 × 0.35, 놓으면 0.25초 복귀)이고, 드래그가 시작되면 드러나는 방향의 팔 수 있는 칸에 비용 태그가 2초 뜬다(D4, 사용자 안).
- **AI가 한 일**: 기획서 아티팩트를 v0.1 → v0.5로 다섯 번 고쳤다(잔디 삭제 → 입구를 화면 맨 위로 → 방·통로 삭제와 실행 중 한 장 그리기 → 결정 5개 확정). 「방으로 인식해 통로를 만들지 말고 자연스럽게」라는 지적에 픽셀 모폴로지(닫기·열기)를 격자 꼭짓점 규칙으로 바꿔 O(꼭짓점 × r²)로 줄였다. 구현은 Core 3 파일 신설 + ShopSim·ShopView·스포너·카메라·시트·Baker 개정, 방 배경 4장과 파이썬 삭제, 타일 3장 생성.
- **문제와 해결**: 마스크 꼭짓점 루프가 맨 아래 줄을 빠뜨려 아래 모서리가 안 깎였다(테스트가 잡음) → 줄 상한 +1. 플레이 중 EditMode 테스트를 불러 에디터가 멈춤 → 재시작(함정 목록 규칙 재확인).
- **검증**: EditMode 70/70(BurrowGrid 5·BurrowShape 4 신설, ShopSim 4 개정), 콘솔 0, 플레이 캡처: 진입 직후 화면 전부 흙·입구가 맨 위, (−2,1) 파기 → 둥글게 불어남·ㄱ 자 메움·카메라 이동, 진열대·오븐 놓기, 웜뱃이 옆 칸 오븐까지 심부름, 오른쪽 드래그 태그 3개 표시 후 2초 뒤 사라짐.

### 4.19 손님 동선 — 4방향 A*, 행동 트리, 위치를 가진 규칙 (2026-09-23)

- **기술 포인트**: 걷기를 규칙(Core)으로 옮겼다. `ShopLayout`이 굴 마스크(굴 그림과 같은 것) 위 0.2유닛 격자에서 사물 바닥 자리를 0.3 넓혀 막은 걷는 땅을 만들고, `BurrowNav`가 4방향 A*(꺾을 때마다 벌점 0.6, 이진 힙, 탐색 배열 재사용)로 길을 찾는다. 막힌 곳(계산대 뒤 웜뱃)에서는 가장 가까운 걷는 점으로 곧장 나온다. 손님은 `Mover`가 A* 꺾임점을 walkSpeed로 따라가고, 화면(`ShopCustomer`·`ShopWombat`)은 매 프레임 Core 위치·보는 방향·상태를 읽어 그리기만 한다. 그래서 가게 시뮬을 1초 틱에서 매 프레임으로 바꿨다(행동 트리 단계마다 최대 1초 멈칫 방지). 손님은 순서·선택·반복 마디 셋과 잎 행동으로 된 행동 트리(`Bt*`, `ShopSim.Customers.cs`)로 움직인다: 구멍에서 톡 → 안 가 본 빵 중 가중치 → A*로 진열대 옆자리(예약) → 있으면 집기, 없으면 최대 2초 두리번(그사이 채워지면 바로 집기) → 다른 빵 → 인내(두리번 합계 6초)가 다 하면 「!!」. 줄은 등록기 앞에서 계산대를 감싸는 자리(가로 5 + 왼쪽 아래 3)를 걷는 땅 판정으로 만들고, 계산은 머리가 머리 자리에 선 뒤에만 시작한다.
- **AI가 한 일**: 플레이 모드에서 0.1초마다 손님 발 위치를 기록해 문제를 수치로 먼저 보였다(속도 0.6~3.0, 오븐 10명·계산대/웜뱃 9명·진열대 9명 뚫기, 줄이 입구까지, 품절 대기 겹침 211). 설계 아티팩트 v0.1(결정 6개) → 사용자 답(가로·세로 A*, 행동 트리, 옆모습 아트) → v0.2. 「도착해서 재고를 보고 판단」하려면 규칙이 도착 시각을 알아야 한다는 점에서 길 찾기를 Core로 옮기는 구조를 정했다. 옆모습은 앞모습 사본 더미로 먼저 넣고, Codex로 토끼 옆모습 시안 3장을 뽑아 선택을 받는다.
- **문제와 해결**: 웜뱃 바닥 자리와 계산대 사이 0.3 틈으로 손님 길이 났다 → 웜뱃 자리를 계산대 밑변까지. 줄을 위로 꺾으면 진열대 옆자리와 부딪혀 계산대 왼쪽 아래로 꺾게 했다. 오븐 사건이 매 프레임 나가 시트가 매 프레임 다시 그려질 뻔해, 오븐 사건은 남은 초가 바뀔 때만 내고 진행 막대는 화면이 매 프레임 읽는다.
- **검증**: EditMode 86/86(BurrowNav 6·행동 트리 5·배치 4 신설, 손님 규칙 테스트 전면 재작성). 플레이 관찰 세 가지(보통 30초·붐빔 40초·빵 바꾸기 30초) 모두 걷는 속도 중앙값 2.20, 사물 바닥 자리 안 발 0, 줄 손님이 통로·입구에 0, 같은 자리에 선 두 손님 0, 웜뱃이 계산대 안 0. 빵 바꾸기에서 두리번 9명 → 다른 빵 6명, 화난 손님 0. 콘솔 오류 0.

### 4.20 손님·웜뱃 옆모습 아트 — 턴어라운드 시트 빈칸 채우기 (2026-09-23)

- **기술 포인트**: 4방향 이동에 필요한 옆모습 5종(토끼·펭귄·여우·고슴도치·웜뱃)을 코드 변경 없이 더미 경로에 덮어썼다. 앞·뒷모습 격자를 같은 칸 크기·같은 바닥선에 나란히 놓고 가운데를 비운 시트를 이미지 모델에 주어, 빈칸을 채우게 해서 카메라·크기를 맞췄다. 숨쉬기·걷기 4프레임은 AI 없이 기존 칸 편집 스크립트에 종마다 설정 한 줄로 만들고, 좌우 뒤집기 때 제자리에 서도록 발 가운데를 피벗에 맞춰 투명 여백을 붙였다.
- **AI가 한 일**: 토끼 시안을 3회차까지 뽑았다. 1차는 네 발 자세, 2차는 눈높이 비율(폭 22칸·눈 20줄 대 앞모습 28칸·24줄)로 사용자 지적을 받았고, 원인이 프롬프트 문구("body taller than wide")임을 찾아 규칙 문서와 메모리를 고쳤다. 3차는 방식 셋(턴어라운드·문장 설명·턴테이블)을 비교했고, 고른 턴어라운드 방식으로 나머지 4종 12장을 한 번에 뽑았다. 입선·볼 모양 지적은 다시 뽑지 않고 칸을 직접 고쳤다.
- **검증**: 게임 바닥 합성에서 폭·눈 줄을 앞모습과 칸 단위로 비교. 플레이 중 옆모습 스프라이트를 그리는 개체를 찾으면 에디터를 일시정지하고 캡처(5종, 두리번·걷기·뒤집기), 콘솔 오류 0.

### 4.21 기능 09 — 지상 폐기, 조이스틱으로 움직이는 웜뱃 (2026-09-23)

- **기술 포인트**: 방치형 자동 가게를 손으로 돌리는 가게로 바꿨다. 지상(존 맵·타일맵·숲·동물원 레벨)은 코드·에셋·테이블째 지우고 게임을 곧장 굴에서 시작한다. 웜뱃 이동은 Core `ShopSim`이 조이스틱 방향 × `wombatSpeed`로 매 프레임 걷히고, 걷는 땅이 아니면 X만·Y만 다시 시도해 벽을 따라 미끄러진다. 웜뱃이 걷는 땅은 손님 땅과 같은 A* 격자를 쓰되 계산대 뒤 웜뱃 자리만 열어 둔 두 번째 격자(`WombatNav`)다. 매 프레임 `interactDistance` 안의 가장 가까운 사물(진열대·오븐·계산대·빈 자리·팔 수 있는 흙 칸)이 대상이 되고, 버튼 하나가 대상에 따라 꺼내기·채우기·시트 열기로 바뀐다(행동 아이콘 4종). 오븐 → 진열대 자동 진열과 심부름 걷기를 지우고, 계산은 웜뱃이 계산대 자리에 있을 때만 흐른다. 대상 종류는 World·UI에 따로 있던 열거형 둘을 Core `Interactable` 하나로 합쳤다. 카메라는 웜뱃을 SmoothDamp로 따라가며 굴 경계에 갇힌다.
- **AI가 한 일**: 프로젝트 분석 → 결정 8개를 객관식으로 확정(조작·수동 여부·카메라·시트·지상·계산·나르기·파기) → 설계 아티팩트 v0.1, 버튼 아이콘 지적으로 v0.2. 지상 에셋 95개는 GUID 참조 검색으로 가게가 쓰지 않는 것만 골라 지웠다. 같은 시간 옆모습 아트를 하던 다른 세션과 에디터 사용 순서·파일 경계를 메시지로 맞췄다.
- **문제와 해결**: 플레이 확인 중 조이스틱 손잡이가 눌린 채 남아 웜뱃이 저절로 걸었다 — 누른 상태에서 포커스를 잃으면 떼는 사건이 오지 않는다. 포커스를 잃을 때도 떼도록 고쳤다. 새 사물이 웜뱃 발밑에 놓여 갇히는 경우는 배치가 바뀔 때 가까운 걷는 점으로 옮긴다.
- **v0.4 — 행동을 데이터로**: 사용자가 「빵 놓기는 자동으로, 앞으로도 수동·자동 작업이 섞이니 칼럼으로 나누고 행동과 사물을 테이블로」를 요청. `actions.json`(행동 5개: 꺼내기·채우기·계산·열기·파기, `mode` manual/auto, 버튼 아이콘 경로)과 `interactables.json`(사물 5개: 거리 `range`, 붙는 행동 목록 = 버튼 우선순위)으로 나눴다. 행동이 하는 일만 코드(`ShopSim.k_Action*`)에 두고, 어느 사물에서 어떤 방식으로 하는지는 표가 정한다. 자동 행동은 가장 가까운 사물이 아니어도 range 안이면 매 프레임 한다(오븐 옆에 서 있어도 진열대가 채워진다). 계산도 계산대의 자동 행동 `serve`가 되어, 표에서 manual로 바꾸면 버튼 한 번에 계산된다. 버튼 아이콘은 프리팹 배열 대신 표의 경로로 읽는다. 검증기가 행동 id 누락·manual인데 아이콘 없음·시트 행동을 auto로·없는 행동 참조를 잡는다.
- **검증**: EditMode 83/83(웜뱃 이동·미끄러짐, 대상 고르기, 꺼내기, 자동 채우기·서 있는 동안 다시 채우기·대상이 오븐이어도 채우기, serve manual·take_out auto로 표 바꾸기, 계산대를 비우면 계산 멈춤, 행동·사물 표 검증, 웜뱃 격자. 테스트가 조이스틱 입력으로 웜뱃을 끌고 다닌다). 플레이에서 오븐 시트 → 굽기 → 꺼내기 버튼(머리 위 6개) → 진열대 옆에 가기만 하면 채워짐 → 계산대 뒤에서 판매, 벽 옆 파기 태그·삽 아이콘, 따라가는 카메라를 캡처로 확인. 콘솔 오류 0.

### 4.22 오븐 굽기 타이머 — 늘이던 막대를 미리 그린 프레임으로 (2026-09-23)

- **기술 포인트**: 흰 채움을 가로로 늘이던 막대(테두리가 가려지고 픽셀이 늘어남)를 칸 단위로 그린 원형 타이머 16프레임으로 바꿨다. 진행 0~1을 반올림해 프레임을 고르므로 늘임·셰이더 없이 픽셀 격자가 그대로다.
- **AI가 한 일**: 이미지 생성 대신 스크립트로 시안 3개(빵 배지 캡슐·원형 타이머·구워지는 빵)를 UI 팔레트로 그리고, 오븐 위 합성과 0→100% 재생 페이지로 선택을 받았다. 다른 세션이 같은 두 파일(OvenView·ShopBaker)을 고치는 중이라 메시지로 에디터 사용을 조율했다.
- **검증**: 컴파일 0, 콘솔 0, EditMode 71/71, 플레이에서 굽기 시작 → 남은 2.5/8초에 10번 프레임 → 완료 시 타이머 꺼짐.

### 4.34 설계 16 — 사건 버스: 선언은 한 곳, 발행은 주인, 구독은 버스 (2026-09-25)

- **기술 포인트**: 클래스마다 흩어진 C# event 21개를 `readonly struct` 사건 16개로 바꿔 `Events` 정적 partial 클래스(Area·Bakery·Plaza 파일)에 모았다. Core는 UnityEngine을 못 보므로 GameKit에 순수 C# `EventBus`를 새로 두고 `EventManager`(MonoSingleton)는 그것을 감싸는 파사드로 남겼다. 버스는 `GameManager.Init`이 꺼내 곳·지갑·굴 격자에 생성자로 넣고, 사건은 일이 일어난 객체(계산대·손님·통로·사물)가 직접 낸다. 모든 사건은 첫 필드가 발신자라 구독자가 자기 곳으로 한 줄 거르고, 두 번째 빵집이 생겨도 사건 이름은 늘지 않는다. 곳이 손님·사물 사건을 받아 다시 내던 전달 코드와 나가기·문 콜백이 사라졌다. 사물 하나만 보는 그림(오븐·진열대·웜뱃)은 C# event `Changed`를 그대로 둬 필터가 없다.
- **AI가 한 일**: 「EventManager 하나에서 관리됐으면 한다」는 요청에 세 안(버스만 · 선언까지 한 곳 · 지금)의 장단점을 표로 비교하고, Core가 싱글턴을 못 쓰는 제약과 partial이 어셈블리를 못 넘는 제약을 먼저 짚어 "2안 절충"을 제안했다. 손님 사건을 곳마다 따로 둘지는 사용자에게 물어 결정(곳마다 도착 행동이 달라질 수 있어 따로)했다. GameKit 패키지 테스트를 게임 EditMode에 포함시키는 `testables` 설정도 함께.
- **검증**: 컴파일 0, 콘솔 0, EditMode 128/128(GameKit `EventBusTests` 4: 타입별 전달·해제·구독자 없음·핸들러 안 해제/구독, 게임 `Passed_FromForeignArea_IsIgnored`). 플레이 스크립트: 굽기 → 꺼내기 → 채우기 → 결제(+10) → 업그레이드 2종 튀기기 → 오븐 설치 → 파기 → 광장 오가기, 효과음 3종 재생 시각 확인.

### 4.33 코드 리뷰 R2 — 사용자의 리뷰 기준을 배워 에이전트가 전부 스스로 리팩토링 (2026-09-24)

- **기술 포인트**: R1에서 사용자가 파일마다 준 기준(책임은 주인 객체에 · 개념은 뜻으로 · 무엇을 할 수 있나는 표에 · 파일·개념 줄이기 · 이름 규칙)을 메모리에 정리하고, 그 기준으로 84개 파일과 11개 표를 전부 다시 봤다. 20개 결정(설계 15): 계산대가 줄과 계산 타이머를 갖고(`CounterInteractable.Queue`·`Tick`·`Serve`), 손님은 `Visitor` 베이스(외형·Mover·톡 뛰기) 위에 `BakeryVisitor`(행동 트리와 잎 행동을 스스로)·`PlazaVisitor`(들를 곳 → 문·계단)로 나뉘어 곳에는 손님들 사이의 일만 남았다. 나가기·문 두 클래스 + 인터페이스를 `PassageInteractable` 하나로, `WombatWalker`를 `Wombat.Walk`로, 손 사건에 "어느 사물에서"를 실어 화면의 마지막 오븐 추적 꼼수를 없앴다. 값은 행동이 지갑에서 치르고(굴 격자는 돈을 모른다), 굴 공통 상수는 굴 코드로, 읽지 않던 데이터 칸 7개와 `TotalCoinsEarned`를 지웠다. 게임에 없는 "Shop" 이름을 World·UI·Editor·프리팹에서 뺐다(`BakeryView`·`WombatView`·`VisitorView`·`ControlHudView`, 파일과 .meta를 함께 옮겨 프리팹 참조 유지).
- **AI가 한 일**: 사용자 요청 「저장한 내용과 리뷰 특징대로 스스로 리팩토링」에 따라 질문 → 대안 → 최선을 스스로 정하고 한 번에 구현했다. 큰 이름 바꾸기는 정규식 치환 스크립트로 테스트까지 한 번에 옮기고, 컴파일 오류 3건(손 사건 인자)만 손으로 고쳤다. 코드 지도 아티팩트를 기능별 소통 다이어그램(기능 안의 화살표 + 다른 기능과 이어지는 곳을 모서리에 요약)으로 다시 만들었다.
- **검증**: 컴파일 0, 콘솔 0, EditMode 123/123(파기 값은 행동이 치름·carryCapacity 정수 검증 추가, 굴 격자·지갑 테스트를 새 책임에 맞게 고침). 플레이에서 굽기 → 자동 꺼내기(손 6) → 채우기 → 손님 결제(코인 +10) → 업그레이드 2종 → 오븐 설치 300 → 파기 150 → 광장 나가기·들어오기를 스크립트로 확인, 캡처에서 빵집·광장 그림 정상.

### 4.32 코드 리뷰 R1 — 업그레이드를 행동 하나로, 행동은 팩토리 중첩 클래스 (2026-09-24)

- **기술 포인트**: 사물의 `CanDo`/`Do`와 따로 놀던 업그레이드 장부(`Upgrades`)·`ShopUpgradeTable`·시트 전용 분기를 모두 지우고, 사물에 하는 일을 행동 객체 하나로 모았다. `ActionTable` 행마다 `ActionFactory`가 행동을 만들고, 행동 클래스는 팩토리 안 중첩 클래스로 sim마다 partial 파일(공통 3 · 빵집 7)에 둬 행동이 늘어도 파일이 늘지 않는다. 어느 사물에서 무엇을 하는지는 `InteractableTable.actions`, 어떻게 하는지는 `ActionTable.mode`(auto·manual·sheet)라 버튼·자동·시트 줄이 같은 틀이다. 업그레이드는 `upgrade` 행동 하나 + 사물 행 안의 `UpgradeInfo`, 단계는 곳이 종류별로 세고 그 값의 뜻은 사물이 `UpgradeValue`로 정한다. "값을 바꾸는 것만 업그레이드"라는 기준으로 오븐 추가는 빈 자리의 오븐 설치로 옮겼다. 행동은 일꾼(`Worker` = 손 + 지갑)을 받아 직원이 같은 행동을 쓸 수 있다. 검증기는 표의 mode와 코드의 행동 종류(`SheetAction` 여부)가 맞는지까지 본다.
- **AI가 한 일**: 사용자의 질문("업그레이드는 상호작용에 속하는 행동 아닌가", "InteractableTable 안에 업그레이드 정보를 넣을 수 있나")을 받아 팩토리·데이터 위치·레벨 범위를 객관식으로 좁히고, 행동마다 파일이 느는 불만에 중첩 클래스 + partial을 제안했다. 빈 자리를 한 행으로 두고 배치 제약은 곳의 규칙으로 따로 두자고 정리해 다음 설계(14)로 넘겼다. 폰트 테스트가 새 문구 "설치"의 두 글자를 잡아 아틀라스를 다시 구웠다.
- **검증**: 컴파일 0, 콘솔 0, EditMode 122/122(업그레이드 종류 공통, 시트 줄 상태 Poor·Max, 진열대 해금 안내 줄, 표의 모든 행동이 붙은 사물을 받는지, 업그레이드 데이터·행동 짝 검증 등 추가). 플레이에서 시트 5종의 글자·가격(오븐 설치 300, 파기 150, 업그레이드 Lv.0→1)과 굽기·업그레이드·진열대 해금·오븐 설치·파기를 UI 버튼으로 확인, 플레이 뒤 폰트 해시 그대로.

### 4.31 폰트 깨짐 — 동적 아틀라스를 정적으로 (2026-09-24)

- **기술 포인트**: 갈무리 TMP 폰트가 동적 아틀라스라 플레이마다 한글이 에셋에 더해져 파일이 바뀌었고, 그 변경을 부산물로 되돌리면 에디터 메모리의 글자 표와 디스크 아틀라스가 어긋나 글자가 깨졌다. 화면에 나올 수 있는 글자를 표에서 모으는 `Texts.Characters`(ASCII + 말줄임표 + 문구·빵·업그레이드 이름, 157자)로 아틀라스를 미리 굽고 Static으로 바꿨다. 굽기는 에셋을 지우지 않고 그 자리에서 다시 채워 GUID가 유지되므로 프리팹을 다시 굽지 않는다. TMP가 말줄임에 쓰는 U+2026을 기본 폰트(동적 NotoSans)에서 찾아 그 파일을 바꾸는 것도 플레이 후 해시 비교로 찾아, 글자에 넣고 기본 폰트를 Galmuri11로 바꿨다.
- **AI가 한 일**: "계속 깨진다"는 증상에서 원인을 자기 작업 습관(부산물 되돌리기)까지 거슬러 찾았다. 문구에 새 글자가 생기면 실패하는 폰트 테스트를 둬 다시 깨지기 전에 잡히게 했다.
- **검증**: 컴파일 0, 콘솔 0, EditMode 116/116(폰트 4종 × 정적·전 글자 보유). 플레이 35초(시트 5종·손님·광장 간판) 뒤 폰트 파일 5개 해시가 그대로.

### 4.30 설계 13 — 곳과 사물, 조립하는 구조로 (2026-09-24)

- **기술 포인트**: 1,100줄 `ShopSim`이 오븐·진열대·웜뱃·대상·업그레이드를 모두 직접 하던 구조를 뼈대 둘로 나눴다. 웜뱃이 다니는 곳 `WombatArea`(걷기·대상 고르기·auto 행동·버튼)를 빵집·광장이 상속하고, 곳 안의 사물 `Interactable`(상태·기준점·시간 흐름·행동)을 오븐·진열대·계산대·빈 자리·파기·나가기·문 7종이 상속한다. 곳은 사물을 조립하고 사이를 잇기만 한다. 사물 종류 enum과 순서를 맞춰 적던 문자열 배열이 없어지고, 대상은 사물 객체 참조가 됐다. 빈 자리·파기 자리는 칸 기준으로 객체를 유지해 다른 칸의 배치가 바뀌어도 보고 있던 대상이 흔들리지 않는다. 행동은 웜뱃이 아니라 손(`Hands`)을 받아 나중에 직원이 같은 사물을 쓸 수 있고, 웜뱃을 하나로 합쳐 든 빵이 광장에 갔다 와도 그대로다. 두 곳에 두 벌 있던 걷기·길 찾기 코드는 `Mover.WalkTo`와 베이스로 합쳤다.
- **AI가 한 일**: 사용자가 파일을 한 줄씩 읽으며 던진 질문(struct와 class의 차이, 상속 이름 규칙)에 C#·.NET 설계 지침 근거로 답하고, 사용자가 낸 3단 상속 안을 "가게가 하나뿐일 때 중간층은 추측"이라는 이유로 2단으로 줄이자고 제안했다. 커피숍·농사·투자 세 sim에 이 구조가 맞는지를 표로 먼저 따져 설계에 반영(행동의 주체를 손으로, 곳은 굴을 모르게)했다. 설계 문서를 확정받은 뒤 18개 파일 이동·7개 사물 클래스·UI·World·테스트를 한 번에 옮겼다.
- **검증**: 컴파일 0, 콘솔 0, EditMode 112/112(기존 107개가 이름만 바뀌어 전부 통과 + 오븐·진열대·손 단위, 대상 객체 유지, 광장 오가도 든 빵 유지 5개). 플레이에서 굽기 → 자동 꺼내기(손 6) → 자동 채우기 → 손님 결제(코인 350 → 410), 사물 5종 시트 열림, 광장 문 대상·들어가기를 확인했다.

### 4.29 코드 리뷰 R1 — 테이블 이름 규칙과 제네릭 TableSet (2026-09-24)

- **기술 포인트**: JSON 파일 = `table` 값 = 행 클래스 = `XXTable`로 이름을 하나로 묶었다. 행 클래스는 GameKit `Table<TId>`(Id string·int)를 상속하고, 조회는 `TableSet.Get<T>(id)`·`GetAll<T>()` 두 가지뿐이다. 클래스 이름으로 파일을 찾아 처음 부를 때 한 번 읽는다. 섹션 객체였던 `game_config.json`을 Id로 찾는 행 테이블 4개(`ConfigTable`·`BakeryConfigTable`·`PlazaConfigTable`·`PlazaDecorTable`)로 나눠 TableManager에 예외 경로가 없다. 문자열 칸(mode·target·carryAt·face)은 enum으로 읽어 `"manual"` 비교가 코드에서 사라졌다. table 이름·Id 빈 값·중복·모르는 enum 값은 읽을 때 한 곳에서 예외가 나고, 검증기에는 칸 값 규칙만 남았다. 테이블 12개를 각각 읽고 넘기던 `GameTables` 묶음 클래스가 없어져 서비스는 `TableSet` 하나를 받는다.
- **AI가 한 일**: 사용자가 파일을 한 줄씩 읽어 가며 준 요청을 정리하고 예시 코드 하나로 확인받았다. 그 뒤 제안 여섯 가지(제네릭 Id, 파일 행 순서 유지, GameKit에 두기, table 이름 대조, `StringTable` 이름 충돌 → `Texts`, Id 검사 한 곳)를 받아 전체 코드에 한 번에 적용했다. 테스트는 표를 바꿔 끼우던 목록 인자 대신 새로 읽은 TableSet의 행을 직접 고치거나 JSON 행을 빼고 읽게 바꿨다.
- **검증**: 컴파일 0, 콘솔 0, EditMode 107/107(모르는 enum 값·Id 중복·설정 행 빠짐 테스트 추가). 플레이에서 시작 코인 350(ConfigTable), 펭귄 손님 머리 위 빵(CarryAt.Head), 광장 장식 로드 오류 0을 확인했다.

### 4.28 진열대 재고 표지판 — 떠 있는 숫자를 칠판 입간판으로 (2026-09-24)

- **기술 포인트**: 진열대 위 흙바닥에 떠 있던 가는 글자 `2/8`을 없애고, 진열대 벽 쪽 앞 모서리에 칠판 입간판을 세워 분필 글자로 재고 수를 쓴다(품절은 주황 0이 깜빡임). 표지판은 진열대 자식이 아니라 따로 선 개체다 — 진열대는 SortingGroup이라 자식이 모두 진열대 발끝 기준으로 앞뒤가 정해져, 표지판 옆에 선 손님의 앞뒤가 틀어지기 때문. 자리는 Core `ShopLayout.ShelfSignBase`(진열대와 같은 2px 칸 격자, 오른쪽 열은 좌우 반전)가 정하고, 벽 쪽 옆 서는 자리는 표지판 너머로 옮겼다(기본 굴에서 걷는 땅 끝 −3.0).
- **AI가 한 일**: 에디터를 다른 세션이 쓰는 동안 에디터 없이 실제 캡처 위에 UI 팔레트·갈무리 비트맵 폰트(PIL은 16px 스트라이크만 받음)로 숫자 배지 3종을 칸 단위로 그려 비교 페이지로 냈다. 사용자가 「진열대 대각선 앞 표지판」을 새로 제안하자 진열대·식빵 그림에서 색을 뽑아 표지판 3종(칠판·나무 푯말·식빵 모양)을 다시 그렸다. 9px 한글 「품절」이 뭉개져 품절은 빨간 0으로 바꿨다. 같은 파일을 고치던 광장 세션과 파일 경계를 나눠, 겹치지 않는 스프라이트·표시 스크립트를 먼저 만들고 그쪽 푸시 뒤에 연결했다.
- **검증**: 컴파일 0, 콘솔 0, EditMode 103/103(서는 자리가 표지판과 0.55 넘게 떨어짐). 처음 벽 쪽 자리를 0.4 밖(−3.2)으로 옮겼다가 벽 여유에 걸려 서는 자리가 1개로 준 것을 테스트가 잡아, 걷는 땅 끝(−3.0)과 표지판 0.05 당김으로 맞췄다. 플레이에서 재고 3 → 2 분필 숫자, 품절 0, 벽 쪽 자리 펭귄이 표지판에 가리지 않는 것을 캡처로 확인.

### 4.27 빵집 알려진 문제 — 줄 가림·비켜 걷기·결제 연속음 (2026-09-24)

- **기술 포인트**: 줄 머리를 계산대 바로 위 걷는 줄(−4.8 → −5.0)로 내려 줄 손님(키 약 1.1)이 진열대 아랫단을 가리지 않게 했다. 비켜 걷기는 길 찾기를 건드리지 않고 Core가 손님마다 `Sidestep`(최대 0.25, 걷는 땅 여유 0.3 안)을 매 프레임 8/초로 부드럽게 맞춘다: 걷는 손님은 앞에 있는 손님 반대쪽 옆으로(한 줄로 마주 오면 서로 오른쪽으로 — 우측통행), 선 손님은 가까운 손님 반대쪽으로. 화면만 위치에 더하므로 도착 자리·줄·계산 규칙은 그대로다. 결제 연속음은 업그레이드 전 결제 간격(계산 1.5 + 줄 머리로 0.36 ≈ 1.9초)이 연속 판정 1초보다 길어 한 번도 오르지 않았다 → `sounds.pay.comboSeconds` 2.5.
- **AI가 한 일**: 재고를 채우고 웜뱃을 계산대에서 떼는 조회 스크립트로 줄 8명을 세워 캡처하고, 픽셀로 가림 폭(약 0.1유닛)을 재서 내릴 거리를 정했다. 피치 문제는 플레이 중 AudioSource 피치를 결제마다 읽어 1.00 고정을 먼저 보이고, 설계 10 → JSON 순서로 고쳤다. 같은 파일을 고칠 광장 세션과 파일 경계·커밋 순서를 메시지로 맞췄다.
- **검증**: 컴파일 0, 콘솔 0, EditMode 92/92(줄이 진열대 밑변 1.1 아래 · 붐비는 20초 동안 그린 자리 겹침이 길 위치 겹침의 절반 미만 · 비킴 ≤ 0.25). 플레이에서 가까운 손님 쌍의 길 거리 → 그린 거리 0.02 → 0.22, 0.20 → 0.53, 줄을 받는 동안 결제 피치 1.00 → 1.05 → … → 1.30.

### 4.27 기능 11 — 굴 밖 광장, 두 곳을 오가는 웜뱃 (2026-09-24)

- **기술 포인트**: 빵집과 같은 칸 격자·굴 그림·길 찾기를 그대로 써서 4×6칸 광장을 만들었다(`PlazaLayout`, 새 그림 규칙 없음). 두 곳을 묶는 `Mall`이 둘 다 매 프레임 돌리고 조이스틱 입력을 웜뱃이 있는 곳으로만 보낸다. 곳을 옮겨도 누르던 방향은 그대로 넘긴다. HUD와 웜뱃 그림은 `IWombatArea` 하나만 보고, 손님 그림은 `IWalker`로 빵집 손님·광장 손님이 같은 프리팹을 쓴다. 손님 도착은 빵집에서 광장 지상 계단으로 옮겼다. 광장 손님은 장식 둘레의 들를 곳(데이터 `decorations.spots`)을 한두 곳 들러 ♥를 띄운 뒤 빵집 문으로 들어가고, 외형은 Core가 정해 빵집 안까지, 나올 때 다시 광장까지 이어진다. 나가기·들어가기는 행동·사물 표에 한 줄씩(`exit`·`enter`, `exit`·`door`)이라 버튼·아이콘 체계가 그대로다.
- **AI가 한 일**: 복도 목업(층별 복도)과 광장 목업을 실제 스프라이트로 걸어 볼 수 있게 만들어 방향을 고르게 했고, 답을 기획서 v0.4 확정 결정표에 올렸다. 같은 시간 빵집 세션이 손님 코드를 고치고 있어서, 광장 코드는 작업 트리 밖에서 준비했다가 그쪽 푸시 뒤에 얹었다. 반쯤 고친 코드가 다른 세션의 컴파일을 깨뜨릴 수 있다는 걸 알아채고 옮긴 것이다. 사용자가 에디터에서 플레이하는 동안에는 굽기를 멈추고 정지를 기다렸다.
- **검증**: 컴파일 0, 콘솔 0, EditMode 103/103(광장 5 + 검증기 5 + 장식 그림 1 추가). 빵집이 꽉 차면 들어가려던 손님이 들를 곳만 끝없이 도는 버그를 테스트가 잡아, 한 곳 더 들른 뒤 떠나게 고쳤다. 플레이 캡처: 빵집 → 나가기 → 광장(문·차양·간판·계단·분수·장식, 손님 ♥) → 들어가기.

### 4.26 굴 환경 A2 — 파 내려간 방으로 읽히는 굴 (2026-09-24)

- **기술 포인트**: 방 윗변 안쪽에 지층 띠(40칸)를 세워 평평한 흙 오림이 파 내려간 방으로 보이게 했다. 걷는 바닥 윗변은 그대로 두고 방 윤곽만 띠만큼 올렸기 때문에, 손님 동선·사물 자리·길 찾기는 한 줄도 바뀌지 않았다. 대신 굴 마스크에 띠 행(`WallRow`)을 더하고 길 찾기는 `IsFloor`(방 안이면서 띠가 아님)만 본다. 모서리 둥글리기를 칸 꼭짓점 대신 픽셀 격자점마다 보게 바꿔서, 옆으로 판 칸의 띠가 이웃 바닥과 만나는 안쪽 모서리까지 같은 코드가 처리한다. 지층은 격자 줄 기준으로 샘플해 둥근 모서리에서도 휘지 않고 윤곽에 잘린다. 방 둘레는 8방향 거리 6겹 턱(선·턱 4·선)이다.
- **AI가 한 일**: 아트방 세션이 캡처 위 Codex 콘셉트 3방향으로 사용자 선택(A2 벽 높이, C 아늑한 내부는 나중 업그레이드)을 받아 규격을 제안했다. 이 세션은 콘셉트 이미지의 픽셀을 재서 띠 높이(40칸)를 정했고, 바닥을 깎지 않는 쪽으로 규격을 바꿔 돌려보냈다. 실제 재료가 오기 전에 더미 지층으로 먼저 붙여 보이고, 사용자가 고른 턱 두께·색을 받아 칠하는 규칙만 바꿨다. 같은 시간 리뷰 세션과 Unity 에디터 순서를 메시지로 맞추고, 한 파일에 섞인 변경은 각자 자기 줄만 커밋했다.
- **검증**: 컴파일 0, 콘솔 0, EditMode 91/91(굴 모양 테스트 4개를 띠 규칙으로 다시 씀). 플레이 중 칸을 파서 옆 칸 띠·안쪽 모서리·턱을 캡처. 굽기 부산물인 폰트 에셋을 되돌린 뒤 글자가 빠지는 것을 찾아, 폰트 재임포트 + 스크립트 리로드로 해결했다.

### 4.25 손이 덜 가게 — 꺼내기 자동, 빈 자리 글자·굴 윗변 곡선 삭제 (2026-09-23)

- **기술 포인트**: 「다 구운 빵을 눌러서 받는 것도 스트레스」라는 지적에, 코드 변경 없이 `actions.json`의 take_out을 manual → auto로 바꿨다(설계 09 v0.4에서 행동을 데이터로 뺀 덕). 화면 쪽은 날아가는 빵의 출발 오븐을 '버튼 대상'이 아니라 '방금 바뀐 오븐'(꺼내기는 오븐 사건 직후 들기 사건)으로 고쳤다. 입구 줄은 4분의 1 타원 깔때기를 지우고 아치 구멍 가운데부터 줄 폭 전체 바닥으로, 윗변 모서리는 다른 면과 같은 반지름으로 깎는다. 「빈 자리」 글자 태그는 문구 키째 지우고 점선 칸만 남겼다.
- **AI가 한 일**: 수동을 가정한 테스트 4개를 자동에 맞게 고치고, 수동 경로는 표를 바꾸는 테스트로 남겼다. 쓰지 않게 된 꺼내기 아이콘·생성 코드·문구 키를 규칙(auto 행동은 아이콘 없음)대로 지웠다. 아트방이 원인·사양을 정리해 넘긴 굴 윗변 수정을 이 세션이 Core에 반영했다.
- **검증**: 컴파일 0, 콘솔 0, EditMode 91/91, 플레이에서 평평한 윗변·점선 칸만 남은 빈 자리·오븐 앞에 가기만 하면 6개를 드는 것을 캡처로 확인.

### 4.24 상단 HUD 시안 C — 바를 없애고 캡슐만 (2026-09-23)

- **기술 포인트**: 초록 바와 가게 이름 줄을 지우고, 왼쪽 위에 반투명 진갈색 캡슐(9-slice `pill_hud`) 하나만 띄웠다. 캡슐은 HorizontalLayoutGroup + ContentSizeFitter로 숫자 길이에 맞춰 늘어난다. 비트맵 폰트라 TMP 밑깔림 대신 같은 글자를 한 번 더 그려 1px 그림자를 만들었다. 그림자 글자가 폭을 잡는 레이아웃 요소가 되고, 본 글자는 그 자식이라 위에 그려진다. 가게 이름 문자열은 테이블에서 지웠다(strings v10).
- **AI가 한 일**: 아트방 세션이 실제 캡처 위에 UI 칸 그대로 합성한 시안 3개로 사용자 선택을 받아 숫자 사양을 넘겼고, 이 세션이 굽기 스크립트로 옮겼다. 사용자가 플레이 중이라 백그라운드에서 플레이 종료를 기다린 뒤 임포트·굽기를 했다.
- **검증**: 컴파일 0, 콘솔 0, EditMode 91/91, 플레이에서 350 · 123.8K 두 값으로 캡슐 폭이 늘어나는 것을 캡처.

### 4.23 기능 10 — 연출 2순위: 오븐 상태 표시·빵 나르기·효과음 (2026-09-23)

- **기술 포인트**: 이미 있는 Core 사건을 화면이 읽기만 해서 연출을 붙였다. Core에 늘린 것은 업그레이드 하나를 샀다는 이벤트 `Upgraded(id)` 하나다. 오븐 타이머 자리에 빈 오븐 = 오르내리는 화살표(웜뱃이 그 오븐 앞이면 끔), 다 구움 = 빈 원판 + 식빵과 개수. 꺼내면 빵 하나가 오븐 → 머리로 포물선을 그리고 도착해야 층이 늘며, 채우면 머리 → 진열대로 날아간다. 업그레이드는 `shop_upgrades.target`으로 월드 대상(진열대 전부·오븐 전부·계산대)을 튀기고, 새로 놓인 진열대·오븐도 한 번 튄다. 공용 `PressScale`(누르는 동안 0.95배)을 굽기 스크립트가 모든 버튼에 붙인다. 효과음은 새 테이블 `sounds.json`(결제·완료·화남)으로, 소리마다 AudioSource를 따로 두어 결제 연속음만 피치를 올린다(0.1초 안 중복은 건너뜀).
- **AI가 한 일**: 5일 전 점검표의 2순위를 지금 조작(조이스틱)에 맞춰 다시 골랐다 — 탭 거절 흔들기·지상 전환은 빼고, 업그레이드 줄 번쩍·빨간 비용은 이미 있음을 확인. 결정 2개(빈 오븐 표시·사운드 범위)를 객관식으로 받고, 작업 중 아트방이 넘긴 오븐 표시 2장(사용자 선택)으로 빈 오븐 표시를 바꿨다. 더미 효과음 3개는 이미지 도구 없이 Python 표준 라이브러리로 합성(사인 두 번 짤랑·배음 감쇠 종·내려가는 사각파 콧소리).
- **검증**: 컴파일 0, 콘솔 0, EditMode 89/89(효과음 표 검증·파일 존재 6개 추가). 플레이에서 `Time.timeScale` 0.1로 늦춰 날아가는 빵과 튀기기를 캡처·스케일 조회, 채널별 마지막 재생 시각으로 세 소리가 난 것을 확인. 날아가는 빵이 쌓인 층(0.6배)보다 크게 보이는 것을 캡처에서 찾아 그 자리에서 맞췄다.
- **든 물건은 손에**: 사용자 지적(「대부분의 동물은 손이 있다」)으로 든 빵을 머리 위 대신 앞발에서 위로 쌓게 바꿨다. 그림에서 앞발 높이를 재어(웜뱃 발끝 위 0.21유닛, 손님은 키의 약 1/3) 앞모습은 몸 앞, 옆모습은 보는 쪽으로 내밀고, 뒷모습은 몸 뒤에 그린다. 날개를 벌린 펭귄만 머리 위 — 「다른 물건을 들 수도 있으니 데이터로」라는 요청에 `visitors.carryAt`(hand/head) 칼럼을 두었다. EditMode 91/91.
- **다른 세션과 나눠 하기**: 같은 시간 아트방 세션이 정면 식빵·발바닥 코인·오븐 표시를 만들어 임포트하면, 이 세션이 플레이에서 확인해 결과(확대 캡처 경로)를 메시지로 돌려줬다. 새 코인 확인 중 결제 코인이 오븐 뒤로 가려지는(그리는 순서 0) 옛 문제를 찾아 순서와 금액 글자 높이를 고쳤다.

## 5. 숫자 (기능 완료 시 갱신)

| 항목 | 값 |
|---|---|
| 커밋 | 110 (2026-09-25, 코드-규칙 v1.14 커밋까지) |
| C# 파일 | Core 50 · Data 1 · Game 3 · UI 9 · World 19 · Editor 5 · Tests 15 (+ GameKit 테스트 1) |
| EditMode 테스트 케이스 | 128 (GameKit 4 포함) |
| JSON 테이블 | 11 (VisitorTable 4행 v8, BreadTable 3행 v2, ActionTable 12행 v4, InteractableTable 7행 v5, SoundTable 3행 v2, DecorationTable 8행 v3, ConfigTable 8행 v2, BakeryConfigTable 1행 v3, PlazaConfigTable 1행 v1, PlazaDecorTable 9행 v1, StringTable v14) |
| 사람이 직접 쓴 코드 | 0줄 (검토·결정·확인만) |
