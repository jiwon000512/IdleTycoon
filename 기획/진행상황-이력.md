# 진행 상황 이력 (2026-09-13 ~ 09-17, 보관용)

2026-09-18에 `진행상황.md`를 세 절로 줄이며 옮겨 둔 원문이다. 세션 시작 시 읽지 않는다. 과거 결정의 경위·Unity CLI에서 겪은 일·더미 리소스 표가 필요할 때만 검색한다.

## 현재 상태 (2026-09-17)

- 기반 세팅 완료: asmdef 6개, JSON 테이블 5개, GameKit v0.1 로컬 연결(`file:../../../UnityGameKit`), TMP 임포트. **GameKit은 건드리지 않았다.**
- 기능 01 완료·커밋(`028ff30`).
- **기획서 v0.9: 2D 월드 맵으로 방향 전환**(2026-09-14). 동물이 우리 안을 돌아다니고 관광객이 우리 사이 길을 걷는다. 카메라 자유 팬. 첫 플레이는 우리 하나. 중복 = 마리 추가.
- **기능 02(가차)·03(월드 슬라이스) 완료, 커밋됨.** 시점은 **원근 카메라(55°, FOV 60) + XZ 평면 + 스프라이트 카드**로 확정(설계 03 6장). 우리는 사각형, 테스트용 3개(가운데·뒤·앞). 사용자 확인 "아주 좋다".
- 과잉 설계 정리 커밋(`db41235`, -105줄): `ZooLevelService.CageCount`·`IsPromotionUnlocked` 삭제(06/07에서 필요할 때 다시 추가), `ZooState.CoinsChanged`는 인자 없는 `Action`.
- 기능 04(수입 틱) 커밋(`d115b83`). 기획서 v0.10, 코드-규칙 v1.6, 데이터-테이블-규칙 v1.6(`game_config` version 4).
- 기능 05(동물 데이터와 상태, 웜뱃) 완료·커밋. 기능 06(관광객) 완료·커밋(`218753a`). 기획서 v0.11, 데이터-테이블-규칙 v1.7(`animals` version 2). **더미 8종 삭제 → 웜뱃 1종(a01).** 등급 확률은 동물이 있는 등급만 정규화(지금 일반 100%). 동물 스탯·연출값 전부 JSON, `AnimalActor`는 Idle·Move FSM + `SpriteAnimator`(프레임 순환).
- **구조 정리(설계 06-1, 2026-09-14, 미커밋)**: 리뷰 8건 반영. `GameManager.Init` 1회 가드 · 마리 수 = `Count`(`PullOutcome.NewSpecies/Duplicate`, `ZooState.AddAnimal → int`, `game_config.animalCount` version 6, 문구 키 `gacha_result_duplicate`) · `IncomeService.IncomePerSecond`+`IncomeChanged`가 초당 수입 단일 소유자 · `FrameCache` · **월드 개체 `Unit` → `Animal`·`Visitor` + `UnitState`(`WalkState`·`WaitState`) 상태 객체, `...Actor`·enum FSM 폐지** · Data asmdef Newtonsoft 삭제 · `GachaResultView`를 `MainScene`이 `UIManager.GetView`로 주입(**GameKit `UIManager.GetView<T>` public으로 변경, GameKit 저장소 미커밋**). 기획서 v0.15, 데이터-테이블-규칙 v1.9, 코드-규칙 v1.7.
- **기능 07-3(타일맵 + 1호점 맵) 완료(2026-09-16, 미커밋)**: 바닥(잔디·흙·길)은 Unity Tilemap(Isometric Grid 1×0.5), 타일 64×32 px = 논리 1유닛, `Iso` k **0.5/0.25**. `CageView` → **`ZoneView`**(6×8 고정, 울타리만 세움, 정수 좌표 기둥 + 뒤 모서리 `fence_post`), **`BranchView`**(맵 사각형 + 존 목록). 프리팹 `Zone.prefab`·`Branch01.prefab`(존 10개 z01~z10, X 2줄 × Z 5줄, 길 2칸, 맵 18×52, 타일은 프리팹에 미리 칠해짐). `WorldView.Zone`은 08까지 z01. 카메라 size 6, 초점 (−0.5, 1.25). `animals`·`visitors` scale 0.6, `facilities` 슬롯 ±2.2/±2.8. 기획서 v0.20, 데이터-테이블-규칙 v1.11. 검증: 컴파일 0·콘솔 0·EditMode 98/98·캡처(존 10개·웜뱃 3·시설 2·관광객). 논의 아티팩트: 타일맵 (아티팩트 삭제됨 2026-09-17) · 1호점 배치 (아티팩트 삭제됨 2026-09-17)
- **흙 타일 실제 아트 3차(2026-09-16, 미커밋, 사용자 피드백 대기)**: 1차(자갈 18개·모래 얼룩)는 "너무 나갔다", 2차(더미 색·자갈 4개)는 "디테일 더", 3차 = 2차 색 + 1~2px 모래 알갱이 촘촘히(밝은·어두운 톤) + 자갈 4개. 파이프라인: : Codex로 top-down seamless 흙 텍스처(웜뱃 참조) → `Sprites/World/Tiles/Source~/make_dirt.py`(인자: 원본, 출력 폴더, 격자 G=96(32의 배수여야 주기가 맞음), 팔레트 문턱 0.00005)(탐욕 팔레트 군집 → 108² 픽셀 격자 → 화면 공간 2:1 압축 → 마름모 18장 시트 `dirt.png`) → Tile `Dirt_00~17` → `Branch01.prefab` Floor를 `(x−z) mod 6, (x+z) mod 6` 색인으로 다시 칠함(주기 3칸, 이음새 없음). 원본은 `Source~/dirt_topdown.png`. 잔디·길도 같은 파이프라인으로 뽑을 것. 웜뱃과 흙 색이 비슷해 개체가 묻히는 문제는 사용자 판단 대기.
- **울타리 실제 아트 2차 + 프리팹에 굽기(2026-09-16, 미커밋, 사용자 피드백 대기)**: 기둥 30 → 26px(사용자 요청). **울타리 29개는 이제 `Zone.prefab` 자식**(`FenceFront/Back0~5`, `FenceLeft/Right0~7` flipX, `FenceCornerPost`; scratchpad `BakeFence.cs`가 한 번 구움). `ZoneView`는 `m_center`·`m_walkArea`·`m_pathArea`만 남고 `Awake`는 위치만 잡는다(`m_groundArea`·`WorldBounds`·`BuildFence` 삭제). 이유: 존 크기가 6×8로 고정돼 코드로 세울 이유가 없어졌고, 에디터에서 바로 보이며 칸 단위로 손볼 수 있다. 1차: Codex로 웜뱃 풍 울타리 한 칸 참조(`Sprites/World/Source~/fence_ref.png`)를 뽑아 팔레트(외곽선 36,24,24 · 나무 168,132,108 · 밝음 192,144,120 · 하이라이트 216,180,156 · 그늘 132,96,84)만 쓰고, 게임용 `fence.png`(38×36, 기둥 6px + 캡, 가로대 2개 5px, 기둥 중심 x=4에서 오른쪽 32px·위 16px, 가로대는 x 7~32까지만 그려 다음 기둥 외곽선에 닿음)와 `fence_post.png`(8×32)는 `Source~/make_fence.py`가 픽셀 단위로 그린다(2:1 기울기·32px 폭이 정확해야 해서 생성물을 그대로 쓰지 않음). 피벗 (4/38, 0)·(4/8, 0), PPU 64. 흙 타일 3차는 확정·커밋(`43fd8b4`).
- **아트 생성은 에이전트가 직접**: Codex CLI(ChatGPT 구독 로그인)의 내장 이미지 생성을 stdin 프롬프트 + `-i` 참조로 호출(자물쇠 1장 테스트 성공). 후처리는 winget으로 설치한 Windows Python 3.12(Pillow·numpy) `C:\Users\jiwon\AppData\Local\Programs\Python\Python312\python.exe`. PATH의 python은 MSYS2용(pip 없음). 아트 생성 단위(seamless → 타일 자르기 vs 타일 직접)는 미정.
- **기능 07-2(순수 2D 쿼터뷰) 완료(2026-09-16, 커밋 `76cd6f3`)**: 07-1의 3D 카메라·빌보드를 버리고 XY 평면 + `Iso`(논리 XZ → 화면, kX 0.7071·kY 0.3536) + 화면 y 정렬(축 (0,1,0)). `Unit.Logical`이 진짜 위치. `CageView`가 논리 사각형 3개와 스프라이트 3장(바닥·길·울타리 칸)으로 바닥·길·울타리를 `Awake`에서 세움. 더미 `Sprites/World/zone_floor.png`·`zone_path.png`·`fence.png`(순수 python 생성). 기획서 v0.19. 실제 아트 프롬프트는 `기획/아트-프롬프트.md` 월드 절. `ground.png`·`path.png`·`fence_rail.png`(사용자 실제 난간)는 더 이상 참조 안 함(파일은 남김).
- **기획 전환 v0.17(2026-09-15, 미구현)**: 뽑기 폐기. 존(`zones.json`, 3자리, 존별 고정 최대 마리 수·개장 비용·해금) → 종 선택 입주(한 존 한 종, 입주비) → 번식(종 주기, 존 한도, 오프라인 포함) → 종 단위 의상(`costumes.json`, 모자·옷, 수입 배수). 등급·가차 코드·프리팹·문구 삭제 예정. 고양이 오피스 구조 분석에서 나온 결정(기획서 9.1 근거).
- **기능 07(우리 꾸미기) 완료·커밋(`65196dd`)**(2026-09-15). 기획 전환: 우리는 지점당 하나, 성장은 시설·장식(`facilities.json` 4행, 홍보가 첫 행). `ZooState` 시설별 단계 + `FacilityService`(Core), 꾸미기 버튼 → 팝업(행 프리팹), World `Facility` 카드가 우리 안 네 모서리에. 씬은 우리 하나(`WorldView.m_cage`). `zoo_levels` v2, `game_config` v7, 기획서 v0.16, 데이터-테이블-규칙 v1.10.
- **구조 변경(사용자 지시, 설계 04 D1·D2)**: `GameBootstrap` 폐지 → **`GameManager : MonoSingleton`**(Game. `Init()`이 테이블 로드·검증·상태·서비스 생성, `Update`가 1초 틱) + **씬 스크립트 `MainScene`**(`Awake`에서 `GameManager.Instance.Init()` 후 그 씬의 View·Presenter만 조립). UI는 `GameManager`를 직접 못 본다(asmdef `Game → UI`), MainScene이 서비스를 생성자로 넘긴다.

## 구현된 기능

01 부트스트랩·상단 바 → 02 가차 → 03 월드 슬라이스 → 04 수입 틱·GameManager → 05 동물 데이터(웜뱃) → 06 관광객 → 06-1 구조 정리 → 07 우리 꾸미기(시설) → 07-2 순수 2D 쿼터뷰 → 07-3 타일맵·1호점 → 07-4 맵 가장자리 → 07-5 존 3×3·카메라 클램프. 지금 코드에 살아 있는 결정은 **`기획/설계/00-구현-요약.md`** 한 장에 있고, 기능별 설계 원문(결정 경위·QA 표)은 git 이력(커밋 `6b30668` 이전)에 있다.

## 기능 로드맵 (수직 슬라이스, 세션당 하나)

| # | 기능 | 설계 문서 | 상태 |
|---|---|---|---|
| 01 | 부트스트랩과 상단 바 | `기획/설계/00-구현-요약.md` | 완료 |
| 02 | 가차 | `기획/설계/00-구현-요약.md` | 완료 |
| 03 | 월드 슬라이스 | `기획/설계/00-구현-요약.md` | 완료 |
| 04 | 수입 틱 (초당 수입 적립, 가차 버튼의 "남은 시간") | `기획/설계/00-구현-요약.md` | 완료 |
| 05 | 동물 데이터와 상태 (웜뱃) | `기획/설계/00-구현-요약.md` | 완료 |
| 06 | 관광객 (우리 앞 길 띠, 구경 + 코인 연출, 수 = 초당 수입) | `기획/설계/00-구현-요약.md` | 완료 |
| 07 | 우리 꾸미기(시설 테이블, 홍보는 첫 행, 정해진 자리 배치, 동물원 레벨 해금). 이전 07(우리 해금·맵 확장)+08(홍보)을 대체 | `기획/설계/00-구현-요약.md` | 완료 |
| 08 | 존·입주·번식(뽑기 삭제, `zones.json` 10행, 존 팝업, 번식 틱, 잠금 표시, 관광객 길 경로) | `기획/설계/08-존-입주-번식.md`(07-3 기준으로 수정 필요) | 다음 |
| 09 | 의상(`costumes.json`, 종 단위 모자·옷, 개체 두 번째 레이어, 꾸미기 팝업 의상 탭) | — | |
| 10 | 저장·로드·오프라인 수익 팝업(오프라인 번식 포함) | — | |
| 11 | 도감 팝업 | — | |

## 다음 세션에서 할 것

- **7회차 완료(2026-09-17) — 사용자 답 대기**: 6회차 평 "참신하지만 매출이 날지 모르겠다 → 다시 타이쿤으로, 기존 장르와 겹쳐도 된다". 순서: ① 타이쿤 매출 웹 조사(`생성7/01`) ② 타이쿤 추가 문답 T1~T4(`재미-프로필.md`: 한 화면 가게 키우기·숫자 방치 / 새 가게·손 놓기·뽑기가 최고의 순간 / 동물원일 필요 없음 / 환생·다음 가게 둘 다) ③ 참신함 비채점으로 기획 30개(생성 5: 검증된 뼈대 / 업종 / 동물 직원 / 벽과 돌파 / 지난 규칙 이식) ④ 심사 2(사용자 대리 `생성7/07`, 프로듀서·매출 `생성7/08`) → 최종 10. 아티팩트 https://claude.ai/artifact/BdqaeC64pmJgAoQMRdFBvb . **조사의 정직한 숫자: 1인·유료 UA 0이면 통상 월 $100~3,000, 월 $1만은 인디 100개 중 1개. 기획이 바꾸는 건 구간 안 위치(2~4배)와 상단 통로(피처드·페스티벌·앱인토스·퍼블리셔)에 올라탈 확률뿐. 검증 구조는 고양이 스낵바형 하나, 테마가 결제층을 정한다(고양이 > 동물 일반).** 순위 = (사용자 대리 + 프로듀서×2)/3: 1 **웜뱃 회전굴**(접시 도장 = 값 ×2, 값이냐 속도냐. 두 심사 모두 2위, 뽑기의 맛 최고, 구별점이 스크린샷에 보임, 4~5개월·230f, 도장 곱셈 상한 필수) · 2 굴 온천(문의 깊이 하나가 방 수·객단가·방치 배수, 실패 −1층. 6회차 1위의 타이쿤판) · 3 빵집 러시(줄 10칸 = HP, 러시 번호 = 끝없는 스테이지. 사용자 대리 1위 / 프로듀서 6위 '클론으로 보여 설치 최하') · 4 온천 마을(프로듀서 1위 '지금 만든다면 이것' — 카피바라 밈·코지·일본, 오가닉 $240~3,000 최상, 아트 최중량 5~6개월. 사용자 대리는 '예보 보고 온도 맞추기 = 숙제'로 10위 밖 → 내 제안: 탕 온도 = 탕지기 직원이 정함 + 단체 버스 3택 + 지점 자연 조건, **심사 안 거침**) · 5 24시 편의굴(방치 = 밤조 편성, 설치당 가치 최고·훅 약함) · 6 야시장(규칙 카드 3택, 화면에 안 보임) · 7 웜뱃역 매점(지갑 = HP, 프로필에 최근접·기차는 아동 분류 위험) · 8 나무 타워(n층 n배속, 새 가게 = 그림 4장) · 9 택배(버릇, 2.5~3개월 최저가·매출 상단 최저) · 10 굴 호텔. 온천은 네 생성자가 따로 도달한 유일한 업종. 두 심사 공통 경고: 생성자가 넣은 자동화 9곳이 전부 결정을 지움(노동만 가져가는 자동화만 허용) · 아트 견적 약 2배 과소 · '이 게임만의 한 가지'는 스크린샷에 보일 때만 돈이 된다. 프로듀서 출시 전략: 공통 코어(대기 큐 + 직원 자리) 2주 → 회색 상자 2개(회전굴·온천) → 8주 차 D1 40% 컷 → 소프트 런칭(호주) → 24주, LevelPlay·보상형만·광고 제거 최상위, 13세 이상 '퇴근한 직장인 동물' 톤, 확률표는 같은 JSON, **쿼터뷰 동물원 맵은 버리고 Core·Data·MVP·GameKit·웜뱃·이미지 파이프라인은 재사용**. 열린 질문: Q1 해 보고 싶은 것 / Q2 온천이면 한 화면 마을 vs 굴 단면 / Q3 이 매출 구간을 보고도 6개월 타이쿤인가, 3개월짜리로 D1부터 재나 / Q4 본 기획서 vs 공통 코어 + 회색 상자 설계 vs 한 번 더. 목업 14장 `기획/루프기획/목업7/`, 본문 `round7.body.html`, 조립 scratchpad `assemble7.py`·`mock7_prompts.py`. 전부 **미커밋**.
- **6회차 완료(2026-09-17) — 사용자 답 대기**: 재미 프로필을 고정 틀로 웜뱃 디펜스 기획 30개(생성 5: 판의 모양 / 웜뱃의 몸과 굴 / 2024+ 히트작 교배 / 4·5회차 규칙 이식 / 동물별 동사) → 심사 2(사용자 대리 `생성6/06`, 프로듀서 `생성6/07`) → 최종 10. 아티팩트 https://claude.ai/artifact/QHZPxHjaNuQgCePPNYNG7v . **두 심사 모두 1위 = 「웜뱃 엉덩이 디펜스: 들였다가 쿵」**(굴 단면 5층·옆방 10칸, 웜뱃이 막을 층을 고르면 마개 위 옆방만 싸우고 깊이 하나가 활성 자리·쿵 배수·전리품 배수·방치 배수를 정한다. 뚫리면 한 층 물러남. 동물은 동사: 쿼카 끌어당김·딩고 몰이·코알라 재움·금조 가짜 엉덩이·데블 마개 아래 등. 다섯 생성자가 따로 5번 도달. 프로듀서: 무음 15초 훅 완결, 1차원이라 기술 위험 최저, 아트 ~170프레임, 3~4개월. 약점: 굴 하나라 그림이 같다 → D7 금고 굴(왕복 도둑)·D14 옆방 흔적·지층 벽·보따리 가차 포장을 개방 로드맵으로). 2 웜뱃 급행(칸 순서 = 편성, 꼬리 분리. 1위와 코어 70% 공유) · 3 웜뱃 목말(동물 탑, 층 높이 = 상대하는 적, 아트 ~110으로 최저·아이콘 최강) · 4 파고파고 웜뱃굴(조각 3택, 지나온 굴 = 방치 생산, 리텐 5, 6개월 쪽) · 5 자리가 기억한다(지킨 시간이 자리에 흔적, 교체가 수확. 단독보다 D7~D14 부품) · 6 자면 돌 깨면 웜뱃(낮밤 교대, 자는 동물 = 벽) · 7 똥벽돌(쓰러진 적 = 블록. 사용자 대리 2위 / 프로듀서 실현 2로 갈림) · 8 웜뱃 등반대(기력 바 하나) · 9 대장 따라 헤쳐 모여(서열 1번의 성격 = 진형, 제작비 최상위) · 10 주머니 원정대. 탈락: 보따리 성벽(프로듀서 수익 1위지만 메뉴 세 겹 → 가차 포장으로만 이식)·족보 디펜스·열두 근·둑 쌓기·뺑뺑이 순찰대. 심사 경고 두 가지: ① 추천 배치·자동 깔기 같은 '자동화로 고쳤다'는 결정을 지운다 → 결정의 빈도를 낮추는 구조로 풀 것 ② 적 아트 견적 2배·이펙트 별도·특수 배수는 전부 상한. 프로듀서 제안: 회색 상자로 1위 1~2주 + 2위 1주 만들어 보고 고른다, 24주 일정, 공통 제작 기반 12단계(수치 곡선 + 전투 시뮬레이터가 먼저). 열린 질문: Q1 해 보고 싶은 것이 있는가·무엇 때문에 / Q2 전투 중 손이 너무 한가한가(열 개는 대부분 '배치해 두고 본다 + 웨이브 사이 3택'의 느긋한 쪽 — 재미 프로필의 '배치·소환은 직접'이 판 안의 바쁨을 뜻하는지 아직 모름) / Q3 다음은 본 기획서 vs 회색 상자 프로토타입 설계 vs 한 번 더 / Q4 없다면 프로필의 무엇을 잘못 읽었나. 목업 12장 `기획/루프기획/목업6/`, 본문 `round6.body.html`, 조립 scratchpad `assemble6.py`. Codex 프롬프트는 Bash 인라인이 따옴표에서 깨져서 파이썬 파일(`mock6_prompts.py`)로 prompt.txt를 만든 뒤 4개씩 병렬로 돌렸다(12/12 성공). 전부 **미커밋**.
- **재미 프로필 확정(2026-09-17) — 다음 기획의 채점 기준은 `기획/재미-프로필.md`**: 5회차 뒤 사용자가 "결국 내가 재미있는지 검증해야 하니 내 재미 요소를 먼저 조사하라"고 해 객관식 19문항을 하나씩 물었다. 결론: 웜뱃이 주인공인 동물 세계 · 배치·소환은 직접/싸움은 자동인 디펜스 · 끝없는 스테이지 + 방치 보상 · 운은 후보를 주고 내가 고름 · 새 동물로 편성이 바뀌고 새 시스템이 단계적으로 열림 · 복잡한 메뉴·재화 싫음 · 2D(뷰는 새로) · 1순위 수익(광고·가차·패스·패키지 전부 허용). 5회차는 "전반적으로 안 끌렸다"(퍼즐이라 피곤·미니게임 같음·성장 없음, 선로 이어 가기만 조금 끌림). **다음**: 이 프로필로 6회차 — 큰 틀(키우기 + 배치형 디펜스 + 방치)은 고정하고 창의성은 디펜스 핵심 규칙과 동물별 역할에. 아직 안 물은 것: 디펜스 판의 모양 · 협동/PvP · 스테이지 길이 · 합성 여부 · 기존 타이쿤 코드 처리.
- **기획 진행 중(5회차까지, 2026-09-17) — 방향 자체가 열려 있다**: 5회차에서 사용자가 "현재 게임 시스템을 유지하지 않아도 된다, 웜뱃 하나만 써도 되고 다른 리소스여도 되고 뷰가 달라져도 3D여도 된다"고 했다. 사용자 답 대기 — 5회차 Q1(타이쿤을 접나, 두고 옆에서 1주 프로토타입을 돌리나. 내 권고는 후자: 별도 씬 하나씩, 기존 프로젝트 안 건드림) / Q2(열 개 중 끌리는 것·지울 것, 세 갈래 = 안전(선로를 먹는 기차) / 터질 수 있음(전원 눈 뜬 순간·하품 전염) / 이 기기에서만(손 그늘)) / Q3(1주 프로토타입을 실제로 만들 것인가, 권장 순서 점수판 인건비 → 선로를 먹는 기차 → 전원 눈 뜬 순간 → 손 그늘) / Q4(웜뱃을 계속 주인공으로 둘 것인가). 6회차 후보: ㉠ 고른 1~2개의 프로토타입 설계 문서 ㉡ 한 번 더 뽑되 웜뱃 사실 목록을 빼고 생성자마다 재료를 다르게 ㉢ '더 세게' 판본만 10개 ㉣ 수익 모델·세션 구조를 2024+ 레퍼런스에 맞춰. 4회차 Q1~Q4·3회차 Q1~Q4는 타이쿤을 유지할 때만 유효. **설계 08(존·입주·번식)은 이 결정이 날 때까지 보류.** 4·5회차 파일(`기획/루프기획/specs4.json`·`round4.body.html`·`round5.body.html`·`생성4/*`·`생성5/*`·`목업5/*`·`panels.py` 수정)과 이 문서는 **미커밋**.
- 07-3 커밋 필요. `run_tests`는 `--mode EditMode --timeout 150`(`--platform` 아님)으로 돌리면 정상(이번 세션 2회).
- 설계 08은 07-3 기준으로 고친다: `ZoneView`·`BranchView`는 이미 있음(P9의 개명 완료). 존은 9개(`zones.json` 9행, z01~z09 앞줄 왼쪽부터, 07-5), 잠금 표시는 존 위 반투명 회색 스프라이트 + 자물쇠(타일 색 안 건드림), `WorldManager.Zone`(z01 고정) → 존별 스폰, 관광객은 길 타일(2칸 폭, 둘레 + 존 사이) 위를 걷고 입구는 맵 앞 가운데. 탭은 `Iso.ToLogical(ScreenToWorldPoint)`로 존 사각형 판정.
- 배경 아트: 타일 3종(잔디·흙·길 64×32)·울타리 칸·기둥·자물쇠·시설 4종. 생성 단위를 정한 뒤 Codex로 뽑는다.
- **기획서 v0.18 반영 완료, 게임 루프 코드는 아직 v0.16 상태(뽑기 있음).** 설계 08(존·입주·번식 + 뽑기 삭제) 작성 → 확정 → 구현. 삭제 대상: `Core/Gacha` 5개, UI `Gacha*` 4개, 테스트 `Gacha*` 3개, `GachaButtonView`·`GachaResultView` 프리팹, `grades.json`, `game_config.gacha`, `strings`의 `gacha_*` 8개, `animals`의 grade·gachaWeight·variantId, `ZooState.PullCount/RecordPull`. 저장(10)은 존 배열 모델.
- 기능 07 커밋 완료(`65196dd`). 2026-09-15 기획 전환(기획서 v0.16, 데이터-테이블-규칙 v1.10): 우리는 **지점당 하나**, 성장은 **우리 꾸미기**. 우리 해금·격자·맵 확장·개방 전 우리 표시 폐기. 구현 완료·검증 완료(설계 07 3장).
- 설계 08(저장·로드·오프라인 수익 팝업) 작성 → 확정 → 구현. 세이브에 시설별 단계 포함, 저장 모델은 처음부터 지점 배열(기획서 v0.13). `GameManager.Init`의 1회 가드 안에서 로드.
- 구조 정리(06-1)·GameKit `UIManager.GetView` 커밋 완료(`d26702c`, GameKit `8224034`).
- **기능 06(관광객) 완료·커밋.** 기획서 v0.14, 데이터-테이블-규칙 v1.8(`visitors` 테이블 신설, `game_config` version 5). 관광객 수 = f(초당 수입, 로그), 길은 우리 앞 띠(`CageView.m_path` 타일 경계), 코인은 머리 위 팝업. 더미 실루엣 1종.
- **기획 확정(2026-09-14, 기획서 v0.13)**: 환생 없음, 성장 축 3개(마리·별·시설), 후반 공급 3종(마일스톤 배수·2호점·이벤트 우리), 공장식 전환 안 함. 06~10 뒤의 순서는 기획서 9장. 후보 상세와 레퍼런스 근거는 9.1. 08(홍보)은 시설 테이블의 첫 행이 되도록 설계한다.

- 새 종 추가 절차: `Resources/Sprites/Animals/<종>/`에 정지 1장 + idle·move 시트 → 에이전트가 CLI로 임포트(격자 슬라이스·PPU 300·피벗 하단) → `animals.json` 행 추가 → 기획서 6.1 표 갱신.

## 알아야 할 것

- **개발 절차(코드-규칙 v1.5)**: 설계 → 사용자 확정 → 한 번에 구현(Unity CLI) → **에이전트가 1회 실행해 검증**(컴파일 0·콘솔 0·EditMode 테스트·플레이 모드 캡처) → 짧게 보고. 테스트 방법 문서 없음, 문서화 최소.
- 기능을 완성할 때마다 `기획/포트폴리오.md` 4장에 한 절(기술 포인트·AI가 한 일·문제와 해결)과 5장 숫자를 갱신한다. 게임이 매출을 못 내면 포트폴리오로 쓴다.
- 에디터 열기: `unity projects open ProjectTycoon`(`project`가 아니라 `projects`). 연결 확인 `unity pipeline list`. Git Bash에서는 `export MSYS_NO_PATHCONV=1`.
- Unity CLI 요령:
  - **구현 시작 전 `editor_status`의 `playMode`를 확인한다.** 플레이 중이면 씬 저장·`OpenScene`이 실패한다(03에서 겪음). 사용자가 켠 플레이 모드면 끄기를 기다린다.
  - 검증 순서: `editor_play` → `console` 오류 0 → `capture_game_view --source screen` → `editor_stop` → `editor_status`로 `playMode=stopped` 확인 → `run_tests`.
  - 여러 줄 C#은 `unity cmd run_script --file <절대경로> --entry <Type.Method> --mode ephemeral`. 에디터 스크립트로 프리팹·스프라이트를 만들 때 이 방식이 가장 확실했다(02에서 프리팹 3개·PNG 8개 생성). 스크립트는 scratchpad에 두고 프로젝트에 남기지 않는다.
  - `console`의 인자는 `--tail N --level warn`.
  - **에디터 창이 뒤에 있으면 플레이 모드 프레임이 멈춘다**(`Time.frameCount` 고정, 코루틴·이동 안 돎). `editor_focus`로 앞에 가져오면 돈다. `set_autotick --enable true`는 소용없었다.
  - 플레이 중 버튼은 `run_script`로 `Button.onClick.Invoke()` 호출로 누를 수 있다. 명령 1회 지연이 1초 넘어 1초짜리 결과 카드는 캡처보다 상태 조회(`FindFirstObjectByType`)로 확인한다.
  - `run_tests`를 플레이 모드에서 돌리면 파이프라인이 영구히 막힌다(사람이 Unity 창에서 Stop 해야 풀림). 반드시 `editor_stop` 후 `playMode=stopped`를 확인하고 돌린다.
- **JSON 테이블을 고친 뒤 플레이하기 전에 강제 임포트**: 에디터가 포커스를 잃은 채로는 `Resources.Load` 텍스트가 옛 값이다. `eval`로 `AssetDatabase.ImportAsset(path, ForceUpdate); AssetDatabase.Refresh(ForceUpdate)`를 돌리고 `Resources.Load<TextAsset>(...).text`로 확인한 뒤 플레이한다(07에서 겪음).
- 세로 화면(size 6)에는 존 하나(화면 폭 7유닛)가 거의 꽉 찬다. 시설 자리는 존 안 네 모서리 ±2.2/±2.8(07-3).
- `capture_game_view`는 base64 PNG를 돌려준다. scratchpad의 `cap.py`처럼 파일로 풀어 `Read`로 본다.
- 플레이 중 상태를 보려면 `run_script`에서 `ZooTycoon.Game.GameManager.Instance.State/Gacha/ZooLevel/Facility`를 바로 읽는다(리플렉션 불필요). 레벨 판정은 틱에서만 하므로 `AddCoins` 직후가 아니라 다음 틱(1초) 뒤에 `Lv`가 오른다.
- 리소스는 JSON 경로 칼럼(`animals.sprite`)으로 참조. 더미가 들어 있고 사용자가 같은 경로로 실제 리소스를 덮어쓴다.
- **관광객(06)**: `VisitorSpawner`(씬 `World` 자식, `WorldManager.m_visitors`)가 `TargetCount`까지 1.5초 간격으로 스폰. 수는 `VisitorCalculator.Count(초당 수입)`(Core), `WorldManager`가 `IncomeService.IncomeChanged`마다 갱신. 시설 단계 변경도 `IncomeService`가 구독해 반영한다(07. 수입을 읽는 곳은 전부 `IncomePerSecond`). 관광객은 `CageView.PathBounds`(길 타일 경계) 안을 X로 가로지르고, 동물이 있으면(`CageView.AnimalCount > 0`) 우리 앞에서 멈춘다. 연출 숫자는 프리팹 인스펙터: `Visitor.m_entryMargin`(1.5)·`m_viewInset`(1), `CoinPopup.m_riseHeight`(0.8)·`m_seconds`(0.7). 관광객 종류·속도·구경 시간은 `visitors.json`.
- **에셋을 같은 경로에 다시 만들 때(`AssetDatabase.CreateAsset` 덮어쓰기) 에디터 메모리의 옛 객체가 깨진 채 남아 프리팹 참조가 "이름 없는 Tile"이 된다(흙 타일 3차에서 겪음, 바닥이 분홍 사각형).** 먼저 `DeleteAsset` → 새로 `CreateAsset` → 대상 에셋·프리팹 `ImportAsset(ForceUpdate)` → 도메인 리로드(`recompile`) 뒤 플레이. 확인은 플레이 중 `Tilemap.GetTile(...)`의 이름·sprite.texture 이름.
- 에디터 스크립트 요령: `GetComponent<T>() ?? AddComponent`는 Unity 가짜 null 때문에 실패한다. `TryGetComponent`를 쓸 것. 이미 열린 씬을 `OpenScene`으로 다시 열면 더티 상태에서 대화상자가 뜰 수 있으니 `SceneManager.GetActiveScene().path`를 먼저 비교한다.
- **UI 문구는 전부 `strings.json`**. 코드·프리팹에 한국어 리터럴 금지(예외 메시지 제외). 새 화면은 문구 키부터 추가.
- 의존 방향 `Game → UI → Core`, `Game → World → Core`, `Game → Data → Core`. UI와 World는 서로 모른다. 레코드·`GameTables`·`StringTable`은 Core, `TableValidator`만 Data.
- 스프라이트·등급 색 읽기는 UI `AnimalVisuals`(static). World는 `Resources.Load<Sprite>`를 직접 한 줄 호출. 등급 색은 `grades.colorHex`.
- **루프 기획 5회차(2026-09-17) — 게임 전체 컨셉**: 4회차에 대한 사용자 평 "창의적으로 바뀐 건 맞지만 울타리 안 동물의 현실적인 생활에만 초점. 시스템·리소스·뷰(3D 포함) 다 바꿔도 되고, 웜뱃 똥 디펜스처럼 현실성 없어도 재미있어 보이면 된다(이 예시에 매몰되지 말 것)" + 추가 지시 "레퍼런스를 잡는다면 2024년 이후 유행한 최신 게임으로". 원인은 4회차 브리프(동물원 필연성 채점 + 실제 동물원·생태학 재료). 브리프를 고쳐(`생성5/00-공통-브리프.md`: 뽑는 것 = 게임 전체, 고정 = 1인·Unity·모바일·3~6개월, 채점 = 한 문장 훅·30초 플레이·재미의 출처·10시간 뒤, '똥 + 디펜스' 제출 금지, 스킨갈이 금지, 레퍼런스 2024+) 생성 에이전트 5개 병렬(① 강제 충돌 ② 말 안 되는 전제 ③ 손 동사 ④ 웜뱃 한 장 제약 ⑤ 15초 영상 역설계, 각 12개 = 60개) + 조사 에이전트 1개(2024-01 이후 유행작 57종을 웹으로 확인 + 반복 패턴 10개, `생성5/06-레퍼런스-2024-이후.md`. 운빨존많겜·버섯커 키우기·카피바라 Go! 사실 확인) + 심사 에이전트 1개(`생성5/07-심사.md`: 뿌리 44 → 탈락 8 → 최종 10, 컨셉마다 2024+ 레퍼런스에서 가져올 것 하나·다른 점 하나, 더 세게, 첫 주 프로토타입 계속/중단 기준). 아티팩트 https://claude.ai/artifact/CxrjQSJjrHxC2omJMEHaBt — 10개: 1 선로를 먹는 기차(선로 8장뿐, 지나간 조각이 그대로 손패로 돌아옴 = 지나온 길의 모양이 미래의 선택지. 27점, 가장 빈틈없음. 레퍼런스 Block Blast!·Balatro) · 2 전원 눈 뜬 순간(야행성 반 졸업 사진, 깨우는 순서·부작용·셔터, 망한 사진이 상품. Is This Seat Taken?·Content Warning) · 3 손으로 돌리는 낮과 밤(3D 구 하나, 자원 = 방향, 배치가 나중의 딜레마. 직접 대응작 없음) · 4 손 그늘(손가락 그늘 = 밤, 덮으면 내 손에 가려 안 보임. 새로움 5, 기존 웜뱃·타일 그대로. The Exit 8·Mage Arena) · 5 하품 전염(시선 부채꼴로 번지는 하품, 손길 3번, 쓰러짐이 경로를 바꿈. Arrows) · 6 점수판 인건비(7세그먼트 막대 하나 = 누운 웜뱃 한 마리, 점수의 모양이 비용. 새 아트 0, 27점이나 재미 미검증이라 6위. Balatro·Buckshot Roulette) · 7 굴 파는 컬링(멈춘 자리가 영구 포탈. 지금 웜뱃·쿼터뷰 타일·울타리를 그대로 쓰는 유일하게 강한 안. Ball x Pit·카피바라 Go!) · 8 안쪽 자리(엘리베이터 4×4 후입선출 실시간 배치. Color Block Jam·Screwdom 3D) · 9 찌르레기 떼(손가락 속도 = 떼의 모양, 리본은 먹고 공은 숨는다. Squad Busters 반면교사) · 10 웜뱃 벽돌공(자는 웜뱃 = 시한부 벽돌, 하중이 잠을 늦춤, 격자 낙하. R.E.P.O.). 10위 밖 눈에 띄는 것: 꺼지는 땅(3D 2순위)·엉덩이 문지기(손맛 최고, 깊이 2 + 예시 인접)·빠른 층 느린 층·본 배만 돈다·웜뱃을 붓는다·뭘 버릴까·땅이 불룩·모래시계 마을. 심사 총평: 사육 시뮬 회귀는 60개 중 1개로 해소, 대신 **브리프에 넣은 웜뱃 사실 8줄이 새 닻**이 됨(자는 벽돌 5/5, 엉덩이로 굴 막기 6개 중 4개는 사용자 예시의 그림자 → 최종 10에서 제외). 다음에 뽑을 땐 사실 목록을 빼거나 생성자마다 재료를 다르게. 내 읽기: 좋은 것은 웜뱃 밖에서 나왔고(1·3·5·8·9위), 어느 것을 고르든 타이쿤 코드(존·관광객·수입 틱·시설)는 대부분 안 쓰게 됨 — 남는 것은 GameKit·asmdef·JSON 테이블·아트 파이프라인·웜뱃·타일. 목업 10장은 Codex 이미지 생성(웜뱃 참조, 프롬프트 영어, 5개씩 병렬 백그라운드, 약 2분/묶음, 10/10 성공) → 폭 540 JPEG로 `기획/루프기획/목업5/g01~10.jpg`. 본문 `round5.body.html`, 조립 scratchpad `assemble5.py`(`.shot` 2단 레이아웃 추가, 장면 합성기 미사용).
- **루프 기획 4회차(2026-09-17)**: 사용자 "기존 레퍼런스 참고에서 끝나지 않는 창의적 아이디어를 원할 때 어떤 프롬프트?" → 기법 5개 제시(뻔한 답 금지 목록 / 장르 밖 메커니즘 이식 / 전제 뒤집기 / 말하고 싶어지는 순간에서 역설계 / 생성과 분리된 심사) → "4회차 돌려봐". **조사 없이** 생성 에이전트 4개 병렬(기법 하나씩, 웹·게임 레퍼런스 인용 금지, 1~3회차 30개 + 장르 클리셰 전부 금지, 출력은 기능명이 아니라 규칙·새 고민·구조적 차이·동물원 필연성·방치 연결·새로 필요) → 48개 → 심사 에이전트 1개(기시감 3↑ / 고민 2↓ / 방치 2↓ 탈락, 수렴은 가산점 아님) → 뿌리 30 → 생존 18 → 최종 10. 원문 `기획/루프기획/생성4/00~05.md`. 아티팩트 https://claude.ai/artifact/Az5AsGAmxgaF5sbtibLqA8 — 10개(자산으로 바꾸는 것): 1 한배 줄(건드리지 않은 시간. 같이 산 시간이 출산 주기를 맞춤, 줄 n마리 = n², 전입·전출이 동기화 절반. 48개 중 유일한 기시감 1) · 2 전 세입자의 흔적(거주 이력. 마리×시간으로 굴·웅덩이, 자기 흔적은 자기에게 무효, 생성자 셋이 독립 수렴) · 3 머리 위 통로(길의 통행량. 웜뱃 유리 터널, 두 존 한 무리 정원 14, 건너는 8초 길 위 전원 지불) · 4 남는 수컷이 구경거리(어긋난 구성. 하렘/짝/단독형, 힘겨루기 ×2.5 대신 번식 타이머 정지, 총각 우리) · 5 눈 돌린 사이(카메라. 보이는 존 수입 ×1.5 / 안 보이는 존 번식 ×2, 비용 낮음. 과장판: 끌 때 카메라 자리가 밤새 '보이는 존') · 6 풀이 돌아오는 존(비워 둔 땅. 흙→풀→덤불→숲 8/24/72h, 버팀 한도 6/4/2) · 7 제 발로 이사(동물의 이의 제기. 불만 → 울타리 넘기, 받아들이면 번식 +50%) · 8 대장 따라 산다(서열·성격. 무리는 대장 3칸 안, 대장은 성격대로 자리) · 9 저장된 개장 줄(부재 → 수요. 1분 1명 상한 480, 개장 전 편집, 3배) · 10 울타리 너머 시선(종 관계 → 서 있는 자리, 양면 길. 3종+). 예비: 옷이 주인을 찾아간다(의상 09에 편입 1순위)·울타리 트기·해설판 한 줄·터줏대감, '관광객 목적지 이동' 부품이 선행돼야 하는 셋(별명 붙은 놈·발을 묶는 인기·피딩 다이어그램). 내 읽기: 열 개는 3회차(손)와 경쟁하지 않고 그 밑에 깔리는 '세계의 규칙'이고, 관통 문장 = **"동물원의 상품은 동물이 아니라 '동물이 보이는 것'이고, 보일지 말지는 동물이 정한다"**(기획서 2장 정체성 후보). 묶음 A 개체 레코드(1+4+8) / B 존 밖을 걷는 동물(3+7) / C 존 바닥 상태(2+6, 반드시 한 시스템) / 5는 어디에나. **가장 싼 첫걸음 1+4+5(웜뱃 1종, 새 UI 0)**. 같이 넣으면 망치는 쌍: 1×7, 2×6 별개 시스템, 8×10 우선순위 없이. 진짜 가격 = 개체 단위 세이브(06-1에서 일부러 `Count`로 정리한 것을 뒤집음) + 개체·무리 집기 드래그. `panels.py`에 4회차 오버레이 추가(타일 grass1~3·trail·roof·glass·lit, 모자 관광객 v_r/v_y/v_b, `dashes` 논리 좌표 점선, `zonetint` 존 바닥 틴트(배우 아래), `rain`; 3회차 출력 바이트 동일 확인). 스펙 `specs4.json`, 본문 `round4.body.html`, 조립은 scratchpad `assemble4.py`(3회차와 같은 뼈대, `.grp-head` 없음). Git Bash heredoc으로 한글 경로가 든 파이썬을 쓰면 깨진다 → Write 도구로 파일을 만든다.
- **루프 기획 3회차(2026-09-17)**: 사용자 "타이쿤 안에서 소거·합성만 하는 것 같다. 다른 장르로 전환하거나 다른 장르 요소를 가져와도 됨(방치형, 곡괭이 왕 아일랜드처럼 직접 가꾸는 장르). 시장조사 먼저 vs 바로 10개?" → 질문 하나("손이 10초마다 무엇을 하고 방치·오프라인과 어떻게 이어지는가")로 짧은 표적 조사 뒤 장르 앵커별 10개로 합의, "총잡이 고양이 같은 장르도 조사" 추가. 조사 에이전트 4개 병렬(코지 샌드박스 손동작 / 곡괭이 왕·한국 힐링 / 타이쿤 밖 루프 / 방치형 키우기 RPG·총잡이 고양이), 보고 원문 `기획/루프기획/조사3/01~04.md`(웹검색 한도 소진으로 스토어·리뷰 피드·위키 직접 열람 위주, 추정은 표시). 핵심 7규칙: ① 손은 자동화 안 된 일, 진행 = 손을 봇·시설로 산다, 산 뒤에도 손 프리미엄 2배(Rusty's·Forager·Slime Rancher) ② 돌아오면 더미가 있어야 한다(곡괭이 왕은 오프라인 무성장 → "자석 당겨보고 현타" 이탈 → 14개월 뒤 주민 추가) ③ 접속 첫 30초 폭발(총잡이 고양이 이탈 리뷰 "업그레이드가 와르르 있어야") ④ 에너지·낮밤·부재 페널티 없음(Sun Haven "스태미나 없음"이 셀링 포인트) ⑤ 탭 대상 → 자동 이동·도구, 가상 패드 금지, 드래그는 익은 것 위에서 시작할 때만 수거 ⑥ 성장은 손동작 자체를 크게·빠르게 ⑦ 잡일화 = 재화 요구 급증, 강제 광고 없음(한국 힐링 최다 칭찬). 아티팩트 https://claude.ai/artifact/RpBAMEQ2GhN6yyNJ9nHChP — 10개: A 장르 전환 1 곡괭이 사육사(캐릭터 탭-이동·삽 하나·개간으로 존 해금, 높음) · 2 개척 퍼레이드(행렬이 숲을 치우며 자동 전진 + 3버튼 연타, 환생은 행렬만·동물원 유지, 중~높음) / B 신의 손 3 먹이 던지기와 쓸어담기(탭 던지기 → 드롭 → 드래그 수거, 급식기·수거 시설 사다리, 중간) · 4 책상 위 동물원(켜 두는 화면·포커스 모드·관찰 일지·절전, 낮~중) · 5 하루 세 번의 손(존당 타이밍·연타·드래그 3번, 시간제 버프, 낮음) / C 타이쿤 밖 6 길 옆 카드(Loop Hero 3택1 길 옆 배치·인접 콤보·하루 마감, 중간) · 7 숲의 실루엣(조건 → 실루엣 → 방문 → 정주 = 가차 없는 획득, 부재중 방문 일지, 중간) · 8 서식지 타일(존 안 타일 배치 퍼즐·즉시 점수·★ 배율·방사, 낮~중) / D 9 강화 연타 동물원(대기줄 = 벽, 3축 연타, 같은 것 3개 합성 = 우리 장비, 환생·전투 없음, 중간) / E 10 소원이 손을 부른다(소원 = 동사, 요청자 앞에서 손으로 수행, 봇은 즉시형만, 중간). 내 읽기: 결정 순서 Q1 손의 형태(캐릭터 1·2 vs 신의 손 3~10; 세로 한 손·마름모 지형이면 신의 손 유리, 캐릭터는 프로토타입 없이 답 못 함) → Q2 더미 형태(드롭·카드·타일·소원 중 하나) → Q3 획득 경로(가차 vs 7 조건형 vs 1 개간) → Q4 숫자 게임 허용(2·9). **최소 정합 조합 3+10(+5 타이밍)**, 중기 7(가차 대체)·8(존의 얼굴), 4의 포커스 모드·일지는 어디에나 공짜. 장면 합성기 `기획/루프기획/panels.py`에 손(탭·드래그)·드롭·타일(물·바위·대나무·진흙·굴)·버튼·미니카드·타이밍 바·호·bars·labels 오버레이와 배우 tree/tree_s/bush/sil/kb/stump/trough 추가(1·2회차 스펙 출력 바이트 동일 확인). 스펙 `specs3.json`, 본문 `round3.body.html`, 조립은 scratchpad `assemble3.py`(뼈대 `round1.template.html` + 추가 CSS `.four/.grp-head`).
- **루프 기획 2회차(2026-09-16)**: 사용자 판정 — 4(하루)·7(머지)·9(계절)·10(사진)은 메인 후보 제외(미니 콘텐츠 후보로 보류), 끌리는 것 2(소원 게시판). 남은 1·2·3·5·6·8을 조합·심화한 10개 + 소원 게시판 설계 초안(종류 즉시/시간/조건/특별, 생성 비율 2:2:1, 슬롯 3~5, 벌칙 없음, 단골 → 후원자 사다리). 아티팩트 https://claude.ai/artifact/6WTPQFNpZJ2EtS5gdYw1hm . 내 읽기: 열 개는 한 게임의 층(1 경제 → 2·9 목표 → 5·7 자동화·애착 → 3·4·6·10 중기 → 8 사다리), 배타는 탐험 계열(3·6·10) 중 하나·구조 계열(4·10) 중 하나. **최소 출시 조합 제안 1+2+9**(소원 + 혈통 + 시설★). 3회차 주제 후보: 선택 조합의 D1 첫 15분 장면 10개 / 소원 템플릿 30개 / 탐험·원정·구조 계열 심화 / 미니 콘텐츠 결합. 장면 스펙 `기획/루프기획/specs2.json`, 본문 `round2.body.html`, 뼈대 `round1.template.html`.
- **루프 기획 1회차(2026-09-16)**: 사용자 방식 = "조사를 바탕으로 기획을 10개씩 찍어낸다. 현재 에셋으로 루프를 시각화(없으면 더미), 아티팩트, 레퍼런스 대비 차별점, 충분히 고민". 1회차 아티팩트 https://claude.ai/artifact/8ruu5inx3UcvPkGg6Cdde6 — 10개: 혈통 도감(번식=수집, 변이·pity·방생) / 소원 게시판(관광객이 목표를 줌) / 사육사 덱(사람이 카드) / 동물원의 하루(아침·낮·밤 실시간) / 탐험대(이야기 있는 종 획득) / 구조 보호소(돌봄·방사·명성) / 머지 우리(슬롯 머지) / 후원자 편지(관광객 서사·후원금) / 사계절 달력(28일 주기) / 사진 의뢰(보는 재미가 루프). 각 4장면(장면 합성기 `기획/루프기획/panels.py` + `specs.json`, 현재 에셋 + 더미) + 3초/3분/3일 + 차별점 + 비용 + 비교표. 내 읽기: 1·4·9는 골격 그대로 공존 가능, 2·8·10은 하나만, 3·5·6·7은 방향 전환. **다음**: 사용자가 지울 것·끌리는 것·2회차 주제(㉠ 우두머리·공연 ㉡ 지점 교환 ㉢ 동선 퍼즐 ㉣ 밤의 동물원 ㉤ 후원 광고 세계관 ㉥ 1·4·9 결합 상세) 고르면 2회차 10개.
- **플레이 루프 기획 조사(2026-09-16)**: 07-4·07-5 커밋(`6b30668`) 뒤 사용자 요청 "디자인 틀이 잡혔으니 기본 플레이 루프를 제대로 정하자. 검증된 게임·도파민 요소·플레이타임 요소를 최대한 많이 조사해 아티팩트로". 조사 에이전트 5개 병렬(레퍼런스 루프 / 도파민 장치 / 리텐션 장치 / 한국 시장 / 동물원 장치) → 한 장으로 압축: https://claude.ai/artifact/9ZUFbSKqpgvYf3e6QeU3pY . 핵심 결론 7개(세 겹 루프 3초/3분/3일, 리셋형 프레스티지 금지 = 지점 확장이 대체, 복귀 첫 30초 = 복귀 선물, 번식에 변이·pity·방생, 동물이 보이게 살아 있음, 광고는 세계관 안 옵트인, 1인 개발 리스크는 케이던스). 우리 후보 15개(효과·비용·시점) + 넣지 않을 것 5개 + 논의 질문 Q30~Q35. **다음 세션**: 사용자와 Q30~Q35를 하나씩 논의해 기획서 2·3·9장을 v0.23으로 갱신, 08 설계에 반영(마일스톤 배수·변이·방생·만족도★·복귀 선물).
- **기능 07-5(화면에 맞춘 1호점) 구현(2026-09-16, 커밋 `6b30668`)**: 사용자 결정 "가로 5 → 3, 위에 한 줄 추가, 나무 더 채움, 카메라 제한" → 존 **X 3열 × Z 3줄 = 9개**(원점 (2+8c, 2+10r), 맵 26×32), 숲 띠 12칸(1330개), **카메라 마름모 클램프**(`WorldCameraController.SetBounds`가 화면 경계 사각형의 내접 마름모를 한계로, 초점 허용 = 반폭 A−hw−(A/B)·hh, 밖이면 중심 쪽 비례 축소. `m_panMargin`·`m_bounds` 삭제). `BranchView.m_mapArea` = (−12,−12,50,56). scratchpad `BuildBranch2.cs`가 `Branch01.prefab` 내용을 통째로 교체(GUID 유지, 변형 타일 색인 포함) — 앞으로 지점을 다시 굽을 때 이 스크립트 기준. 실측 초점 범위 x −12.35~9.36, y 1.76~12.76. 설계 07-5. 08의 `zones.json`은 **9행**(z01~z09, 앞줄 왼쪽부터). 초점 테스트는 리플렉션으로 `m_focus` 설정 + `ApplyFocus` 호출(scratchpad `Focus.cs`).
- 에디터 콘솔에 `TreeViewController… NullReferenceException`·`GUI Error: GUIClips`가 쌓이는데 Hierarchy 창이 1000개 넘는 자식을 그리다 내는 에디터 내부 오류다. 게임 오류 판정은 `error CS`·게임 스택만 본다.
- **1호점 맵을 화면 모양에 맞추는 논의 중(2026-09-16)**: 사용자 "카메라 이동을 제한하고 나무 띠가 화면의 끝이었으면. 1호점 맵 디자인 변경". 지금 맵(18×52 마름모)은 세로 화면과 모양이 안 맞아 마름모 모서리에서 항상 밖이 보인다. 제안 B: **세로 띠 맵** — 존 k를 (8k, 8k)(논리 대각선 = 화면 세로)에 한 줄 하나씩, 길은 존 오른쪽 대각선 복도(x−z = 5~7) + 존 사이 가로 복도, 나무는 띠 양옆(화면 좌우 끝) + 위아래 끝, 카메라 x 고정·y만 클램프. 줌·존 크기 표(5×7 + size 8 권장, 웜뱃 65px). 대안 A: 지금 맵 + 숲 채우기 + 화면 사각형 클램프. 아티팩트 (아티팩트 삭제됨 2026-09-17) . 결정 대기 Q26(방향)·Q27(존 크기)·Q28(size)·Q29(길 위치). 07-4 결과(나무 띠·무한 잔디)는 "나머지는 좋다"로 확인됨, 미커밋.
- **기능 07-4(맵 가장자리: 무한 잔디 + 자연 경계) 구현(2026-09-16, 미커밋, 사용자 피드백 대기)**: 사용자 결정 = 사방 나무·덤불 랜덤, 입구 틈 앞 가운데 2칸, 그림자 없음, 띠 깊이 2칸. `Branch01.prefab`에 `Backdrop`(잔디 화면 주기 `Tiles/grass_period.png` 192×96, Tiled, order −2100, 왼쪽 아래 (3i, −1.25+1.5j) 정렬)과 `Border`(293개: `Sprites/World/Border/{tree_big 88×84, tree_small 46×56, bush 42×22}.png`, Codex → `Source~/make_sprite.py`(흰 배경 투명·팔레트·목표 높이 최근접 축소), 피벗 하단 중앙, PPU 64, 시드 20260916, 1칸 격자 ±0.35 흔들림, 큰 35/작은 35/덤불 30%, flipX 랜덤) 구움. `BranchView.m_mapArea` → (−3,−3,24,58)(카메라가 띠 바깥 0.5칸까지). 카메라 배경색 (110,173,86). 검증: 앞·z01·오른쪽·뒤 4곳 1080×1920 렌더에서 단색 0px, 사방에 나무·덤불, EditMode 98/98. **남은 관찰**: 잔디 텍스처의 꽃 3송이가 3칸 주기로 격자처럼 반복돼 넓은 잔디에서 눈에 띈다(잔디 재생성 때 꽃 제거 또는 주기 6칸 검토). 원본 생성물은 `Border/Source~/`. scratchpad `BakeBorder.cs`는 마지막 return에서 언로드된 객체를 읽어 Runtime Error가 났지만 저장은 모두 끝난 뒤였다.
- **맵 가장자리 논의 중(2026-09-16)**: 잔디·회색 길·경계선 커밋(`dc641ca`). 사용자 스크린샷: 카메라를 앞으로 끌면 잔디 타일(맵 밖 6칸)이 끝나고 카메라 배경색(102,158,77, 잔디 기본색 110,173,86과 다름)이 단색으로 드러남. 원인: 초점 범위 = 맵 화면 경계 ±2, 화면 반높이 6 → 맵 아래 8유닛까지 보이는데 잔디는 3유닛. 레퍼런스 5패턴(무한 바닥+클램프 / 자연 경계 / 섬 / 확장 예정지 / 세계-물체형) 정리 아티팩트 (아티팩트 삭제됨 2026-09-17) . 제안 A(지금): 잔디 화면 주기 텍스처 192×96(`Tiles/Source~/grass_period.png`, 시트에서 복원)을 Tiled 배경 스프라이트로 프리팹에 깔고 격자 정렬(주기 이미지 (u,v) ↔ 월드 px x∈[u,u+1), y∈[15−v,16−v) → 스프라이트 왼쪽 아래를 (0, −1.25)유닛에 두고 3×1.5유닛 주기) + 카메라 배경색을 잔디 기본색으로. B(2호점 전): 맵 둘레 산울타리·나무 띠. 결정 대기 Q22·Q23.
- **길 타일 실제 아트 2차(2026-09-16, 사용자 피드백 대기)**: 사용자 결정 "복도 폭 2칸 유지, 2칸을 꽉 채우는 방식, 돌길이 아니라 일반 회색 길". 1차 판석은 폐기. Codex 밝은 회색 포장(`Tiles/Source~/path_topdown.png`, 두 톤 알갱이 + 짧은 줄, 흙 타일과 같은 결, 팔레트 5색) → `make_tiles.py`(팔레트 4색) → `Tiles/Path_00~17` → `Branch01.prefab` Path 936칸 다시 칠함. 경계선 조각(존 둘레·맵 둘레)은 그대로. 돌길 구조 정리 아티팩트((아티팩트 삭제됨 2026-09-17) , 복도보다 좁은 돌길·폭 1/2/3 비교)는 보류 — 나중에 좁은 길로 바꾸고 싶을 때 참고. 오해 기준 "돌길 바닥 시안" 아티팩트는 삭제함.
- **에셋 작업 전에 `editor_status`의 `playMode`를 확인**: 사용자가 플레이 중이면 프리팹 저장·타일 교체 뒤에도 실행 중 인스턴스는 옛 참조(null 타일)로 남아 캡처가 틀리다. `editor_stop` 후 다시 플레이해서 확인한다(길 타일 1차에서 겪음).
- **지형 경계선 구현(2026-09-16, 안 A, 미커밋)**: `Sprites/World/edge.png`(32×16, 한 칸 변의 2:1 계단 1px 진갈색 52,32,32, 피벗 왼쪽 아래 = 변 시작점, PPU 64). `Zone.prefab`에 존 둘레 28조각(`Edge*`, order −950), `Branch01.prefab`의 `MapEdge` 아래 맵 둘레 140조각(+X 변은 그대로, +Z 변은 flipX). scratchpad `BakeEdges.cs`가 구움. 타일 PNG는 손대지 않는다. 사용자: "별도 리소스(시설 등)는 뽑을 때 웜뱃처럼 외곽선을 넣는다".
- 지형 경계선 논의(2026-09-16): 사용자 피드백 "잔디·흙 끝에 검은 선이 없어 웜뱃과 안 어울린다. 같은 지형끼리는 선 없음, 잔디↔길·흙↔길 경계에만 선". 시안(캡처 위 기하로 1px 진갈색)을 1호점 맵 아티팩트 0장에 넣음 (아티팩트 삭제됨 2026-09-17) . 제안 A(프리팹에 외곽선 스프라이트 2종: 존 둘레·맵 둘레) vs B(경계 타일 층). 결정 대기 Q11~Q13. **캡처 요령**: 정확한 화면 좌표가 필요하면 `capture_game_view --source camera --width 1080 --height 1920`(카메라 위치 + size 6 → 160px/유닛). `--source screen`은 에디터 게임 뷰 크기라 배율이 다르다.
- **잔디 타일 실제 아트 1차(2026-09-16, 미커밋, 사용자 피드백 대기)**: 흙과 같은 파이프라인. Codex 잔디 텍스처(원본 `Tiles/Source~/grass_topdown.png`, 채도 낮은 녹색 + 두 톤 알갱이 + 풀잎 획 + 꽃 3송이) → `make_dirt.py`(출력 파일명은 `dirt.png`라 복사 후 `grass.png`로) → `Tiles/Grass_00~17` → `Branch01.prefab` Ground 1920칸 다시 칠함. 시트 슬라이스 이름 파싱은 밑줄 뒤 번호(`grass_00`도 되게 고침). 울타리 2차·프리팹 굽기·지점 런타임 생성·`WorldManager` 통합은 커밋(`820c82a`).
- **`WorldManager` 싱글턴으로 통합(2026-09-16, 사용자 결정)**: "MVP는 UI에만, 나머지 MVP 폐지". `WorldView`+`WorldPresenter` → `WorldManager : MonoSingleton<WorldManager>`(World, 씬 `World` 오브젝트에 부착, `Initialize(state, income, tables)`를 `MainScene.Awake`가 호출, `OnDestroy`에서 구독 해제). `VisitorSpawner`는 `WorldManager.Instance`를 쓴다(직렬화 참조 삭제). World asmdef에 `GameKit.Unity` 추가. 코드-규칙 v1.8(싱글턴 예외 3번째, `...Manager` 접미사 허용). 설계 08의 `WorldView`/`WorldPresenter` 항목은 전부 `WorldManager`로 읽는다.
- **지점 프리팹은 실행 중 생성(2026-09-16, 사용자 지시)**: 씬의 `Branch01` 인스턴스 삭제. `WorldManager.m_branchPrefab`(인스펙터) → `Branch` 프로퍼티가 처음 쓸 때 `Instantiate`(`Initialize`에서 처음 쓰이므로 지연 생성). **실행 중이 아닐 때 씬 `World` 아래는 비어 있어야 한다.** 씬 편집 스크립트가 월드 인스턴스를 씬에 저장하지 않게 할 것.
- 월드 코드 구조 정리 아티팩트(2026-09-16): (아티팩트 삭제됨 2026-09-17) (계층·클래스·흐름·좌표 규칙·아트 파이프라인·08에서 바뀔 자리)
- **월드는 순수 2D + Tilemap(07-2·07-3)**. 논리 좌표 XZ(유닛 = 타일)는 `Unit.Logical`·`ZoneView`·`BranchView`의 Rect에만 있고, 화면 XY는 `Iso.ToScreen`(+X 오른쪽 위, +Z 왼쪽 위, k 0.5/0.25)으로만 만든다. 타일맵 셀 (x, z) = 논리 (x, z). 카메라는 직교 size 6, 회전 없음, z −10. 새 월드 오브젝트를 놓을 때는 반드시 `Iso.ToScreen(논리)`로. 존 6×8 타일, 1호점 18×52. 타일맵은 `Branch01.prefab`에 미리 칠해져 있고 존 위치는 그 안 `ZoneView.m_center`(고치면 `Floor` 타일도 같이). 씬에는 인스턴스가 없고 `WorldView`가 실행 중 생성한다. 새 지점은 `BuildBranch.cs` 같은 에디터 스크립트로 한 번에 만든다(scratchpad에 두고 프로젝트에 남기지 않음). 동물 스프라이트는 PPU 128·피벗 하단 중앙(실제 리소스로 교체할 때도 같은 설정), 런타임에 카메라 기울기로 세운 카드.
- **깊이 정렬은 화면 y**(`Assets/Settings/Renderer2D.asset` Custom Axis (0,1,0), 07-2). 정렬점은 Pivot(발끝·기둥 밑). 울타리는 긴 한 장이 아니라 칸 단위라야 y 정렬이 맞는다. 칸은 타일 모서리(정수 좌표)마다 두고 가로대는 다음 기둥까지 정확히 1칸(32px). 타일맵은 Chunk 모드라 서 있는 것을 타일로 넣지 않는다. 동물·관광객 카드와 난간의 Sprite Sort Point는 **Pivot**(발끝·밑동). 06에서 카메라 거리 정렬이 옆으로 떨어진 관광객을 난간 뒤로 보내는 버그(난간 정렬점이 가운데 하나뿐이라 x가 멀수록 거리가 늘어남)를 발견해 바꿨다. 땅 −1000·길 −1500·그림자 −900·잔디 −2000만 고정 order. 수동 y 정렬·축소 코드 없음. **렌더러 .asset을 텍스트로 고쳤을 때는 `AssetDatabase.ImportAsset(path, ForceUpdate)`로 강제 재임포트해야 메모리에 반영된다**(Refresh만으로는 안 됐다).
- 동물 연출값은 **전부 `animals.json`**(시트·frameRate·scale·moveSpeed·idleSeconds). 프리팹 `Animal`에는 숫자가 없다. 그 외 월드 연출값은 인스펙터: 우리 이동 영역(`CageView.m_walkArea`, XZ), 카메라 기울기·거리·시작 초점(Main Camera `WorldCameraController`), 팬 여유(`World`의 `m_panMargin`).
- 웜뱃 시트는 사용자가 프레임 간격을 띄운 픽셀아트로 교체(`Wombat.png`, `Wombat_Idle.png`, `Wombat_Move.png`). 프레임 간격이 균등하지 않아 격자 대신 **투명 열로 나뉜 섬 단위 슬라이스**(폭 통일·중심 정렬)로 임포트. 새 시트가 오면 같은 스크립트 방식(데이터-테이블-규칙 5장)으로 다시 자른다. `Wombat_Move`는 6프레임 픽셀 시트의 **앞 4프레임만 스프라이트로 만들어 1→2→3→4→1 반복**(사용자 요청, 5·6번 섬은 건너뜀). 몸 높이 260px vs Idle 264px라 PPU 295로 정규화. PNG는 그대로이므로 다시 자를 때 이 예외를 유지할 것.
- **개체 프리팹 계층(동물·관광객 동일)**: `Animal`/`Visitor`(루트, `Unit` 상속, 땅 위 XZ 위치, 회전·크기 없음) → `ModelRoot`(`localScale` = 레코드 `scale`) → `Sprite`(SpriteRenderer, 빌보드 55°) + `Shadow`(눕힘, y 0.02). 상태는 `WalkState`·`WaitState` 객체(설계 06-1). 시트 로드는 `WorldManager.Frames`(`FrameCache`, 경로당 1회, 번호 정렬). 시트 피벗 y는 발끝 행 자동 계산(데이터-테이블-규칙 5장). 발이 그림자 위로 뜨면 피벗을 의심할 것.
- 플레이 모드 없이 카메라 프레이밍을 보려면: eval로 카메라 위치를 `초점 − forward × 거리`로 놓고 `capture_game_view --source camera --width 540 --height 960`.
- **시설(07)**: `facilities.json`(자리·해금 레벨·단계·배수·비용). 총 배수 = Π(1 + 단계당 배수 × 단계), `IncomeCalculator.FacilityMultiplier`. 상태는 `ZooState.GetFacilityStage/UpgradeFacility` + `FacilitiesChanged`, 규칙은 `FacilityService`(Core). 팝업 행은 `Prefabs/UI/FacilityRowView.prefab`을 `FacilityPopupView.AddRow`가 복제. 월드 `Facility.prefab`은 우리 자식으로 `우리 위치 + (slotX, 0, slotZ)`, 1단계에 한 번 생성. 시설을 늘리려면 행 추가 + `Resources/Sprites/Facilities/<이름>/<이름>.png`(PPU 128, 피벗 하단 중앙).

## 더미 리소스 (사용자가 교체할 것)

| 경로 | 용도 | 현재 |
|---|---|---|
| `Assets/Fonts/NotoSansKR-VF.ttf` · `NotoSansKR SDF.asset` | 한글 UI 폰트(TMP 기본 폰트) | Windows Noto Sans KR 가변 폰트(SIL OFL). 폰트를 바꾸면 SDF 에셋을 다시 만들고 프리팹 4개의 TMP에 다시 물린다 |
| `Assets/Resources/Sprites/Animals/Wombat/` | 웜뱃 실제 아트(정지 1장 + idle·move 6프레임 시트) | 더미 아님. 더미 8종은 05에서 삭제했고, 새 종은 같은 구조의 폴더로 추가 |
| `Assets/Sprites/World/Tiles/{grass,dirt,path}.png` + `Assets/Tiles/*.asset` | 바닥 타일 3종(64×32 마름모, PPU 64, 피벗 중앙) | **더미**(Pillow 생성). 실제 아트는 같은 경로로 교체, 임포트 규칙은 데이터-테이블-규칙 5장 |
| `Assets/Sprites/World/fence.png` · `fence_post.png` | 울타리 한 칸(36×48, 기둥 + 1:2 가로대 32px) · 기둥만(4×32) | **더미**. `ground.png`·`path.png`·`fence_rail.png`(사용자 실제 난간)는 참조 없음, 파일만 남음 |
| `Assets/Sprites/World/shadow.png` | 발밑 그림자 | 단색 생성물 |
| `Assets/Sprites/World/fence_rail.png`(참조 없음) | 07-1까지의 난간 | **실제 아트**(2026-09-14). 사용자 원본(1254², 투명 여백)을 알파 경계로 잘라 923×571로 교체, PPU 961(타일 0.96×0.594유닛). 가로대 오른쪽 끝의 아웃라인 캡은 지우고 안쪽 색을 타일 끝까지 이어 그려(스크립트) 다음 기둥에 바로 붙게 함. 교체 시 같은 방식: 여백 없이 "기둥 + 가로대" 한 칸만 담고 가로대는 오른쪽 끝까지 닿게, PPU = 폭 ÷ 0.96, 난간 size y = 높이 ÷ PPU |
| `Assets/Sprites/World/coin.png` | 코인 팝업 스프라이트(`CoinPopup.prefab`) | 노란 원 64²(PPU 128) |
| `Assets/Resources/Sprites/Visitors/Visitor/Visitor.png` · `Visitor_Move.png` | 관광객 실루엣(정지 1장 + 걷기 2프레임 시트). `visitors.json` 경로 | **더미**: 남색 실루엣 240×360(PPU 300, 피벗 하단). 실제 아트가 오면 동물과 같은 임포트 규칙(데이터-테이블-규칙 5장)으로 다시 자르고, 종류를 늘리면 행 추가 |
| `Assets/Prefabs/Visitor.prefab` · `CoinPopup.prefab` | 관광객 개체(`Animal`과 같은 계층) · 코인 팝업 | 숫자는 인스펙터(위 "알아야 할 것") |
| `Assets/Prefabs/Animal.prefab` | 동물 개체(루트 `Animal : Unit` + `SpriteAnimator` → `ModelRoot` → `Sprite` 카드 + `Shadow`) | 스프라이트·숫자는 런타임에 레코드에서 주입 |
| `Assets/Resources/UI/TopBarView.prefab` | 상단 바 | 단색 + 텍스트 4개 |
| `Assets/Resources/UI/GachaButtonView.prefab` | 가차 버튼 + 확률 줄 + 남은 시간(`WaitText`, 버튼 안 비용 아래) | 주황 단색 버튼 720×240 |
| `Assets/Resources/UI/GachaResultView.prefab` | 결과 카드 | 검정 α0.5 차단막 + 카드 640×720. 표시 시간 `m_displaySeconds` 1초(인스펙터) |
| `Assets/Resources/UI/FacilityButtonView.prefab` · `FacilityPopupView.prefab` · `Assets/Prefabs/UI/FacilityRowView.prefab` | 꾸미기 버튼(가차 버튼 위 오른쪽 240×120) · 시설 팝업(차단막 + 카드 900×1100) · 행 840×160 | 단색 |
| `Assets/Resources/Sprites/Facilities/{Sign,Bench,Shop,Tree}/*.png` | 시설 아트 4장(`facilities.sprite`) | **더미** 도형(PPU 128, 피벗 하단 중앙). 실제 아트는 같은 경로로 교체 |
| `Assets/Prefabs/Facility.prefab` | 시설 카드(`Sprite` 빌보드 + `Shadow`) | 스프라이트는 런타임에 레코드에서 |

## 알려진 문제

- **빌드 타깃이 Standalone이다.** 기술 결정은 Android IL2CPP ARM64인데 아직 안 바꿨다. 제품명·회사명·패키지 ID도 임시(DefaultCompany).
- 폰트가 가변 폰트라 기본 굵기가 얇다. 텍스트는 전부 Bold.
- 씬의 `Global Light 2D`는 원근 카메라에서도 스프라이트를 비추지만, 2D 라이트가 필요 없어지면 URP 2D Renderer → Universal Renderer 전환을 검토한다.
- 시설은 1단계에만 월드에 나타나고 단계가 올라도 모습이 안 바뀐다. 실제 아트가 오면 단계별 스프라이트(`sprite` 배열, version +1)를 검토.

---

# 2026-09-23 정리 때 옮긴 원문(진행상황 현재 상태·다음에 할 것)

## 현재 상태 (2026-09-23)

- **방향: 「사업가 웜뱃」** — 굴 하나 = 업종 하나, 번 돈으로 다음 굴. 기획서 아티팩트 https://claude.ai/artifact/TQaPKFEcLafuVm4WNitbGv . 사용자 답: 업종 하나(빵집)를 먼저 만들어 다듬는다 · 첫 굴은 빵집 고정 · 장기 성장(이사 등)은 미정 · **지상 존 맵 유지, 존 = 굴 입구, 존을 누르면 가게 내부 화면으로 전환**, 위치별 장사 효율은 나중에.
- 코드: 상단 바(코인)·존 맵 + 빵집(`ShopSim` 매 프레임 · `BurrowGrid` 칸 격자 · `ShopLayout` 배치·걷는 땅 · `BurrowNav` 4방향 A* · 손님 행동 트리 · `ShopView` 굴 그림 + 자리·사물 시트). EditMode 86/86. `game_config` v14·`breads` v1·`shop_upgrades` v4·`strings` v8·`visitors` v5(토끼·펭귄·여우·고슴도치, 옆모습은 더미). 프리팹은 **ZooTycoon/Bake/Shop**, 손님 시트는 **ZooTycoon/Bake/Import Visitor Sheets**. 데이터-테이블-규칙 v1.22.
- **손님 동선(2026-09-23, 설계 v0.2 구현·검증 완료, 미커밋)**: 아티팩트 https://claude.ai/artifact/BjZ3x1F5Y5ePaQKU8y9qou . 위치·걷기를 Core로: `ShopLayout`(사물 자리·구멍·줄 자리·서는 자리·걷는 땅, 배치 숫자는 상수) + `BurrowNav`(0.2 격자, 벽·사물 0.3 띄움, 4방향 A* 꺾임 벌점 0.6) + `Mover`. 화면은 Core 위치를 매 프레임 읽기만. 손님 행동 트리(`Bt.cs` 순서·선택·반복 + `ShopSim.Customers.cs`): 톡 나오기 → 빵 고르기(안 가 본 빵 가중치, 재고 안 봄) → 진열대 옆자리 → 집기 / 두리번 최대 lookSeconds 2 → 다른 빵 → 인내(두리번 합계 6초) 다 하면 「!!」. 줄은 계산대를 감쌈(가로 5 + 왼쪽 아래 3), 계산은 머리가 선 뒤. 빵 들고 가기·하트(갈무리 ♥)·구멍 톡. **옆모습 아트는 더미(앞모습 사본)** → 토끼 시안 3장 선택 대기.
- **굴 격자(2026-09-23, 설계 v0.5 확정·구현·검증 완료)**: 아티팩트 https://claude.ai/artifact/B6SSiD3Q5bpGm4xvLhmNiu . 가게 = 보이지 않는 칸(3.375 × 2.4, 입구 줄 2.0)의 집합, 굴 그림은 `BurrowShape`(Core 마스크: 꼭짓점 규칙 둥글림 r=12칸, 입구 깔때기) + `BurrowPainter`(World, 1px = 한 칸, PPU 40)가 팔 때마다 한 장으로 다시 그린다. 자리마다 진열대/오븐 자유, 파기 비용 150 × 1.4ⁿ, 걷기 = 경로 ÷ walkSpeed 3(마지막 세로 한 칸은 enter·toQueue·wombatWalk에 포함). 카메라 고무줄 + 드래그 방향 파기 태그 2초. 방 배경 4장·`make_shop_sections.py` 삭제, 재료는 `floor_tile`·`wall_tile`·`slot_empty`·`arch`(`make_burrow_tiles.py`). 미완: 카메라 배경색은 흙 배경이 덮어 그대로 둠 · 굴 가장자리 손그림 흔들림 없음 · 손님이 계산대 아래 칸으로 갈 때 줄을 가로지름.
- **굴 입구 1단계 + 굴 바닥 적용(2026-09-18)**: 굴 1·2·3단계(`Sprites/World/Burrow/burrow_{1,2,3}.png`, 흙 둔덕 → 통나무 아치 → 잔디 언덕, **단계 기준은 미정**), 62칸 × 4px = 폭 248px, 외곽선 1칸(`--thin`). `ZoneBaker`가 울타리 대신 1단계 굴을 굽고 밑면 중심 = 존 중심. 존 바닥은 굴과 같은 분홍빛 흙 타일 `Yard_00~17`(`BranchBaker`가 칠함). 흙 `Dirt` 타일은 참조 없음.
- **아트 규칙 확정(2026-09-18, 아트-프롬프트 0장)**: 동물은 웜뱃을 단독 참조로 주지 않는다(모양을 베낌) · 격자는 `make_pixel.py`가 강제 · 한 화면 한 칸 크기(지상 서 있는 것 4×4, 타일 4×2). 나무·잔디·길은 아직 옛 격자(나무는 지금 크기로 맞추면 외곽선이 사라져 보류, 다시 뽑을 때 맞춘다). 근거 https://claude.ai/artifact/HHzxLaVcZaMcM4wnsRWVXy .
- **사물 터치 시트 + UI 규칙 v1.0(2026-09-23)**: 기획 https://claude.ai/artifact/CsoJvfeLrRr3dV5K2v1PCN (시트 E: 칩 줄 + 행 목록), UI 규칙 https://claude.ai/artifact/B3sRpN8QNks8zj4Pkc65vh + `기획/UI-디자인-규칙.md`, 시안 https://claude.ai/artifact/H4ZrrMM15oK5jrHumypjbR (시트 B·버튼 A·아이콘 A, 확인 뒤 지운다). 업그레이드 버튼·팝업 2개 삭제, 진열대·잠긴 진열대·오븐·잠긴 오븐·계산대 탭 → `ObjectSheetView/Presenter`, 잠긴 사물 위 비용 태그, 갈무리 폰트(OFL) TMP 비트맵, 부품 PNG는 Codex 시안 → `Sprites/UI/Source~/make_ui.py`. 메뉴: Bake/Import UI Sprites → Fonts → UI → Shop 순서. 폰트는 **Bitmap TTF**(비트맵 스트라이크)만 쓴다: 벡터판은 획이 끊기고 Bilinear 아틀라스는 흐렸다(2026-09-23). 월드 글자는 Galmuri11-Bold fontSize 2.75(= 11칸, 정수 배).
- **연출 1차(2026-09-23)**: 설계 https://claude.ai/artifact/5dfnPjKjk545yDRCxj79ja . 결제 "+N" 팝업(줄 옆)·상단 코인 0.25초 카운트업 · 빈 진열대 "0/8" 깜빡임 + 마지막 2초 말풍선 흔들림(`shop.patienceWarnSeconds`) · 줄 간격 0.8 + 같은 칸 대기자 옆으로 · 상단 바는 코인만(레벨·진행 삭제, `strings` v5) · 안 되는 오븐 탭은 흔들림 + "굽는 중/진열대 가득/심부름 중". 2순위 이후는 보고서 https://claude.ai/artifact/SSj4VMkXBKhyjfHwtywE8g 5장.
- **빵집 소품·배경 실제 아트(2026-09-23)**: 바닥·입구·진열대·오븐(굽기 속도 5단계부터 벽돌 `oven_2`, `shop_upgrades` v2 `lookLevel`)·계산대·빵 3종·말풍선, 전부 PPU 80. 구역 배경은 둥근 방 벽(`make_shop_sections.py`) — 사용자 지적 「배경이 사각형이라 캐릭터와 안 어울림」 뒤 직선 벽·경계선을 버림. 시안 페이지 https://claude.ai/artifact/Qyq6BV66TrSDd9YXWQAcoP (확인 뒤 지운다). 남은 더미: 오븐 진행 막대 `bar_*`, 지상 시설 4종.
- **손님 4종 완료(2026-09-23)**: 펭귄·여우·고슴도치를 토끼 파이프라인으로 추가(`Resources/Sprites/Visitors/{Penguin,Fox,Hedgehog}/`), 손님 시트 칸 폭 60 → 88px(임포터 `k_FrameWidth`). 외형은 행 중 균등 난수. 결과 페이지 https://claude.ai/artifact/Qyocoqb8e6MZTxuzWR89tj (확인 뒤 지운다). 관찰: 재고 0이면 손님이 같은 진열대 자리에 겹쳐 서고 말풍선도 겹친다 · 여우 꼬리가 줄에서 옆 손님과 겹칠 수 있다.
- **첫 손님 토끼 인게임 적용(2026-09-21)**: 앞·뒤 × 숨쉬기·걷기 시트(`Resources/Sprites/Visitors/Rabbit/`), 위로 걸으면 뒷모습. 옛 실루엣 더미 삭제. 시안 비교 아티팩트는 확정 후 삭제(과정은 포트폴리오 4.13). 관찰: 줄 간격 0.55가 토끼 키(1.05)보다 좁아 뒤 손님 얼굴이 앞 손님 귀에 가려진다(연출 3번 「줄 간격」에서).
- 남은 관찰: 굴 픽셀이 웜뱃보다 굵다 · 존 안 동물은 굴 뒤에 가려진다 · 관광객이 존 옆길에서 굴 뒤로 그려질 수 있다(큰 스프라이트 한 장의 y 정렬 한계. 지금은 관광객 0이고 08에서 손님은 내부 화면으로 가므로 보류) · `Assets/_Recovery/0.unity`는 에디터 강제 종료 때 생긴 복구 씬(미추적, 지워도 된다).
- 아트 현황은 `아트-프롬프트.md` 3장. 프리팹 굽기는 에디터 메뉴 **ZooTycoon/Bake/Zone · Branch01**.
- 알려진 문제: 빌드 타깃 Standalone(결정은 Android IL2CPP ARM64) · 잔디 꽃이 3칸 주기로 반복 · 가변 폰트라 텍스트 전부 Bold.

## 다음에 할 것

1. **설계 08 v0.5 구현·검증·판정 완료(2026-09-18)** — 원본 아티팩트 https://claude.ai/artifact/Gu4ULj8nQboKNC9gVdLfiA (설계는 이제 md 대신 아티팩트). 손님이 입구 → 좌우 진열대 → 가운데 줄 → 계산대 → 퇴장, 오븐 탭 → 빵 고르기, 업그레이드 4종 + 굴 넓히기(빵 3번째에 층 추가·세로 스크롤). EditMode 77/77. 3분 플레이 판정 "재미있는 플레이 흐름"(끌린다) · 커밋 완료. 데이터-테이블-규칙 v1.13에 breads·shop_upgrades·shop 반영.
2. **이미지 에셋 뽑기 진행 중.** 웜뱃 정면·뒷모습 + 숨쉬기 idle 완료(2026-09-18, 계산대에서 기다릴 때 정면·계산 중 뒷모습, 둘 다 4프레임 1.6초, 몸 1칸 오르내림 + 귀가 한 박자 늦게. **걷기 + 굽기 심부름(설계 08 v0.6)**: 오븐 탭 → 웜뱃이 오븐까지 걸어가 굽기 시작 → 복귀, 비운 동안 계산 멈춤(`ShopSim` 심부름·`ShopWombat`), `game_config` v10 `shop.wombatWalkSeconds`. EditMode 80/80. 데이터-테이블-규칙 v1.14 반영. 가게 유닛 = 한 칸 2px·PPU 80·스케일 1). 가게 화면 한 칸 = 2px·PPU 64·위 30도 시점 확정(아트-프롬프트 0·1장). 손님 4종·빵집 소품 완료(09-23). 남은 더미는 오븐 진행 막대·지상 시설뿐. 다음: 빵집 더미(구역 배경 4·진열대·오븐·계산대·말풍선·빵 3종). 그 뒤 3번 연출 → 09 직원.
3. **UI 디벨롭(사물 터치)**: 1차 구현 완료(2026-09-23). 남은 것: 오프라인 팝업 등 다른 화면을 같은 부품으로, 09 직원은 계산대 시트에 칩 줄 + 고용 행.
4. **연출 다듬기**: 1순위 5개 완료(2026-09-23). 다음은 보고서 2순위: 빈 오븐 "눌러도 됨" 표시, 굽기 완료 빵 이동, 업그레이드 번쩍·못 사는 줄 빨간 비용, 손님 페이드·화면 전환, 공용 버튼 눌림, 사운드 최소 세트. 남은 더미: 오븐 진행 막대 `bar_*`, 지상 시설 4종.
5. **옆모습 아트**: 토끼 옆모습 시안 3장 비교 https://claude.ai/artifact/VBBmUmkPzaepNfdyeYL3sR (A 완전 옆 추천, 원본 `Shop/Source~/rabbit_side_*`) → 사용자 선택 → 같은 결로 펭귄·여우·고슴도치·웜뱃 → 숨쉬기·걷기 스크립트(옆모습용 SPEC 추가) → 시트. 같은 경로(`<종>_SideIdle`·`_SideMove`, `wombat_side*`)에 덮어쓰면 코드 변경 없음. 커밋 두 개로 나눈다: 규칙 문서(v1.21·v1.22·CLAUDE.md 틱) / 손님 동선 기능.
4. 기능 순서: 08 빵집 코어 → 09 직원 → 10 벽 → 11 오프라인·저장 → 12 굴 지도(존별 위치 효율·굴 단계) → 13 두 번째 업종.
