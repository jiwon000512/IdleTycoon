# 코드 규칙

버전 v1.6 · 2026-09-14
목표는 하나다. **규칙이 어디 있는지 항상 알 수 있고, 한 곳을 고치면 한 곳만 바뀌는 코드.** 스파게티(MonoBehaviour에 규칙이 흩어지고, 서로를 Find로 찾고, 상태를 여기저기서 바꾸는 코드)를 구조로 막는다. 사람과 AI 에이전트 모두 이 문서를 따른다. 줄 단위 표기·포맷·오류 처리·이벤트 규칙은 `프로그래밍-규약.md`. 제네릭 템플릿은 스킬 `unity-project-setup/templates/code-rules.md`.

---

## 1. 결정 (2026-09-13)

| 항목 | 결정 |
|---|---|
| 계층 강제 | asmdef 5개(+Editor 1개). 의존은 아래로만. Core·Data는 `noEngineReferences` |
| UI 패턴 | MVP. View는 MonoBehaviour(표시·입력 이벤트만), Presenter는 순수 C# |
| 의존성 주입 | 게임 규칙 서비스는 `GameManager.Init()`(v1.6, 설계 04)이 만들어 보관하고 생성자 주입. 씬 스크립트(`MainScene`)가 그 씬의 View·Presenter를 조립. DI 프레임워크 없음 |
| 공통 기반(GameKit) | 별도 UPM 패키지 `com.jiwon.gamekit`. 기반 서비스(UI·테이블·저장·이벤트·풀)는 `MonoSingleton<T>` 파사드(lazy 자기 초기화)로 제공. 게임 규칙에는 싱글톤 금지 유지 |
| 이벤트·비동기 | 순수 C# `event` + 동기 틱. 연출은 코루틴. 추가 라이브러리 없음 |
| 상태 변경 | 모델 또는 서비스의 메서드를 통해서만. UI·View는 상태를 직접 바꾸지 않음 |
| 테스트 | Core·Data의 규칙·수식은 EditMode 테스트 필수. Presenter는 권장 |

## 2. 계층과 어셈블리

```
Game ──▶ UI ──▶ Core            (컴포지션 루트가 맨 위. Game은 Data·UI·World·Core를 전부 본다)
Game ──▶ World ──▶ Core         (2D 월드: 우리·동물 개체·카메라. UI와 서로 참조하지 않는다)
Game ──▶ Data ──▶ Core          (화살표 = 참조 가능. 역방향 참조 금지)
Game, UI ──▶ GameKit.Unity      (Manager 파사드)
Tests.EditMode ──▶ Core, Data, UI, GameKit.Core
Editor ──▶ 전부 (에디터 전용 도구)
```

**UI는 Game을 참조하지 않는다**(2026-09-14 변경). 씬 스크립트(Game)가 Presenter를 조립하려면 Game이 UI를 볼 수 있어야 하고, UI 쪽은 Core만 있으면 되기 때문이다. 따라서 UI 코드는 `GameManager`를 직접 부르지 않고, 씬 스크립트가 `GameManager`의 서비스를 Presenter 생성자에 넘긴다.

| asmdef | 폴더 · 네임스페이스 | 역할 | 참조 | 엔진 참조 |
|---|---|---|---|---|
| `ZooTycoon.Core` | `Scripts/Core` · `ZooTycoon.Core` | 게임 규칙·수식·상태 모델·세이브 모델. **JSON 테이블 레코드와 `GameTables`·`StringTable`도 여기 둔다**(Core 서비스가 레코드를 직접 읽으므로). `IClock`, `IRandom` 경계 인터페이스 선언. 알림은 C# event | 없음 | 없음 |
| `ZooTycoon.Data` | `Scripts/Data` · `ZooTycoon.Data` | 테이블 검증기(`TableValidator`). 로드는 GameKit `TableManager`가 하고 파싱된 행을 `GameTables`로 받음 | Core | 없음 |
| `ZooTycoon.Game` | `Scripts/Game` · `ZooTycoon.Game` | 유니티 어댑터: `GameManager`(상태·서비스 생성·보관, 수입 틱), 씬 스크립트(`MainScene`), 시계·난수 구현. 테이블·저장은 GameKit Manager 호출 | Core, Data, UI, `GameKit.Core`, `GameKit.Unity`, `UnityEngine.UI`, `Unity.InputSystem` | 있음 |
| `ZooTycoon.UI` | `Scripts/UI` · `ZooTycoon.UI` | HUD·팝업 View(MonoBehaviour)와 Presenter(순수 C#). 화면 열기·닫기는 GameKit `UIManager` | Core, `GameKit.Core`, `GameKit.Unity`, `UnityEngine.UI`, `Unity.TextMeshPro` | 있음 |
| `ZooTycoon.World` | `Scripts/World` · `ZooTycoon.World` | 월드(설계 03): XZ 평면 위 `CageView`, `AnimalActor`(이동, 빌보드 카드), `WorldView`/`WorldPresenter`, `WorldCameraController`(원근 카메라 초점·팬). 연출 난수는 `UnityEngine.Random` | Core, `Unity.InputSystem` | 있음 |
| `ZooTycoon.Tests.EditMode` | `Tests/EditMode` · `ZooTycoon.Tests` | 유닛 테스트. 실제 JSON을 `TableFile<T>`로 읽으므로 GameKit·Newtonsoft를 참조한다 | Core, Data, UI, `GameKit.Core`, `Newtonsoft.Json.dll`, TestRunner | Editor 전용 |
| `ZooTycoon.Editor` | `Scripts/Editor` · `ZooTycoon.Editor` | 에디터 도구(테이블 검증 메뉴 등) | 전부 | Editor 전용 |

- 폴더 = 네임스페이스 = 어셈블리. 하위 폴더는 네임스페이스에 붙이지 않는다(`ZooTycoon.Core` 안에서 `Gacha/`, `Income/` 폴더로만 나눔).
- Core·Data가 UnityEngine을 쓰고 싶어지면 그것은 설계 오류 신호다. 필요한 것을 인터페이스로 뽑아 Game에서 구현한다(예: 로그가 필요하면 `ILogger`).
- 새 asmdef를 추가하거나 참조를 바꾸는 것은 사용자 승인 사항이다.

## 3. 설계 원칙 (금지와 필수)

**필수**
- 클래스 하나 = 책임 하나 = 파일 하나. 파일 이름 = 클래스 이름.
- 의존은 생성자로 받는다. 서비스는 `GameManager.Init()`, Presenter는 씬 스크립트(`MainScene`)만 `new`로 조립한다.
- 외부 경계(시간, 난수, 저장, 테이블 텍스트, 로그)는 인터페이스 뒤에 둔다. Core는 `DateTime.UtcNow`, `System.Random`을 직접 쓰지 않는다.
- 상태는 모델(`ZooState`)이 소유하고, 변경은 모델 또는 서비스의 메서드로만 한다(여러 서비스가 건드리는 값은 모델에 둔다 — 코인이 그렇다). 변경 결과는 `event`로 알린다.
- 숫자는 `game_config.json` 등 테이블에서 온다(`데이터-테이블-규칙.md`). 코드 상수는 형식(포맷 자릿수 등)에만 허용.
- public 표면은 최소로. 기본은 `internal`/`private`. 다른 어셈블리가 써야 할 때만 `public`.
- 불변을 기본으로. 레코드·설정은 `readonly`/init-only. 컬렉션은 `IReadOnlyList<T>`로 노출.

**금지**
- MonoBehaviour 안의 게임 규칙(코인 계산, 확률, 비용 곡선, 레벨 판정 등). MonoBehaviour는 생명주기 훅에서 서비스를 호출만 한다.
- 게임 코드(ZooTycoon.*)의 `static` 가변 상태, 싱글턴(`Instance`), 서비스 로케이터. 예외: GameKit Manager(`UIManager`, `TableManager`, `DataManager`, `EventManager`, `PoolManager`)와 **`GameManager`**(v1.6: 게임 상태·서비스를 씬 사이에서 유지. 게임 규칙은 넣지 않는다)의 `Instance` 접근은 Game 계층(Manager는 UI도)에서 허용한다. Core·Data는 어셈블리 경계로 접근 불가.
- `FindObjectOfType`, `GameObject.Find`, `GetComponentInChildren`으로 의존을 "찾는" 코드. 참조는 주입받거나 `[SerializeField]`로 인스펙터에서 연결한다.
- `Update`에서 게임 로직 폴링. 수입 갱신은 `GameManager.Update` 하나가 `game_config.income.tickSeconds`(1초)마다 `IncomeService.Tick(dt)`를 호출한다.
- UI에서 모델 필드를 직접 대입하거나 저장을 호출하는 것. UI는 Presenter를 통해 서비스 메서드를 호출한다.
- 순환 참조(A→B→A). asmdef가 계층 간은 막지만 계층 안에서도 금지. 이벤트로 끊는다.
- 인터페이스를 위한 인터페이스. 구현이 하나뿐이고 테스트 대역도 필요 없으면 클래스를 그대로 쓴다.
- Manager에 게임 규칙을 넣는 것. Manager는 GameKit 파사드 전용이며 순수 C# 클래스를 감싸 Unity 수명주기만 더한다(`프로그래밍-규약.md` 10장).
- 200줄 이상 클래스, 5개 이상 생성자 매개변수. 넘으면 분리를 검토하고 이유를 남긴다.
- `#region`, 주석 처리된 코드, TODO 없는 임시 코드.

## 4. 계층별 규칙

**Core**
- 결정적이어야 한다. 같은 입력·같은 `IRandom` 시드면 같은 결과. 이것이 테스트 가능성의 근거다.
- 서비스는 상태를 받아 변경하고 결과 객체를 돌려준다(예: `GachaService.Pull(state) → PullResult`). 결과 객체는 `readonly struct`나 불변 클래스.
- 이벤트 이름은 과거형 명사구: `CoinsChanged`, `AnimalPulled`, `ZooLevelReached`.

**테이블 (Core) · 검증 (Data)**
- 레코드는 JSON 필드와 1:1인 클래스이며 Core에 둔다. Newtonsoft 역직렬화 때문에 `get/set`을 허용한다(이 한 곳만 불변 원칙의 예외). 파생값(확률 등)은 별도 계산 클래스(`GachaTable`)가 만든다.
- 로드는 GameKit `TableManager`가 하고, `GameManager.Init()`이 파싱된 행으로 `GameTables`를 만든다. `TableValidator`(Data)가 `데이터-테이블-규칙.md` 7장 규칙을 검사해 실패 목록을 돌려준다. 예외를 던지지 않고 결과로 보고한다.
- 화면 문구는 `strings.json` → `StringTable`. UI 코드·프리팹에 한국어 리터럴을 두지 않는다(예외 메시지는 한국어가 규칙이므로 제외).

**Game**
- 씬에 놓는 게임 MonoBehaviour는 씬 이름 스크립트(`MainScene`) 하나다. `Awake`에서 `GameManager.Instance.Init()`을 부른 뒤 그 씬의 View·Presenter만 조립한다. `GameManager.Init()` 순서: 테이블 로드·검증 → 저장 로드 → 오프라인 수익 계산 → 서비스 생성. 틱은 `GameManager.Update`.
- 어댑터(`ITableSource`, `ISaveStorage`, `IClock`, `IRandom` 구현)는 GameKit이 제공한다. 게임 전용 어댑터가 필요할 때만 Game에 만들고 이름으로 역할을 드러낸다(`XxxSource`, `XxxStorage`).
- 코루틴은 연출(숫자 보간, 뽑기 애니메이션)에만. 규칙 흐름을 코루틴으로 쓰지 않는다.

**UI (MVP)**
- View: `[SerializeField]`로 위젯 참조, `Show(...)`/`Set...(...)` 표시 메서드, `event Action Clicked` 같은 입력 이벤트. 계산·분기·상태 보관 금지. Core를 몰라도 컴파일되게 원시 타입·DTO만 받는다.
- Presenter: 생성자로 View 인터페이스와 서비스를 받는다. 모델 이벤트 구독 → View 갱신, View 입력 → 서비스 호출. `IDisposable`로 구독 해제.
- View 하나에 Presenter 하나. 팝업도 같은 구조. Presenter끼리 직접 참조하지 않고 모델 이벤트로 동기화한다.

**Tests**
- Core 서비스·수식은 기획서 표의 숫자를 그대로 테스트 케이스로 옮긴다(예: 6.3 표의 n=10 → 311).
- 테이블 검증 테스트는 실제 JSON을 로드해 7장 규칙을 전부 검사한다.
- 테스트 이름: `대상_조건_기대` (`GachaCost_At10Pulls_Is311`).

## 5. 이름과 스타일

- 식별자 영어, 주석·문서·커밋 한국어. 주석은 "왜"를 적는다. "무엇"은 이름이 말한다.
- 표기는 `프로그래밍-규약.md` 2장(Unity 6판 가이드): `PascalCase` 타입·메서드·프로퍼티, `m_camelCase` private 필드, `s_` static, `k_` 상수, `camelCase` 지역·매개변수, 인터페이스 `I`, 이벤트 발생 `On...`, 핸들러 `Subject_EventName`.
- 접미사로 역할을 드러낸다: `...Service`(규칙 실행), `...Calculator`(순수 계산), `...Table`(테이블 조회), `...Storage`/`...Source`(IO 경계), `...View`/`...Presenter`, `...Ticker`(주기 호출). `...Manager`는 GameKit 파사드에만.
- 4칸 들여쓰기, 여는 중괄호 새 줄, `var`는 우변에서 타입이 보일 때만. 한 줄 120자.
- `public` 필드 금지. 인스펙터 노출은 `[SerializeField] private`.

## 6. 작업 절차 (2026-09-14 확정)

작업 단위(기능 묶음)마다 네 단계로 진행한다.

1. **설계**: 에이전트가 `기획/설계/NN-<이름>.md`에 ① 코드 설계(계층 / 클래스 / 책임 / 의존 / 공개 API) ② QA 계획(무엇을 어떤 방법으로 검증하는지, 통과 기준) ③ 기획서 대응 표 번호를 적는다. 사용자와 함께 검토·수정해 확정한다.
2. **구현**: 확정된 설계대로 에이전트가 한 번에 전부 구현한다. Unity CLI(unity-cli 스킬)를 최대한 써서 스크립트 작성·컴파일·씬·프리팹·플레이 모드까지 에디터를 직접 조작한다. 중간 확인을 위해 사용자를 기다리지 않는다.
3. **검증 (2026-09-14 오후 확정)**: 에이전트가 **1회 실행해 직접 검증**한다 — 컴파일 오류 0 · 콘솔 오류 0 · EditMode 테스트 통과(`run_tests`는 플레이 모드가 아닐 때만) · 플레이 모드 진입 후 캡처로 동작 확인. 이것이 개발 완성의 정의다.
4. **알림**: 짧게 보고한다. 만든 것, 검증 결과, 설계와 달라진 점. 테스트 방법 문서는 쓰지 않고 문서화는 최소한만(진행상황 갱신 + 설계 문서에 달라진 점 몇 줄).

규칙을 어길 수밖에 없을 때는 코드에 `// 규칙 예외: <이유>` 주석을 남기고 보고에 적는다. 예외가 반복되면 규칙을 고친다.

## 7. 리뷰 체크리스트 (스파게티 징후)

- [ ] MonoBehaviour에 `if`가 두 단계 이상 중첩된 게임 규칙이 있다
- [ ] `static` 필드, `Instance`, `Find...`가 보인다
- [ ] 한 클래스가 계산·표시·저장 중 둘 이상을 한다
- [ ] 같은 숫자가 두 파일에 있다
- [ ] UI 코드가 모델 프로퍼티에 대입한다
- [ ] 이벤트 구독을 해제하지 않는다
- [ ] 클래스가 200줄, 메서드가 40줄을 넘는다
- [ ] 게임 코드 이름이 `Manager`, `Helper`, `Util`, `Common`이다(책임이 불명확하다는 신호. GameKit 파사드는 예외)

## 8. 첫 버전 클래스 지도 (제안, 구현 시 설계 목록으로 확정)

| 계층 | 클래스 | 책임 |
|---|---|---|
| Core | `ZooState` | 코인, 누적 코인, 뽑기 횟수, 홍보 단계, 동물별 레벨, 대기실, 마지막 저장 시각 |
| Core | `GachaService` | 비용 계산(6.3), 가중치 추첨(`IRandom`), 새 종/레벨업/최대레벨 처리, `AnimalPulled` 이벤트 |
| Core | `IncomeCalculator` | 동물별 수입(6.2), 합계 × 홍보 배수 |
| Core | `IncomeService` | `Tick(dt)`로 코인 적립, `CoinsChanged` |
| Core | `PromotionService` | 단계 비용(6.4), 업그레이드, 해금 확인 |
| Core | `ZooLevelService` | 누적 코인 → 레벨·우리 수, 대기실 자동 입장, `ZooLevelReached` |
| Core | `OfflineIncomeCalculator` | 경과 시간(캡) × 초당 수입 |
| Core | `SaveData` | 상태 ↔ 세이브 DTO. 저장·로드는 GameKit `SaveService`/`DataManager` |
| Core | `IClock`, `IRandom` | 시간·난수 경계(게임이 선언, Game 계층이 구현) |
| GameKit | `TableFile<TRow>`, `SaveFile<T>` | 테이블·세이브 봉투(패키지 제공) |
| Core | `AnimalRecord`, `GradeRecord`, `ZooLevelRecord`, `StringRecord`, `GameConfig` | JSON 레코드 |
| Core | `GameTables`, `StringTable` | 테이블 조회·UI 문구 |
| Data | `TableValidator` | 데이터-테이블-규칙 7장 검증 |
| Game | `GameManager` | MonoSingleton. `Init()`으로 테이블·상태·서비스 생성, `Update`에서 1초 틱, (08) 주기 저장 |
| Game | `MainScene` | Main 씬 스크립트. `GameManager.Init()` 호출 후 View·Presenter 조립 |
| Game | `SystemClock`, `SystemRandom` | `IClock`, `IRandom` 구현(시드 없음. 결정성은 테스트 대역 `SequenceRandom`이 맡는다) |
| GameKit | `MonoSingleton<T>`, `EventManager`, `PoolManager`, `TableManager`, `DataManager`, `UIManager`/`UIView` | 기반 Manager(패키지 제공, v0.1) |
| UI | `TopBarView/Presenter`, `GachaButtonView/Presenter`, `GachaResultView/Presenter`, `PromotionPopupView/Presenter`, `CodexPopupView/Presenter`, `OfflineRewardPopupView/Presenter` | HUD·팝업(기획서 4장) |
| World | `CageView`, `AnimalActor`, `WorldView/WorldPresenter`, `WorldCameraController`, (05) `VisitorActor`, `PathView` | 2D 월드(기획서 4장 월드) |
| UI | `BigNumberFormatter` | K/M/B 표기(표시 전용이므로 UI에 둠) |

## 변경 이력

- v1.0 (2026-09-13) 최초 작성. asmdef 계층 강제·MVP·수동 DI·C# event 결정 반영
- v1.1 (2026-09-13) GameKit 패키지와 MonoSingleton Manager 허용 범위 추가. 표기 규칙을 `프로그래밍-규약.md`로 이관(m_/s_/k_, Subject_EventName)
- v1.2 (2026-09-14) 설계 01 구현 반영: 레코드·`GameTables`·`StringTable`을 Core로, Data는 검증기만. 의존 방향을 `Game → UI`로 뒤집어 컴포지션 루트를 한 곳으로 유지. 상태 변경은 모델 메서드도 허용. Tests가 GameKit.Core·Newtonsoft 참조
- v1.3 (2026-09-14) 작업 절차 3단계 QA를 사용자가 직접 하는 것으로 변경(에이전트는 컴파일 0·콘솔 0까지, 테스트 방법 정리로 마무리). 클래스 지도 `SeededRandom` → `SystemRandom`
- v1.4 (2026-09-14) 설계 03: `ZooTycoon.World` asmdef 추가(Game → World → Core). 클래스 지도에서 uGUI 우리 격자 삭제, World 행 추가
- v1.5 (2026-09-14) 절차 3단계를 "에이전트가 1회 실행해 검증"으로 개정(v1.3의 사용자 QA 철회). 테스트 방법 문서 폐지, 문서화 최소화
- v1.6 (2026-09-14) 설계 04: `GameBootstrap`·`GameTicker` 폐지 → `GameManager`(MonoSingleton, 싱글턴 금지의 예외) + 씬 스크립트 `MainScene`. 수입 틱 1초
