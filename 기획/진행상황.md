# 진행 상황 (세션 인수인계)

세션 시작에 읽고 끝에 갱신한다. **세 절만 두고 각 절은 10줄 안쪽**으로 유지한다. 지난 일은 여기 쌓지 않는다 — 코드 결정은 `설계/00-구현-요약.md`, 기능별 기록은 `포트폴리오.md` 4장, 경위는 커밋 로그. 2026-09-17까지의 상세 원문은 `진행상황-이력.md`(세션 시작 시 읽지 않는다).

## 현재 상태 (2026-09-23)

- **방향: 「사업가 웜뱃」** — 굴 하나 = 업종 하나. 빵집을 먼저 다듬는다. 기획서 https://claude.ai/artifact/TQaPKFEcLafuVm4WNitbGv . 지상 존 맵 유지, 존을 누르면 가게 내부.
- 코드: 상단 바(코인)·존 맵 + 빵집(`ShopSim` 매 프레임 · `BurrowGrid` 칸 · `ShopLayout` 배치·걷는 땅 · `BurrowNav` 4방향 A* · 손님 행동 트리 · `ShopView` 굴 그림 + 사물 시트). EditMode 86/86. `game_config` v14·`breads` v1·`shop_upgrades` v4·`strings` v8·`visitors` v5. 데이터-테이블-규칙 v1.22. 굽기 메뉴: Import UI Sprites → Fonts → UI → Shop, 손님 시트는 Import Visitor Sheets.
- **손님 동선(설계 v0.3, 구현·검증·커밋 완료)** https://claude.ai/artifact/BjZ3x1F5Y5ePaQKU8y9qou : 위치·걷기는 Core(`ShopLayout`·`BurrowNav`·`Mover`), 화면은 매 프레임 읽기만. 행동 트리: 톡 나오기 → 안 가 본 빵(가중치) → 진열대 옆자리 → 집기 / 두리번 최대 2초 → 다른 빵 → 인내(두리번 합계 6초) 다 하면 「!!」. 줄은 계산대를 감쌈, 계산은 머리가 선 뒤. **옆모습은 더미(앞모습 사본)**.
- **굴 격자(설계 v0.5)** https://claude.ai/artifact/B6SSiD3Q5bpGm4xvLhmNiu : 보이지 않는 칸(3.375×2.4)의 합집합을 `BurrowShape`+`BurrowPainter`가 한 장으로 그림, 파기 150×1.4ⁿ, 고무줄 카메라 + 드래그 방향 파기 태그.
- **UI**: 사물 터치 시트 + UI 규칙 v1.0(`기획/UI-디자인-규칙.md`), 갈무리 **Bitmap TTF** 폰트(벡터판은 획이 끊김).
- **아트**: 빵집 소품·배경·손님 4종(앞·뒤) 실제. 남은 더미: 옆모습 5종, 오븐 진행 막대 `bar_*`, 지상 시설 4종. 규칙은 `아트-프롬프트.md` 0장.
- **연출 1차** 완료. 2순위 목록은 https://claude.ai/artifact/SSj4VMkXBKhyjfHwtywE8g 5장.
- 알려진 문제: 빌드 타깃 Standalone(결정은 Android IL2CPP ARM64) · 줄 손님이 식빵 진열대 아랫단을 조금 가림 · 손님끼리 비켜 걷지 않음 · `Assets/_Recovery/0.unity`(미추적, 지워도 됨).

## 다음에 할 것

1. **옆모습 아트 완료**: 5종 옆모습(토끼 3차 A·펭귄 A·여우 B·고슴도치 A·웜뱃 B)을 더미 경로에 덮어씀. 게임 확인은 토끼·펭귄·여우·고슴도치까지. 웜뱃 옆모습은 선반이 가득 차 굽기가 막혀 게임에서 못 봤다(Bake/Shop 뒤 확인). 마지막 플레이 뒤 콘솔 오류 8건 미확인.
2. **연출 2순위**: 빈 오븐 "눌러도 됨", 굽기 완료 빵 이동, 업그레이드 번쩍·못 사는 줄 빨간 비용, 공용 버튼 눌림, 사운드 최소 세트.
3. **UI 디벨롭**: 오프라인 팝업 등 다른 화면을 같은 부품으로.
4. 기능 순서: 09 직원(계산대 시트에 고용 행) → 10 벽 → 11 오프라인·저장 → 12 굴 지도(존별 위치 효율·굴 단계) → 13 두 번째 업종.

## 도구 함정 (Unity CLI)

- 플레이·재임포트 부산물이 git 변경으로 남는다: 갈무리 폰트 에셋(동적 아틀라스에 글자 추가), 손님 시트 메타(임포터가 spriteID를 새로 만듦), `CoinPopup.prefab`(Bake/Shop이 금액 글자를 다시 붙임). 기능과 무관하면 커밋 전에 `git checkout`으로 되돌리고 `AssetDatabase.Refresh`(2026-09-23).
- 구현·에셋 작업 전 `editor_status`의 `playMode` 확인. 플레이 중이면 씬 저장·프리팹 교체가 틀어진다. **`run_tests`를 부르는 그 명령 줄 안에서** `playMode=stopped`를 다시 확인한다(2026-09-23: 사용자가 에디터에서 플레이를 켜 둔 상태에서 호출 → 150초 타임아웃 → 에디터 멈춤 → 재시작). `run_tests`는 반드시 `editor_stop` 후 `playMode=stopped`에서 `--mode EditMode --timeout 150`으로 **한 번만** 호출(같은 명령을 파이프로 두 번 부르면 두 번째가 겹쳐 테스트 러너가 깨지고 파이프라인이 막힌다. 2026-09-18). 막히면 `taskkill //F //PID`로 에디터를 죽이고 **`ProjectTycoon/Temp/UnityLockfile`을 지운 뒤** `unity projects open`(잠금 파일이 남으면 모달이 떠 영원히 연결되지 않는다).
- `eval`은 5초 넘으면 타임아웃 오류를 콘솔에 남긴다(스프라이트 여러 장 임포트하는 굽기). 작업은 끝나 있으니 프리팹을 조회해 확인하거나 `run_script`로 굽는다.
- 프리팹을 다시 구운 뒤에는 플레이를 새로 시작한다(이미 돌던 세션은 옛 프리팹으로 만든 개체를 그대로 쓴다).
- `capture_game_view --save_path`는 프로젝트 안 경로만 받고 `Temp/x.png`가 `Assets/Temp/`에 저장된다(에셋으로 임포트됨). 확인 뒤 `AssetDatabase.DeleteAsset("Assets/Temp")`.
- 에디터가 포커스를 잃으면 플레이 프레임이 멈춘다(`editor_focus`도 곧 풀림). 긴 관찰은 플레이 중 `Application.runInBackground=true`를 eval로 켠다(런타임만, 저장 안 됨).
- 에디터 창이 뒤에 있으면 플레이 프레임이 멈춘다 → `editor_focus`. **편집 모드에서 모든 명령이 30초 타임아웃되는데 포트는 ready·프로세스는 응답·모달 없음이면 에디터 메인 스레드가 멈춘 것**(2026-09-23 10:58, 플레이 종료 `editor_stop` 직후 `eval`로 `AssetDatabase.DeleteAsset(\"Assets/Temp\")`를 부른 다음 발생. 라이선스 클라이언트 로그의 주기 요청이 그 시각에 끊기고 CPU 0, 전경으로 올려도 반응 없음). 파이프라인은 명령을 `EditorApplication.update`에서 꺼내므로 update가 멈추면 배경 명령까지 실행 게이트(한 번에 하나) 뒤에서 같이 죽는다 — 재시도를 1분마다 보내면 게이트를 더 막으니 하지 말 것. 복구는 taskkill → `Temp/UnityLockfile` 삭제 → `unity projects open`뿐. 예방: 플레이 종료 뒤 `editor_status`로 `playMode=stopped`를 확인한 다음에 에셋 삭제/임포트를 부른다. Interaction Mode는 No Throttling(레지스트리 값 1)로 바꿔 둠(배경 절전 배제용, 이 멈춤의 원인은 아님).
- 같은 경로에 에셋을 다시 만들 때는 `DeleteAsset` → `CreateAsset` → 프리팹 `ImportAsset(ForceUpdate)` → `recompile` 뒤 플레이(옛 객체가 "이름 없는 Tile"로 남는다).
- 콘솔은 세션을 넘어 남는다. 컴파일 판정 전에 `clear_console` → `recompile` → `console --json`(옛 오류를 새 오류로 오인한 적 있음, 2026-09-23). 명령 목록은 `unity list`.
- 콘솔 로그 조회는 `console --json`(`console_logs` 명령은 없다. 잘못 부르면 그 호출 자체가 콘솔 오류 1건으로 남는다).
- 여러 줄 C#은 `run_script --file --entry --mode ephemeral`. 일회성 조립·조회 스크립트만 scratchpad, 프리팹을 통째로 만드는 스크립트는 `Scripts/Editor`에 MenuItem으로. `GetComponent ?? AddComponent`는 가짜 null로 실패 → `TryGetComponent`.
- 플레이 중 상태는 `GameManager.Instance.State/...`를 `run_script`로 읽는다. 1초짜리 연출은 캡처 대신 상태 조회. 정확한 화면 좌표는 `capture_game_view --source camera --width 1080 --height 1920`.
- 콘솔의 `TreeViewController NullReference`·`GUIClips`는 Hierarchy 창의 에디터 내부 오류. 게임 오류 판정은 `error CS`·게임 스택만.
- Git Bash는 `export MSYS_NO_PATHCONV=1`. 에디터 열기 `unity projects open ProjectTycoon`, 연결 확인 `unity pipeline list`.
- 에디터 게임 뷰가 가로 1280×720이라 `--source screen` 캡처는 가로로 나온다. UI까지 세로로 확인하려면 플레이 중 캔버스를 `ScreenSpaceCamera` + `sortingOrder` 5000으로 바꾼 뒤 `--source camera --width 540 --height 960`(scratchpad `TapCheck.cs`). 재컴파일은 플레이가 끝날 때까지 미뤄지므로, 플레이 중 컴파일 0은 새 코드를 검사한 결과가 아니다.
