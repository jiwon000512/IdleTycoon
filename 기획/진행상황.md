# 진행 상황 (세션 인수인계)

세션 시작에 읽고 끝에 갱신한다. **세 절만 두고 각 절은 10줄 안쪽**으로 유지한다. 지난 일은 여기 쌓지 않는다 — 코드 결정은 `설계/00-구현-요약.md`, 기능별 기록은 `포트폴리오.md` 4장, 경위는 커밋 로그. 2026-09-17까지의 상세 원문은 `진행상황-이력.md`(세션 시작 시 읽지 않는다).

## 현재 상태 (2026-09-26)

- **방향: 「사업가 웜뱃」** — 굴 하나 = 업종 하나. 빵집을 먼저 다듬는다. 기획서 v0.9 https://claude.ai/artifact/TQaPKFEcLafuVm4WNitbGv . 지상은 폐기. 굴 밖은 넓은 광장(빵집 문만, 같은 지갑, 순서대로 개업, 꾸미기는 보기·들를 곳만).
- 코드: 상단 HUD(시안 C: 바 없이 반투명 캡슐 + 금색 코인 숫자, 가게 이름 줄 없음, UI 규칙 v1.1) + **설계 13 곳과 사물** https://claude.ai/artifact/MHnPqbnv4h93PoPdxZD9gf : 곳 `WombatArea` → `BakeryArea`·`PlazaArea`, 사물 `Interactable` → `OvenInteractable` 등 7종(상태·기준점), 웜뱃 하나(`Wombat` = Mover + 일꾼 `Worker`(손 + 지갑), Mall이 두 곳에 넘김), Core 폴더 `Area`·`Bakery`·`Plaza`·`Burrow`. **행동 = `ActionFactory` 중첩 클래스(v0.6, sim별 partial)**: 사물에서 할 일은 `InteractableTable.actions`, 방식은 `ActionTable.mode`(auto·manual·sheet). 업그레이드는 행동 하나(데이터 = 사물 행의 `UpgradeInfo`, 단계는 종류 공통), 오븐 추가는 빈 자리의 오븐 설치(가격 `BakeryConfigTable`), `ShopUpgradeTable` 삭제. **리뷰 R2(에이전트 자가 리팩토링, 2026-09-24)**: 계산대가 줄·계산(`CounterInteractable.Queue`·`Tick`·`Serve`), 손님은 `Visitor` → `BakeryVisitor`(행동 트리 스스로)·`PlazaVisitor`, 통로 `PassageInteractable`(exit·door), `Wombat.Walk`, `Hands.Changed(사물)`, `TryChoose`, 값은 행동이 지갑에서, 굴 상수는 `BurrowShape`·`BurrowNav`, World 이름 `Shop*` → `BakeryView`·`WombatView`·`VisitorView`·`BakerySound`·`BakeryVisitorSpawner`, UI `ControlHudView`, 프리팹 `Prefabs/Bakery`. 빵집(`BakeryArea` 매 프레임 · `BurrowGrid` · `BakeryLayout` · `BurrowNav` A* · `BakeryView`). **설계 16 사건 버스(R3, 2026-09-25)** https://claude.ai/artifact/Jgovpc2wQNbYPvzu7ZcWPM : 사건 16개는 `Core/Events/Events.Area·Bakery·Plaza.cs`(`readonly struct`, 첫 필드 = 발신자)에 선언, 발행은 주인이 `Area.Bus`로, 구독은 순수 C# `GameKit.Events.EventBus`(`GameManager.Init`이 `new EventBus()`로 만들어 생성자로 넣음, 사건 싱글턴 없음, 테스트는 `new EventBus()`) 토큰 + 발신자의 곳으로 거르기. C# event는 `Interactable.Changed`·`Hands.Changed`·UI View 입력만. 곳 옮기기는 `Passed` 사건 → Mall. GameKit 테스트는 manifest `testables`로 EditMode에 포함. EditMode 128/128(GameKit 4 포함). **테이블 11개 = `XXTable.json` ↔ 행 클래스 `XXTable : Table<TId>`, 조회는 GameKit `TableSet.Get<T>(id)`·`GetAll<T>()`만**(설정은 `ConfigTable`·`BakeryConfigTable`·`PlazaConfigTable`·`PlazaDecorTable`, 문구는 `tables.Text/Format`), 데이터-테이블-규칙 v1.36·코드-규칙 v1.15·프로그래밍-규약 v1.4(상속 이름 = 부모의 마지막 명사, 중첩 클래스 예외, `XXData`). 손님 비켜 걷기(`Visitor.Sidestep`, 화면만 더함 — 걷는 손님은 앞사람 반대쪽·마주 오면 오른쪽, 선 손님은 밀려남), 줄 머리는 계산대 바로 위 줄(−5.0). **빵 없을 때 손님(2026-09-25)**: 빈 진열대 앞에서 진열대를 본 채 머리 위 점 셋 말풍선(`wait_sheet` 3프레임 0.4초, 선 순간부터 점이 늚, `VisitorView.m_waitFrames`), 좌우 두리번·「!!」·퇴장 효과음은 뺐다(Core `Angry`·`BakeryVisitorGaveUp` 사건은 광장 재진입 판정용으로 남김). 시트 자르기는 `VisitorSheetImporter.Import(path, sheet, frameWidth)`를 굽기가 같이 쓴다. **설계 17 빈 진열대는 아무 빵(2026-09-26, 기획서 v0.5 확정 결정 표에 기록)**: 진열대 `Bread`는 null 가능(첫 `Put(bread, n)`이 정하고 `TryPick`으로 0이 되면 null), 채우기는 같은 빵 진열대에 자리가 남았으면 빈 진열대를 안 씀(`BakeryArea.HasRoomFor`), 빵 해금은 오븐 굽기 시트의 다음 빵 칩(`Bake.TryChoose` → `UnlockBread`, 빈 오븐이면 바로 굽기), 진열대는 빈 자리 `place_shelf`(150 × 2ⁿ, 최대 4, `Unlock` 행동·`SheetOptionState.Locked` 삭제), 손님은 `ShelfFor(bread, from)`(재고 있는 가장 가까운 곳, 없으면 가장 가까운 진열대에서 점 셋)로 가고 다른 빵으로 채워진 진열대에서는 집지 않는다. `ShelfOf` 삭제 → `ShelfFor`·`StockOf`. 재고 0이면 빵 아이콘 끔(`ShelfView.Show(icon, stock)`). 섞어 담기(안 2)·칸 진열대(안 3)는 보류(칸 진열대는 층 틈이 빵 높이만큼 있는 새 진열대 그림이 먼저). **레이어(2026-09-25)**: 정렬점 Pivot은 `Editor/WorldSpritePostprocessor`(`OnPostprocessPrefab`)가 프리팹 임포트 때 모든 SpriteRenderer에 강제한다(프리팹 파일에는 Center로 남아도 임포트 결과는 Pivot, 검사·테스트 불필요). 실행 중 만드는 렌더러(`PlazaView` 장식)만 코드에서 Pivot. 사물의 부품 order(오븐 불·계산대 출력기 등)는 반드시 SortingGroup 안에서만 — 계산대는 `Body` 자식 그룹(밑변 = CounterBase), 웜뱃은 그룹 밖. 계산 진행은 계산대 왼쪽 영수증 출력기(`counter_timer_c` 16프레임, 평소 00·계산 중 01~15, `Counter.Serving/Progress`, 2026-09-25 사용자 선택 C). 진열대 재고는 벽 쪽 앞 모서리의 칠판 입간판(재고 0 깜빡임은 뺌, `ShelfSignView`, 자리 `BakeryLayout.ShelfSignBase`, 벽 쪽 옆자리는 표지판 너머 −3.0). **굴 환경 A2**: 드러난 윗변 위 지층 띠 40칸(`BurrowShape.WallRow`, 걷는 바닥은 그대로 `IsFloor`), 방 둘레 8방향 턱 6칸(`BurrowPainter.k_Ledge`), 아치 피벗 아래 = 57칸 선. 빈 자리는 점선 칸만. 굽기 메뉴: Import UI Sprites → Fonts → UI → Bakery, 손님 시트는 Import Visitor Sheets. **굴 밖 광장(설계 11)**: `Mall`(빵집·광장을 함께 틱, 웜뱃 하나를 옮김) · `PlazaArea`(지상 계단 도착 → 장식 들를 곳 → 빵집 문 `Admit`, 나온 손님은 문에서 다시) · `WombatArea`(HUD·웜뱃 그림 공용) · `Visitor`(손님 그림 공용) · 빵집 구멍 앞 `exit`/광장 문 `door` 버튼 · 전환 0.3초 페이드 인.
- **설계 18 자유 배치·편집 모드(2026-09-26)**: 사물(진열대·오븐·계산대·광장 장식)은 칸이 아니라 좌표 + 바닥 사각형(`IPlaced`, 표 `IPlacedKind`: `halfWidth`·`depth`·`spots(role customer/worker/queue)`·`price`). 규칙은 `WombatArea.Placement`(`CanPlace/CanMove` = 바닥 안 · 겹침 없음 · 입구 안 덮음 · 작업 자리 걷는 땅 + 입구에서 길 · `TryBuy/TryPlaceStored/TryMove/TryStore/ThingAt/PriceOf/IsMaxed/Stored`), 곳은 `ShopKinds`·`PlacedThings`·`Create/Destroy/OnPlacementChanged`만. 배치 격자 `ConfigTable.placeCell` 0.2. `BakeryLayout.Rebuild(cells, shelves, ovens, counters, capacity)`: 줄은 계산대마다(`QueueSlots(counter)`), 손님 자리는 진열대마다(`ShelfSpots(shelf)`), 표지판은 늘 왼쪽 앞. 계산대 여러 대(`Counters`, `ShortestQueue`, 마지막 한 대·줄 선 계산대는 못 치움, 웜뱃 그림은 첫 계산대 프리팹 것 하나). `SlotInteractable`·`HasSlot`·`place_shelf/place_oven` 삭제. UI: `EditModeView`(편집 버튼·패널·카드·`PointerRelay`·`EditCardView`) + `EditModePresenter`(**카드 탭 = 화면 가운데 근처 빈 자리에 사기/꺼내기(설계 20, `WombatArea.TryFindSpot` 격자 고리 탐색 반지름 3)**, 사물 끌기 = 옮기기, 패널에 놓기 = 보관, 빈 곳 끌기 = 팬, 흙 탭 = 파기 시트) → `MainScene`이 HUD `SetEditing`·`WorldManager.SetEditing/ShowGhost/Pan`에 잇는다. World: `GhostView`(초록/빨강), `BakeryView.Build`가 목록을 객체 키로 동기화(놓기·옮기기·치우기 모두 LayoutChanged 하나), `PlazaView`도 같음, 편집 중 파기 태그 상시, 카메라 `SetFollowing/Pan`. `LayoutChanged.Area`(광장도 낸다). 테스트 135(PlacementTests 6). **편집 하이라이트 C-2(2026-09-26 사용자 선택 「흐름·톡튐」)**: 옮길 수 있는 사물(진열대·오븐·계산대 몸체, 광장 장식)에 실루엣을 따라 도는 점선 외곽선 — `EditOutlineView.Attach(body)`가 몸체 스프라이트 사본 4장을 상하좌우 0.025 밀어 몸체 뒤(order −1)에 두고 `Resources/Shaders/EditOutline.shader`(알파 × 월드 격자 대각선 점무늬, `_Time`으로 흐름, 색은 renderer.color)로 그린다. 크림 = 옮길 수 있음, 노랑 = 잡음(`EditModePresenter.HeldChanged` → `WorldManager.SetHeld`). 편집 진입 때 사물마다 `Fx.Bounce` 한 번. 오븐 그림이 바뀌면 `Show`가 사본 스프라이트를 다시 맞춘다.
- **설계 22 점원 깨우기·대화·이모지 말풍선(2026-09-26, 구현·검증 완료, 기획서 v0.11)**: 말풍선은 Core `BubbleState`(손님 `Visitor.Bubble`·점원·`Wombat.Bubble` 공용, `BubbleTable` 행: 시트 칸·프레임·seconds 0 = 상태형)를 화면(`World/Bubbles.Show`, `bubble_sheet` 9칸 52px 더미 `make_bubble_sheet.py`)이 매 프레임 읽는다. 손님 두리번 wait·결제 heart(화남 「!!」는 2026-09-25 결정대로 없음), 광장 기분 heart, 해고 점원 angry(`PlazaVisitorEmoted` 사건·`Emote` 글자 삭제). 점원 딴짓 `IdleKind` Daze(wait)·Stroll(note, `WombatNav` 걷는 점으로)·Chat(chat, `IdlingClerkOther` 옆 `chatOffset`으로 가 마주 봄), 딴짓 중엔 `ClerkInteractable`(id clerk, range 1.0)로 `Placed`에(`SyncClerkThings`), 웜뱃이 range 안이면 question + 웜뱃 쪽 보기. 행동 `wake`(manual, 아이콘 `Actions/wake` 더미) → `Clerk.WakeUp`(EndIdle·alert·`wakeSkips` 판정 건너뜀·`ClerkWoke`) + `BakeryArea.StartDialogue("clerk_wake")`. `Dialogue`(순수: `DialogueTable` 줄을 `lineSeconds` 간격으로 `DialogueLine(speakerObject, textId, seconds)` → 끝에 `DialogueEnded`, 곳이 하나만 돌림). 글자 말풍선 = `bubble.png` 9-slice(border 8/16/8/8) + TMP(`VisitorView.Say`·`WombatView.Say`, `BakeryVisitorSpawner`가 `DialogueLine`을 잇는다, `BakeBubbles` 공용 굽기). **사용자 피드백 2건(같은 날)**: 딴짓 시간 = (3 + 87 × (100 − 일머리)/100) × 0.75~1.25(일머리 1이면 1분 넘게), **외출** `IdleKind.Outing`(outingChance 0.3): 구멍으로 걸어가 톡 뛰어들어 `Away`(`ClerkWentOut`) → `PlazaArea`가 같은 외형의 `PlazaVisitor`(`Clerk` 참조, 말풍선 `ShareBubble`, 들를 곳을 계속 돌고 없으면 그 자리 대기)와 `ClerkInteractable`(위치 = 그림)을 세운다 → 시간이 다 되거나 광장에서 깨우면 `ClerkReturning` → 그림이 문으로 들어가 `ClerkCameBack` → `Clerk.ArriveBack`(구멍에서 톡 나와 자리로). 외출 중 해고 = 점원 즉시 삭제 + 그림 `Dismiss`(계단). 돌아오는 길(깨웠거나 시간이 다 됨)은 딴짓이 아니다: `Clerk.Idling` = 딴짓 && !돌아오는 중이라 「?」·「♪」·깨우기 버튼이 다시 뜨지 않고, 구멍으로 뛰어드는 중에 깨우면 광장에 가지 않고 바로 다시 나온다(2026-09-26 버그 수정, 테스트 171). 광장 그림의 「?」·웜뱃 보기는 `PlazaArea.TickClerkFigures`, 대화 줄은 `PlazaVisitorSpawner`가 그림·광장 웜뱃 위에. EditMode 169(WakeDialogueTests 7 · OutingTests 4). 안 한 것: 실제 말풍선 아트·깨우기 아이콘(아트방), 선택지·대화 상자, 수다 상대의 반응(상대는 자기 딴짓 그대로), 다른 사업장으로의 외출(사업장이 생길 때).
- **설계 21 점원(2026-09-26, 구현·검증 완료, 기획서 v0.10)**: `Clerk : Visitor`(외형·걷기·톡 뛰기는 손님 뼈대, 할 일은 Bt 두 그루 — 일(들어오기 → 반복 바퀴)·퇴장) + `BakeryArea.Clerks.cs`(명부: `Clerks`·`Candidates`(처음 볼 때 채움)·`ClerkOf`·`WageFor`·`Negotiate`·`TryHire`·`Fire`·`TryRefreshCandidates`, 월급은 점원마다 `UntilPay`). 오븐 점원 바퀴: 자리 → `OvenInteractable.LastBread`(없으면 해금 첫 빵, 사용자 플레이 피드백 2건째) 굽기 → 다 구울 때까지 → `WombatArea.TryDo(take_out)` → 진열대 왼쪽 앞(−0.6, −0.7)로 → `TryDo(fill)` → 딴짓 판정(`Looking` = 점 셋). 계산 점원: 자리 → `CounterInteractable.Served`가 늘 때까지 → 딴짓. 계산대 `Tick`은 점원이 있으면 `clerk.Working`, 없으면 웜뱃 자리 붙기. 웜뱃은 `IsStaffed` 사물의 auto 행동만 건너뛴다. 사물 보관 → `Fire(Stored)`, 옮기기 → `Clerk.Repath`(서 있어도 새 자리로). `Negotiation`(순수, IRandom): 표시 왕복·`Stop` 색 판정·유지/인하/인상 가중치·금액 난수. 데이터 `ClerkTable`(oven 12·counter 9)·`ClerkConfigTable`(주기 60·후보 5·새 후보 50·skew 2·zones·names)·`VisitorTable` v9 `role`(v05 회색 웜뱃 = `make_wombat_palette.py` 팔레트 D, 손님 시트 형식 104px). UI `ClerkPopupView`(점원 버튼 = 편집 버튼 아래, 팝업 세 상태: 목록/고용할까 창/협상) + `ClerkRowView` + `ClerkPresenter`, `UIBaker.Clerk.cs`. World: `BakeryVisitorSpawner`가 점원도 `VisitorView`로(든 빵 `ShowCarry`, 해고 「!!」). 사건 `ClerkHired/Fired(reason)/Left`·`CandidatesChanged`. EditMode 156(ClerkTests 16). 버그 4: 자리 앞 마지막 한 걸음(0.09)에서 옆모습 한 프레임 → `Mover`는 0.15보다 짧은 선분에서 방향을 바꾸지 않는다. 사용자 플레이 버그 1건 수정: 한 바퀴가 성공하면 `BtRepeat`이 성공을 돌려 「들어오기 → 반복」 순서가 처음부터 다시 돌아 구멍에서 다시 나왔다 → 들어오기는 순서 밖 한 번, 바퀴는 `BtSequence` 하나를 계속 틱(끝나면 스스로 되돌아감). 버그 3(좌우 떨림): 진열대가 가득 차면 바퀴가 매 프레임 되돌아 `WalkTo(자리)`를 반복했고, 격자 밖 자리(x −1.6875)에서 A*가 격자 점(−1.6)을 거쳐 돌아오는 한 걸음이 떨림이 됐다 → `Mover.WalkTo`는 이미 그 자리면 길 없음, 점원은 진열대 자리가 날 때까지 `TickWaitShelf`로 서서 기다린다. 안 한 것: 산책·수다 딴짓, 오프라인 월급, 점원 간 이동, 줄 탭 시 카메라 이동, 해고 토스트는 Unpaid만(플레이에서는 점원이 벌어 안 잘림). 점원 그림이 웜뱃과 같은 자리(계산대)에 겹침은 그대로.
- **설계 19 웜뱃 시스템 이동(2026-09-26)**: `Wombat.Guide(nav, target, arrive)` = 길 따라 걷기(`Mover.WalkTo`), `Guided`, 조이스틱을 건드리면 `Walk`가 그 자리에서 길을 지운다(`Mover.Place`), `Stop`도 지움. 첫 쓰임은 계산대: 계산은 `CounterInteractable.WombatAtSpot`(자리 0.05 안)일 때만 흐르고, 줄 머리가 기다리는데 range 안·대상·자리 밖·조이스틱 놓음이면 `Guide(Layout.WombatNav, WorkerSpot, Down)`. 앞으로 미션 가이드도 같은 호출. 테스트 137(계산 붙기 2). **웜뱃 조작(설계 09 v0.4)** https://claude.ai/artifact/G8DPXpRRK9p6CVEWL586k3 : 조이스틱 + 상호작용 버튼. 행동은 `ActionTable`(manual/auto — 열기·파기만 버튼, 꺼내기·채우기·계산·**굴 이동(2026-09-26, exit·enter auto: 구멍·문 앞 띠(가로 ±0.75, 바닥선에서 0.25 이상 위, `PassageInteractable.DistanceTo`)에 들어오면 거리만으로 바로. 들어온 웜뱃은 바닥선(띠 밖)에 서고 `Wombat.WaitRelease`로 조이스틱을 놓을 때까지 굴을 등지고 서서 되돌아가지 않는다)**은 자동), 사물은 `InteractableTable`(range·행동 순서). 손님 동선 설계 v0.3 https://claude.ai/artifact/BjZ3x1F5Y5ePaQKU8y9qou .
- **연출 2순위(설계 10, 구현·검증·커밋 완료)** https://claude.ai/artifact/UqHs95xJMcQyXfLeUk95ML : 오븐 상태 표시(빈 = 오르내리는 화살표, 대상이면 끔 / 다 구움 = 원판 + 식빵, 아트방 제공), 빵 나르기 비행, 업그레이드·새 사물 튀기기(`WombatArea.Upgraded`), 공용 `PressScale`, 효과음 2개(`SoundTable`, 합성 `Audio/Source~/make_sfx.py`. 화난 퇴장 give_up은 2026-09-25 삭제). 든 빵은 앞발에서 위로(뒷모습이면 몸 뒤), 손님별 자리는 `VisitorTable.carryAt`(펭귄만 head). EditMode 91/91.
- **아트**: 빵집 소품·배경·손님 4종·웜뱃(앞·뒤·옆), 정면 식빵, 발바닥 코인(결제·UI), 오븐 타이머·불·연기·상태 표시, 조작 UI 실제. 결제 코인은 가게 그림 위(order 59), "+10"은 0.42. 발밑 그림자·효과음 2개·굴 A2 재료(`wall_face`·`wall_tile`·`arch`, `make_burrow_a.py`)도 실제(2026-09-24). 규칙은 `아트-프롬프트.md` 0장.
- 알려진 문제: 빌드 타깃 Standalone(결정은 Android IL2CPP ARM64) · 세로 줄(x −2.6)은 간격 0.8 < 키 1.1이라 앞 손님이 뒷손님 몸을 가림(깊이상 정상) · 결제 피치는 줄을 계속 받으면 1.30에 머묾(`comboSeconds` 2.5, 조작 손맛 때 들어 볼 것) · `Assets/_Recovery/0.unity`(미추적, 지워도 됨).

## 다음에 할 것

0. **설계 22 점원 깨우기·대화·말풍선(2026-09-26, 기획서 v0.11) — 구현·검증 완료, 사용자 플레이 피드백 대기**. 아트방 요청: 이모지 말풍선 시트 9칸(52px, 지금 더미 순서 그대로), 깨우기 버튼 아이콘, 점원 버튼 아이콘. 그 앞 **설계 21 점원 — 커밋 완료(사용자 플레이 버그 4건 수정 포함)**. 후보: 점원 아트(앞치마·모자 겹 그림은 같은 104px 캔버스 24프레임 오버레이로, 팔레트는 `make_wombat_palette.py` 사전에 줄 추가). 그 앞 **설계 20 — 커밋 완료**: 사용자가 「배치 안 됨」이라 한 것은 카드가 끌기였기 때문 → 탭하면 화면 가운데 근처 빈 자리에 생성, 카드 글자 11 굵게·값 그림자+금색·아이콘 가운데(피벗이 위면 preserveAspect가 위로 붙임). 그 앞 **설계 19 — 커밋 완료(기획서 v0.8)**. 그 앞 **설계 18 자유 배치·편집 모드 — 커밋 완료(기획서 v0.7)**. 사용자 플레이 버그 2건 수정: 계산대 프리팹 원점을 밑변으로(옛 「줄 윗변」 원점이라 그림이 1.55 아래에 그려져 못 잡고 손님 자리가 어긋남), 진열대를 전부 보관하면 손님이 null 진열대로 가던 것(빵 못 찾음으로 나감). **계산대 방향(2026-09-26 사용자)**: 웜뱃은 계산대 뒤 = 위(worker +0.4, 발이 계산대 그림에 묻힘 — 웜뱃 걷는 땅에서 계산대는 밑변 선만 막고 배치 검증의 웜뱃 자리는 그 사물을 뺀다, 계산 중 앞모습), 손님 줄은 앞 = 아래(queue −0.8, 머리는 위를 봄). 편집 하이라이트(도는 점선 + 톡 튐) 완료. 남은 것: 편집 버튼 아이콘은 삽(임시, 아트방에 망치·연장 아이콘 요청), 시작 배치를 예쁜 도면으로(아트방), 저장 형식(`things` ix·iy)은 저장 기능 때. **배치 점수(프레스티지·아늑함 → 손님 수)는 개발 막바지에** 시스템으로 붙이기로 기록만(레퍼런스: Two Point 프레스티지·Tavern Master·Kairosoft 인접 콤보). 점원은 계산대 worker 자리에 세우면 된다. **저장 메모(2026-09-26 조사)**: 유저 = JSON 문서 한 장(`version`·`savedAtUtc`·coins·unlocked·upgrades·burrow.cells·things[칸/좌표·종류·상태]·plaza.decor), 로컬 → 출시 때 플랫폼 클라우드 세이브(Play Games·iCloud, $0) → 필요해지면 Unity Cloud Save/Firestore 문서 저장(1만 DAU 월 $0~10, 쓰기 횟수가 비용). Core `SaveData` + Game `ISaveStore`.
1. **코드 리뷰**(코드 지도·다이어그램 https://claude.ai/artifact/TJ7EJPTT4Z6HyVgfNxfJJo , R2 결정 기록 설계 15 https://claude.ai/artifact/M3coGPpEDm55Yxn9KwnJ4U ): R1(사용자 파일별) → R2(에이전트가 사용자 기준으로 전부 자가 리팩토링, 2026-09-24) 완료. 사용자가 결과를 보고 다시 시키면 R3. R3(설계 16 사건 버스) 완료(2026-09-25). 다음 후보는 없음. 설계 14 배치 규칙은 위 0번 자유 배치 논의로 대체.
1. **광장 다음**: 장식 사기·옮기기는 설계 18 편집 모드에 흡수(설계 12 꾸미기 완료) → 다른 가게 문 자리(사용자 더 고민). https://claude.ai/artifact/TeZ7mszCNa8gNiyDeFPapw
2. **조작 손맛**: 상하좌우만 이동은 2026-09-25 실험 뒤 되돌림(레퍼런스: 같은 장르·픽셀 탑다운 모두 자유 이동 + 4방향 그림). 사용자 3분 플레이로 wombatSpeed 3·carryCapacity 6·사물 range(진열대·오븐 1.3, 계산대·빈 자리 1.0, 파기 0.6) 조정. 효과음 음량·느낌도 같이 들어 본다.
3. **직원 완료(설계 21)**. 그다음 벽 → 오프라인·저장(점원 월급 포함) → 굴 밖·두 번째 가게(점원 팝업 가게 탭 확장).
4. **연출 3순위**(점검 1차 5장): 굴 넓히기 클라이맥스, 웜뱃 발밑 먼지, 햅틱, 팝업 등장.
5. **UI 디벨롭**: 오프라인 팝업 등 다른 화면을 같은 부품으로.

## 도구 함정 (Unity CLI)

- **폰트(2026-09-26 사용자)**: 정자체(9·11·14)의 1px 획이 얇다는 지적에 ① 전부 11 굵은체 → 「다 똑같아 보인다」 ② 아틀라스에서 2px로 굵히기 → 「굵은체와 너무 비슷하다」 ③ 오른쪽 아래 1px 반투명 그림자 → 「혼자만 그림자라 굵은체와 안 어울린다」 세 번 반려. **결론: 정자체는 원래대로(1px)**, 제목 14·보조 9·굵은 11 유지.
- **점원 UI 폴리싱(2026-09-26 사용자: 「·」로 정보 두 개 이상을 한 글에 넣지 않는다)**: Codex 시안 3장(두 줄 카드·칸 표·명찰 카드) → 사용자가 C 명찰 카드 + A 붉은 게이지 + B 표를 골라 2차 3장(작은 표·라벨 줄·세 칸 표) → **D1 작은 표** 확정(버튼은 세로 28로 카드 가운데 높이, 고용도 같게). **굽기는 시안 그림 그대로**(사용자: 「웜뱃 사진·폰트 빼고 100% 같아야」): `Sprites/UI/Source~/make_clerk_ui.py`가 시안(raw/clerk_d1.png) 격자를 재서(4.45px) 211×375칸으로 줄이고 패널·닫기·탭·카드·초상 틀·빈 틀·배지(선/채움)·자리 알약·표·게이지·코인·버튼 16조각을 칸 좌표로 잘라 `clerk_*.png`(한 칸 = 캔버스 5px, ui_slices.json `ppu` 20)로 쓴다. 글자 자리는 비우고, 둥근 모서리 밖 크림은 투명으로. `UIBaker.Clerk`는 시안 칸 수(`k_Cell`)로 배치: 카드 178×49, 초상 180px(틀 안 RectMask2D), 이름은 틀에서 8칸 띄우고 자리 알약이 이름 뒤를 따른다, 요약 줄은 마지막 카드 아래, 목록은 세로 스크롤. 후보 줄은 배지 없이 표 머리글 「기본 월급」, 고용할까 창도 같은 표. 요약 두 칸(점원 1/2 · 월급 합), 후보 제목 「오븐 1 후보」, 협상 제목 「월급 협상」, 새 후보 버튼 = 글 + 값 알약. StringTable 18 폴리싱(clerk_summary_*·clerk_col_*·clerk_away). 시안 페이지는 다음 작업 때 지운다.
- **폰트는 정적 아틀라스(2026-09-24)**: 갈무리 4종은 화면 글자(`Texts.Characters` = ASCII + 말줄임표 + 문구·빵·업그레이드 이름)를 미리 구운 Static이고 TMP 기본 폰트도 Galmuri11이라 플레이해도 폰트 파일이 안 바뀐다. 문구에 새 글자가 생기면 `FontAssetTests`가 실패 → `ZooTycoon/Bake/Fonts`(에셋을 그 자리에서 다시 채워 GUID 유지). 폰트 에셋은 `git checkout`으로 되돌리지 않는다(에디터 메모리와 어긋나 글자가 깨지던 원인). 다른 부산물: 손님 시트 메타(임포터가 spriteID를 새로 만듦), `CoinPopup.prefab`(Bake/Bakery가 금액 글자를 다시 붙임) — 기능과 무관하면 `git checkout` 후 `AssetDatabase.Refresh`. 에디터는 한 번에 한 세션만: 다른 방이 쓰는 중이면 끝났다는 메시지를 받은 뒤에 쓴다.
- 구현·에셋 작업 전 `editor_status`의 `playMode` 확인. 플레이 중이면 씬 저장·프리팹 교체가 틀어진다(2026-09-23: 사용자가 직접 플레이하던 중 Bake/UI → 버튼 대상 그래픽·시트 활성·조이스틱 알파가 실행 중 값으로 저장됨, recompile → 도메인 리로드로 손님 NRE 21건. 굽기·recompile 직전마다 확인). **`run_tests`를 부르는 그 명령 줄 안에서** `playMode=stopped`를 다시 확인한다(2026-09-23: 사용자가 에디터에서 플레이를 켜 둔 상태에서 호출 → 150초 타임아웃 → 에디터 멈춤 → 재시작). `run_tests`는 반드시 `editor_stop` 후 `playMode=stopped`에서 `--mode EditMode --timeout 150`으로 **한 번만** 호출(결과는 `--format json`을 파일로 받아 `Summary`를 본다. 결과가 중첩 객체라 줄 grep이 비어도 이미 돌았다 — 다시 부르기 전에 `test_status`, 2026-09-23)(같은 명령을 파이프로 두 번 부르면 두 번째가 겹쳐 테스트 러너가 깨지고 파이프라인이 막힌다. 2026-09-18). 막히면 `taskkill //F //PID`로 에디터를 죽이고 **`ProjectTycoon/Temp/UnityLockfile`을 지운 뒤** `unity projects open`(잠금 파일이 남으면 모달이 떠 영원히 연결되지 않는다).
- `eval`은 5초 넘으면 타임아웃 오류를 콘솔에 남긴다(스프라이트 여러 장 임포트하는 굽기). 작업은 끝나 있으니 프리팹을 조회해 확인하거나 `run_script`로 굽는다.
- 프리팹을 다시 구운 뒤에는 플레이를 새로 시작한다(이미 돌던 세션은 옛 프리팹으로 만든 개체를 그대로 쓴다).
- `capture_game_view --save_path`는 프로젝트 안 경로만 받고 `Temp/x.png`가 `Assets/Temp/`에 저장된다(에셋으로 임포트됨). 확인 뒤 `AssetDatabase.DeleteAsset("Assets/Temp")`.
- 에디터가 포커스를 잃으면 플레이 프레임이 멈춘다(`editor_focus`도 곧 풀림). 긴 관찰은 플레이 중 `Application.runInBackground=true`를 eval로 켠다(런타임만, 저장 안 됨).
- 에디터 창이 뒤에 있으면 플레이 프레임이 멈춘다 → `editor_focus`. **편집 모드에서 모든 명령이 30초 타임아웃되는데 포트는 ready·프로세스는 응답·모달 없음이면 에디터 메인 스레드가 멈춘 것**(2026-09-23 10:58, 플레이 종료 `editor_stop` 직후 `eval`로 `AssetDatabase.DeleteAsset(\"Assets/Temp\")`를 부른 다음 발생. 라이선스 클라이언트 로그의 주기 요청이 그 시각에 끊기고 CPU 0, 전경으로 올려도 반응 없음). 파이프라인은 명령을 `EditorApplication.update`에서 꺼내므로 update가 멈추면 배경 명령까지 실행 게이트(한 번에 하나) 뒤에서 같이 죽는다 — 재시도를 1분마다 보내면 게이트를 더 막으니 하지 말 것. 복구는 taskkill → `Temp/UnityLockfile` 삭제 → `unity projects open`뿐. 예방: 플레이 종료 뒤 `editor_status`로 `playMode=stopped`를 확인한 다음에 에셋 삭제/임포트를 부른다. Interaction Mode는 No Throttling(레지스트리 값 1)로 바꿔 둠(배경 절전 배제용, 이 멈춤의 원인은 아님).
- 같은 경로에 에셋을 다시 만들 때는 `DeleteAsset` → `CreateAsset` → 프리팹 `ImportAsset(ForceUpdate)` → `recompile` 뒤 플레이(옛 객체가 "이름 없는 Tile"로 남는다).
- 콘솔은 세션을 넘어 남는다. 컴파일 판정 전에 `clear_console` → `recompile` → `console --json`(옛 오류를 새 오류로 오인한 적 있음, 2026-09-23). 명령 목록은 `unity list`.
- 콘솔 로그 조회는 `console --json`(`console_logs` 명령은 없다. 잘못 부르면 그 호출 자체가 콘솔 오류 1건으로 남는다).
- 여러 줄 C#은 `run_script --file --entry --mode ephemeral`. 일회성 조립·조회 스크립트만 scratchpad, 프리팹을 통째로 만드는 스크립트는 `Scripts/Editor`에 MenuItem으로. `GetComponent ?? AddComponent`는 가짜 null로 실패 → `TryGetComponent`.
- 플레이 중 상태는 `GameManager.Instance.State/...`를 `run_script`로 읽는다. 1초짜리 연출은 캡처 대신 상태 조회. 정확한 화면 좌표는 `capture_game_view --source camera --width 1080 --height 1920`.
- 콘솔의 `TreeViewController NullReference`·`GUIClips`는 Hierarchy 창의 에디터 내부 오류. 게임 오류 판정은 `error CS`·게임 스택만.
- Git Bash는 `export MSYS_NO_PATHCONV=1`. 에디터 열기 `unity projects open ProjectTycoon`, 연결 확인 `unity pipeline list`. 에디터 명령은 `unity cmd <이름>`(`unity editor_status`는 unknown command), 메뉴는 `unity cmd menu --path "ZooTycoon/Bake/Bakery"`. `--json` 결과는 `"playMode": "stopped"`처럼 콜론 뒤에 공백이 있다(grep 패턴에 넣지 않으면 조건이 늘 거짓, 2026-09-24).
- 에디터 게임 뷰가 가로 1280×720이라 `--source screen` 캡처는 가로로 나온다. UI까지 세로로 확인하려면 플레이 중 캔버스를 `ScreenSpaceCamera` + `sortingOrder` 5000으로 바꾼 뒤 `--source camera --width 540 --height 960`(scratchpad `TapCheck.cs`). 재컴파일은 플레이가 끝날 때까지 미뤄지므로, 플레이 중 컴파일 0은 새 코드를 검사한 결과가 아니다.
