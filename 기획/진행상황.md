# 진행 상황 (세션 인수인계)

세션을 시작할 때 이 파일을 먼저 읽고, 세션이 끝날 때 갱신한다. 상세는 각 설계 문서와 커밋 이력에 있으므로 여기에는 "다음 세션이 알아야 할 것"만 짧게 둔다.

## 현재 상태 (2026-09-14)

- 기반 세팅 완료: asmdef 6개, JSON 테이블 4개, GameKit v0.1 로컬 연결(`file:../../../UnityGameKit`), TMP 임포트.
- GameKit v0.1: MonoSingleton, EventManager, PoolManager, TableManager, DataManager, UIManager. 저장소 jiwon000512/UnityGameKit.
- 게임 코드: 아직 없음. 씬 `Main.unity`는 카메라·조명만.
- 기획서 v0.6, 미결 없음. 아트 톤 샘플만 남음.

## 구현된 기능

(없음)

## 다음 세션에서 할 것

- 설계 01(`기획/설계/01-코어-규칙-계층.md`) 검토 확정 후 구현.

## 알아야 할 것

- 개발 절차: 설계(코드+QA) → 사용자 검증 → 한 번에 구현(Unity CLI) → QA → 보고. 기능 하나 = 세션 하나.
- 에디터가 열려 있으면 `unity pipeline list`로 연결 확인 후 라이브 CLI 사용. Git Bash에서는 `export MSYS_NO_PATHCONV=1`.
- 리소스는 JSON 경로 칼럼(`animals.sprite`)으로 참조. 더미 리소스가 들어 있고 사용자가 같은 경로로 실제 리소스를 덮어쓴다. 더미를 만든 경로 목록은 아래 표에 유지한다.

## 더미 리소스 (사용자가 교체할 것)

| 경로 (Assets/Resources/) | 용도 | 현재 |
|---|---|---|
| (없음) | | |

## 알려진 문제

(없음)
