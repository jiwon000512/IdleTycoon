# 진행 상황 (세션 인수인계)

세션 시작에 읽고 끝에 갱신한다. **세 절만 두고 각 절은 10줄 안쪽**으로 유지한다. 지난 일은 여기 쌓지 않는다 — 코드 결정은 `설계/00-구현-요약.md`, 기능별 기록은 `포트폴리오.md` 4장, 경위는 커밋 로그. 2026-09-17까지의 상세 원문은 `진행상황-이력.md`(세션 시작 시 읽지 않는다).

## 현재 상태 (2026-09-18)

- 코드: 기능 01~07-5 완료·커밋(마지막 `c161f41`). 코드는 **기획서 v0.16 상태(뽑기 있음)**, 기획서는 v0.22(뽑기 삭제·존·입주·번식은 미구현). 설계 08은 방향이 정해질 때까지 보류.
- 기획: 루프 기획 7회차까지 끝, **사용자 답 대기**. 방향 자체가 열려 있다(타이쿤 유지 여부, 프로토타입 여부). 채점 기준은 `재미-프로필.md`. 회차 원문은 `루프기획/`(round*.body.html · 생성N · 목업N · 조사3), 7회차 아티팩트 https://claude.ai/artifact/BdqaeC64pmJgAoQMRdFBvb (1위 웜뱃 회전굴 · 2위 굴 온천 · 3위 빵집 러시).
- 아트: 실제 = 웜뱃(사용자)·타일 3종·울타리·숲 3종·경계선. 더미 = 관광객·시설 4종·자물쇠·UI 프리팹. 현황 표는 `아트-프롬프트.md` 3장.
- 프리팹 굽기는 에디터 메뉴 **ZooTycoon/Bake/Zone · Branch01**(`Scripts/Editor/ZoneBaker.cs`·`BranchBaker.cs`, 2026-09-18에 scratchpad에서 옮김. 에디터에서 컴파일·실행 확인 필요).
- 미커밋: 루프 기획 4~7회차 파일, `재미-프로필.md`, `NotoSansKR SDF.asset`, `panels.py`, 2026-09-18 규칙 개정(진행상황 축소·이력 파일·코드-규칙 v1.9·아트-프롬프트·CLAUDE.md·Editor 스크립트 2개).
- 알려진 문제: 빌드 타깃 Standalone(결정은 Android IL2CPP ARM64) · 잔디 꽃이 3칸 주기로 반복 · 시설 단계별 그림 없음 · 가변 폰트라 텍스트 전부 Bold.

## 다음에 할 것

1. 사용자 답: 7회차 결과에서 무엇을 고르나, 프로토타입으로 검증하나. 규칙(코드-규칙 6장): 기획 회차는 2회를 넘기지 않고 하나를 1주 프로토타입(별도 씬)으로 검증한다.
2. 방향이 정해지면 설계 08(존·입주·번식 + 뽑기 삭제)을 07-5 기준으로 고쳐 확정 → 구현. 삭제 대상: `Core/Gacha` 5, UI `Gacha*` 4, 테스트 `Gacha*` 3, 프리팹 2, `grades.json`, `game_config.gacha`, `strings` `gacha_*` 8, `animals`의 grade·gachaWeight·variantId, `ZooState.PullCount/RecordPull`.
3. 남은 더미 아트(관광객 1종·시설 4종·자물쇠)를 스타일 가이드(아트-프롬프트 0장)로 한 세션에 정리. 절차는 시안 3장 비교 → 선택 → 굽기.
4. 그 뒤 09 의상 → 10 저장·오프라인 → 11 도감.

## 도구 함정 (Unity CLI)

- 구현·에셋 작업 전 `editor_status`의 `playMode` 확인. 플레이 중이면 씬 저장·프리팹 교체가 틀어진다. `run_tests`는 반드시 `editor_stop` 후 `playMode=stopped`에서 `--mode EditMode --timeout 150`으로. 플레이 중 돌리면 파이프라인이 영구히 막힌다.
- 에디터 창이 뒤에 있으면 플레이 프레임이 멈춘다 → `editor_focus`. JSON·에셋을 고친 뒤엔 `AssetDatabase.ImportAsset(path, ForceUpdate)`로 강제 임포트한 뒤 플레이.
- 같은 경로에 에셋을 다시 만들 때는 `DeleteAsset` → `CreateAsset` → 프리팹 `ImportAsset(ForceUpdate)` → `recompile` 뒤 플레이(옛 객체가 "이름 없는 Tile"로 남는다).
- 여러 줄 C#은 `run_script --file --entry --mode ephemeral`. 일회성 조립·조회 스크립트만 scratchpad, 프리팹을 통째로 만드는 스크립트는 `Scripts/Editor`에 MenuItem으로. `GetComponent ?? AddComponent`는 가짜 null로 실패 → `TryGetComponent`.
- 플레이 중 상태는 `GameManager.Instance.State/...`를 `run_script`로 읽는다. 1초짜리 연출은 캡처 대신 상태 조회. 정확한 화면 좌표는 `capture_game_view --source camera --width 1080 --height 1920`.
- 콘솔의 `TreeViewController NullReference`·`GUIClips`는 Hierarchy 창의 에디터 내부 오류. 게임 오류 판정은 `error CS`·게임 스택만.
- Git Bash는 `export MSYS_NO_PATHCONV=1`. 에디터 열기 `unity projects open ProjectTycoon`, 연결 확인 `unity pipeline list`.
